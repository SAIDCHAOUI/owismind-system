# Storage layer for the OWIsMind WebApp backend.
# Direct SQL via SQLExecutor2 on the admin-configured PostgreSQL connection
# (chosen in the webapp Settings, see sql_config) - no DSS Flow at runtime.
