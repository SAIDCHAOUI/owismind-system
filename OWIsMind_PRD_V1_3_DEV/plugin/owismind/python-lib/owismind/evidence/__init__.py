"""Evidence Studio - re-runs the agent's stored SELECT scope, read-only.

Pure analysis (sql_parse, query_builders, whitelist) is dataiku-free and
unit-tested; only service.py touches the DSS runtime.
"""
