# Flow (OWIsMind_LAB)

Vide cote repo : les datasets du benchmark (historique des runs, resultats) sont
crees et remplis par le scenario DSS `Run_Benchmark`, pas par des recettes codees
dans le repo. Les 3 steps Python du scenario vivent dans
`../project-library/python/benchmark/dss_steps/` (step_run_matrix, step_judge,
step_aggregate). Carte complete : `../README.md`.
