# Session 2026-06-01 - Setup contexte projet (Claude Code)

## Objectif
Mettre en place le contexte projet optimal pour Claude Code (CLAUDE.md, mémoire, settings, hooks,
skills) APRÈS exploration réelle du repo. Aucune écriture de code applicatif, aucun build, aucun zip.

## Fait
- **Exploration** : cartographie du repo ; 4 sous-agents pour lire/synthétiser `cadrage/` (cahier des
  charges + 2 guides techniques + snippets) et `maquette/` (~12 800 lignes HTML/JS/CSS).
- **Identifiants réels confirmés** sur disque + guides (figés dans `PROJECT_STATE.md`) : plugin
  `owismind`, webapp `webapp-owismind-ai-agents`, package `owismind`, resource `owismind-app`,
  racine `Plugin/owismind/`, staging `Plugin/ready-for-dataiku/`, SQL `SQL_owi`, project key
  `OWISMIND_DEV`, agent revenue `agent:rNTZ781a`, API `/owismind-api/*`.
- **Fichiers créés** :
  - `CLAUDE.md` (racine, < 200 lignes) - identité, `@memory/CONTEXT.md`, protocole mémoire,
    identifiants, règles non négociables, renvois cadrage.
  - `memory/CONTEXT.md` (courte), `memory/PROJECT_STATE.md` (longue), `memory/LESSONS.md` (L001-L006),
    `memory/sessions/2026-06-01-setup.md`.
  - `.claude/settings.json` - permissions (allow read/build/staging ; **deny stricte de tout install**
    + edition des dossiers générés).
  - `.claude/hooks/guardrail.sh` (PreToolUse) + `.claude/hooks/session-start.sh` (SessionStart), testés.
  - `.claude/skills/{build-plugin,package-plugin,log-session}/SKILL.md`.
  - `Plugin/owismind/python-lib/CLAUDE.md` + `Plugin/owismind/frontend/CLAUDE.md` (règles scopées par
    chemin via CLAUDE.md imbriqués - mécanisme fiable, à la place de `.claude/rules/`).

## Décisions
- **Mémoire** : réutilisation du dossier `memory/` existant (anglais) plutôt que créer `memoire/`.
- **Permissions** (choix utilisateur) : auto pour lecture/build/staging ; **interdiction stricte et non
  négociable de tout `install`** (npm/pip/brew/yarn/pnpm/npx) → l'agent demande, l'utilisateur installe.
- **Safety Dataiku** : règle non négociable ajoutée (CLAUDE.md + hooks + rules) - toujours se demander si
  le code est risqué/lent/surchargeant pour l'instance.
- **Code en anglais** (code + commentaires), standard pro ; communication utilisateur en français.
- Règles par chemin via `CLAUDE.md` imbriqués (et non `.claude/rules/`) pour garantir le chargement.

## Validé / non validé
- Validé : noms réels, structure plugin, chaîne build/package (mécanique guides), hooks (smoke tests OK :
  install bloqué exit 2, édition dossier généré bloquée, `npm run build` passe, rappel sur backend.py).
- Non validé / à faire (code) : backend modulaire `routes.py`, SQL/agent/streaming en backend Flask
  (validés notebook only), conversion maquette → Vue 3, Python 3.11/FastAPI.

## Prochaines étapes
1. Remplacer `backend.py` (template DSS) par bootstrap modulaire + `python-lib/owismind/api/routes.py`
   avec `/owismind-api/ping`.
2. Nettoyer `webapp.json` (params scaffold) ; probes storage + agent depuis Flask.
3. Streaming SSE `/owismind-api/chat/stream` ; puis conversion maquette → Vue 3 (design system d'abord).
