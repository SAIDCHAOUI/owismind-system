# Session 2026-06-18 - Refonte UI "charte Orange" + fiches d'agent rédigées par l'admin

> Session **design uniquement** demandee par l'user (PDG, sensible a la marque Orange). Frontend Vue +
> un minimum de backend (stockage des fiches d'agent). Aucune logique d'agent touchee.

## Objectif
Rendre l'UI plus **chartee Orange** et **moins "IA generique"** ; refaire la **sidebar repliee en rail**
(+ / aide ? / profil) ; ameliorer les **cartes d'agents** et surtout la **fiche d'agent** (jugee "flagrant
IA") ; **bannir les descriptions hardcodees** (rendues par l'admin, stockees en base) ; ameliorer la
**console admin** (selection + formulaire de fiche) ; **tout en anglais** + renommer Settings -> "My account" ;
petites **animations** ; **securite/surete instance** verifiees par un agent dedie.

## Fait
- **Fondation** (`tokens.css`, `base.css`, `icons.js`) : couche additive sobre (graisses, `--rail-w`,
  `--square`, motion, `u-rise`), **tokens neutres existants inchanges** -> chat/Evidence ne bougent pas.
- **Backend fiches d'agent** (le seul changement backend) : `validate_agent_meta()` (pur, **ne leve jamais**,
  borne tagline 120 / desc 700 / caps 8x120 / tools 16x48, **whitelist d'icones** = registre front, enum
  badge, sanitize control-chars) ; `routes.py` : `/admin/agents` POST capture+valide la `profile` par agent
  (ordre preserve, `listing_cache` -> moins d'appels DSS) et `/agents` l'expose **sans fuite** d'`agent_id`/
  projet ; la fiche voyage **dans le JSON `enabled_agents` existant de `webapp_settings_v1` (PAS de nouvelle
  table)**. +8 tests (`test_validation.py`).
- **Front** : `services/backend.js` (profile dans save/`/agents`) ; **`agentMeta.js` SUPPRIME** (fin du
  hardcode) ; `AgentsView` lit les fiches admin (recherche + cartes + fiche editoriale) ; `AdminView`
  refondu (selection Ajouter/Ajoute + **modale d'edition de fiche** icone/badge/desc/capacites/outils +
  apercu) ; `Sidebar` **rail** (carre de marque qui deplie, +, Agents, puis aide ? au-dessus du profil ;
  liste gardee montee en `v-show` -> pas de re-fetch) ; `AppLayout` grille `--rail-w` ; `SettingsView` ->
  "My account" ; i18n **anglais par defaut** (FR garde) + ~70 cles fr/en.
- **2 revues adversariales** (Workflow) : (1) securite/correctness/a11y/cohesion/proprete -> 0 critical,
  1 medium securite (`validate_agent_meta` `TypeError` sur icone/badge non-hashable) + 1 medium correctness
  (Sidebar sur-fetch /conversations quand repliee au demarrage) + corrections a11y -> **tout corrige** ;
  (2) audit final securite+mauvaises pratiques -> **securite CLEAN** (25 entrees adverses, 0 fuite, XSS sur,
  pas de surcharge instance) + 3 LOW (doc/`_clean_str` exotic-whitespace/token mort) **corriges**.
- **Revert apres retour user** (je m'etais permis des ajouts non demandes) : ChatEmpty (titre **orange**,
  pas de logo centre, pas de glow), bouton "New conversation" **neutre**, avatars **ronds gris**, **glows**
  + grosses ombres (`--shadow-pop`) supprimes, **puces carrees** d'eyebrow retirees, `--orange-text` ->
  `--orange`/`--orange-deep` (palette de la charte), coins de carte 10px, tokens speculatifs retires.
  Puis 2e retour user : **focus-ring orange global retire** (c'etait le contour orange sur la zone de
  saisie) -> focus gris d'origine sur le PromptBar, focus orange-soft sobre sur les seuls champs refondus,
  token `--focus-ring` supprime ; **bouton de depli restaure dans le header** (`MainTop`, `v-if collapsed`).
- **Charte Orange officielle fournie par l'user** (a coller dans `CLAUDE.md`) : orange = accent rare sur
  actions/etats actifs/accroches, fond blanc, texte noir, aplats, ombre 1px, carre-net, Helvetica, pas de
  degrade/blur/emoji.

## Decisions
- Stockage fiche = **dans le JSON whitelist existant** (pas de nouvelle table) : simple, sur, borne.
- Icone admin = **whitelist serveur == registre front** (defense en profondeur ; inconnu -> rien rendu).
- **Discipline de marque** : ne PAS sur-designer. L'orange est un accent rare ; revenir a l'original pour
  tout ce qui n'est pas explicitement demande. (Lecon L091.)
- Le focus-ring global etait une mauvaise idee (touche le chat valide) ; focus par-champ uniquement.

## Valide / non valide
- **Valide local** : build Vite OK, **124** tests frontend, **438** backend (+8 fiche), zip propre
  (**79 entrees, `index-Bd4XhFvS.js`**), **zero tiret** cadratin/demi, **0** degrade/blur/glow/emoji ajoute.
- **NON valide DSS** : rendu reel + flux fiche d'agent (creation/edition/affichage) intestables hors instance.

## Prochaines etapes (DSS)
- **Upload du zip** + ⚠️ **REDEMARRER le backend** (python-lib change : `validation.py` + `routes.py`).
- **Administration > Agents** : ouvrir "Edit profile" sur l'orchestrateur et **remplir la fiche** (tant
  qu'elle est vide -> carte "profile to complete", c'est voulu, plus aucune description en dur).
- Pas de recoll des Code Agents (logique agents inchangee).
- (Si voulu) coller la **charte Orange** dans `CLAUDE.md`.
