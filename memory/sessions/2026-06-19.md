# Session 2026-06-19 - Refonte UI "maquette Orange" (multi-agents) + charte durable

## Objectif
L'user (employé Orange) n'aimait pas le style trop "IA générative" et pas aux couleurs de l'entreprise. Il a
fourni une **maquette HTML** statique (`style-reference/OWIsMind - Account & Admin (Orange).html`, 1087 lignes)
couvrant My account, Administration (+ modale profil agent), Available agents (+ fiche détail), modale Response
mode, et le shell (rail + top bar). Demande : **refondre le style des vraies pages OWIsMind pour matcher la
maquette** (pas au pixel près), en lançant des agents en parallèle (un par page). Puis : **mémoriser la charte
Orange comme style obligatoire permanent** (la maquette sera supprimée par l'user).

## Fait
- **Reconnaissance** : la maquette + l'architecture front (pages déjà construites session précédente). Découpage
  par **ownership de fichier** (pas seulement par page) pour zéro conflit entre agents parallèles. Tokens existants
  (`styles/tokens.css`) déjà "charté Orange sobre" -> restyle = surtout du CSS scoped + petits ajouts de markup.
- **6 agents Sonnet en parallèle** (périmètres disjoints, même charte stricte) :
  - `foundation` : `tokens.css` (ajout **`--fw-heavy: 800`** uniquement) + `PageShell` (eyebrow + H1 36/800 +
    **title-bar orange 52x4** + desc) + `SettingCard`/`EmptyState` carrés + `Button` (carré, ghost bordure 2px
    inverse au hover, primaire orange) + `Modal` (scrim plat **sans blur**, carte carrée, ombre minimale). APIs préservées.
  - `shell` : `Sidebar` (rail logo/+/Library/aide/avatar, item actif = barre orange inset) + `MainTop` (top bar charté).
  - `settings` : `SettingsView` (My account : segmenté thème + select langue, Profil, Budget, Context, Usage 4-up).
  - `admin` : `AdminView` (eyebrow + badge Admin, onglets soulignés orange, KPI règle haute orange, Storage,
    Agents exposés, Users, Quotas checkbox carrée, modale Profil d'agent picker d'icônes + aperçu live). `Tabs.vue` non touché.
  - `agents` : `AgentsView` (recherche, grille de cartes carrées, fiche détail avec coches orange + chips outils).
  - `modepicker` : `ModelModePicker` (liste/détail barre orange, dots coût/vitesse, encart enveloppe 50 $).
- **Intégration** : périmètre vérifié (seuls les 12 fichiers assignés + rien d'autre), **0 tiret long**, **0 clé
  i18n manquante** (toutes réutilisées, familles dynamiques `mode.*`/`ag.badge.*`/`admin.agents.badge.*` peuplées),
  **0 `color-mix`**, **0 blur** (juste un commentaire confirmant le ban), **0 hex en dur** dans les vues. Compile-check Vite OK.
- **Correctif logo (retour user)** : l'agent `shell` avait suivi la maquette à la lettre = **logo reconstruit en
  carré CSS** + import de la vraie image retiré. **Rebranché la VRAIE image** `frontend/src/assets/orange-logo.png`
  (`import logoUrl` + `<img :src="logoUrl">`) au rail ET à la sidebar dépliée ; CSS du carré bidon nettoyé.
- **Build + package** : `/build-plugin` (entrée **`index-BHeG2NRY.js`**, css `index-BKSA-ZL6.css`, png bundlé
  `orange-logo-C6rK4N-7.png`), `body.html` recâblé (via `Write`, le `cp` est refusé - F10), `/package-plugin`
  -> zip propre **79 entrées**, logo png présent.
- **Charte durable (la maquette va disparaître)** : créé **`docs/cadrage/CHARTE_ORANGE_UI.md`** (auto-suffisant) ;
  ajouté **règle non négociable #10** + ref dans `CLAUDE.md` racine ; section "Style = Charte Orange" dans
  `frontend/CLAUDE.md` ; mémoire persistante `~/.claude/.../memory/orange-charter-styling.md` (+ `MEMORY.md`).

## Décisions
- **Découpage par ownership de fichier** : `foundation` possède le chrome partagé + primitives ; les vues n'éditent
  que leur fichier. Interdiction de toucher `tokens.css`/`base.css`/i18n/`Tabs.vue` (ce dernier partagé avec
  l'Evidence panel validé du chat).
- **Géométrie carrée assumée** sur les pages restylées (la maquette est carrée + la charte officielle dit
  "carré-net") ; `Button`/`Modal` partagés deviennent carrés/plats -> le chat hérite aussi (cohérence, on-charter ;
  corrige au passage 3 entorses de la modale : radius 16px + blur + grosse ombre).
- **i18n centralisé** : agents interdits d'éditer `extra.js` (fichier partagé) ; au final aucune nouvelle clé.
- **Logo = vraie image, jamais généré** (retour user explicite) -> nouvelle leçon L092 + interdit dans la charte.
- **Mockup non commité** (untracked, l'user va le supprimer) : exclu du commit de session.

## Validé / non validé
- **Validé local** : build Vite OK, périmètre/charte/i18n vérifiés statiquement, zip propre, png bundlé.
- **NON validé DSS** : rendu réel sur l'instance (avec données) = à confirmer par l'user après upload.

## Prochaines étapes
- **Upload du zip** (79 entrées, `index-BHeG2NRY.js`). **Pas de redémarrage backend** ni de recoll d'agent
  (frontend uniquement, python-lib inchangé). Smoke-tests : vrai logo au rail + sidebar, pages carrées chartées,
  thème clair/sombre, modales à plat.
- Reste en file d'attente (sessions précédentes) : valider Run 5/6 agents, brancher le resolver, etc. (voir CONTEXT).

---

## Run 2 (nuit) - Documentation ultra-complète (correction + complétion) + plateforme web HTML/CSS "parcours"

> Session **autonome** (user endormi, main libre, "des sous-agents Sonnet suffisent"). Géré par moi (Opus) via 2 workflows dynamiques Sonnet + QA navigateur.

### Objectif
(1) Corriger + compléter toute la doc `project-documentation/` (miroir fidèle du système, propre, ultra-complète). (2) Construire une **plateforme web HTML/CSS** de lecture en **parcours progressif** (du simple : UI/capacités -> au technique : maintenance), style charte Orange + maquette `style-reference/OWIsMind - Account & Admin (Orange).html`.

### Fait
- **Phase 1 - Doc (workflow 23 agents Sonnet, ~2.7M tok)** : découpage par **ownership de fichier** (11 clusters), pipeline correction -> vérif adversariale -> intégration/audit. Corrigé les affirmations périmées (budget blocage = **codé** `/chat/start` 402 fail-open ; confusion EN/FR de `CONVENTIONS.md` §1.3 = la doc est **anglaise**) ; complété les 3 features livrées après la passe doc du 2026-06-18 : fiches d'agent admin (`validate_agent_meta`, `/agents`/`/admin/agents`), budget/quota (`storage/budget.py`, table `webapp_user_quota_v1`, `/usage`, `/admin/budget(/users)`), charte/UI Orange + rail. **+5 nouveaux fichiers** : `01-user-guide/05-account-and-budget.md`, `06-agents-and-administration.md`, ADR `0013-agent-profiles-admin-authored`, `0014-monthly-budget-and-per-user-quotas`, `0015-orange-charter-ui`. README portail + index ADR à jour. **61 .md**, en-têtes/`See also` conformes, **21 routes couvertes**, build->fichiers->zip documenté en détail (+ matrice rebuild/restart/re-paste).
- **Phase 2 - Plateforme web (workflow 10 agents Sonnet, ~1.3M tok)** : `project-documentation/site/` autonome **HTML/CSS**, offline (double-clic, pas de fetch/CDN/install). Architecte = `assets/site.css` + `assets/site.js` (chrome rail+topbar+TOC scrollspy+pager auto depuis `PAGES`, thème clair/sombre persistant clé `owi-theme`, recherche client) + `_TEMPLATE.html` + `_STYLEGUIDE.md`. 8 pages = **parcours descendant** : `index`(0) -> `01-experience`(1) -> `02-architecture`(2) -> `03-frontend` -> `04-backend` -> `05-agents` -> `06-operations`(6) -> `07-decisions`(7). ~30 **diagrammes SVG faits main** (4 couches, séquence tour de chat 15 étapes, boucles orchestrateur/sous-agent, pipeline build->zip, modèle de données, arbre dépôt...). Index de recherche 80 entrées. **Vrai logo** `assets/orange-logo.png` copié (jamais reconstruit CSS, règle #10).
- **QA navigateur réel (Playwright + `python3 -m http.server` local stdlib)** : chrome OK, **vrai logo chargé** (naturalWidth 1772), thème clair **et** sombre OK + **persistance confirmée** (clé `owi-theme`), TOC scrollspy, recherche, diagrammes. **Diagrammes denses aérés** (1 agent Sonnet ciblé) : séquence 15 étapes (viewBox 640->780, "Polling loop" passé en caption au-dessus de la boîte, annotation step-8 `text-anchor=end` dans le cadre) + arbre dépôt (540->620). Re-screenshots = lisibles.
- **Bonus règle #9 (code)** : corrigé **12 em-dash** dans `Plugin/owismind/resource/compute_available_connections.py` (labels/log/commentaires -> `-`, byte-safe Python, `py_compile` OK).

### Décisions
- **Doc = anglais** (L088 confirmé) ; **correction + complétion**, pas de réécriture (la doc 2026-06-18 était déjà solide).
- **Plateforme = parcours multi-pages** (1 page/niveau), chrome **JS-rendu** depuis `PAGES` pour cohérence ; contenu riche + **SVG faits main** (offline `file://` sans fetch -> pas de markdown-render runtime). Les `.md` restent la **source canonique** (liens "source reference" dans chaque page).
- **Sous-agents Sonnet** partout (demande user), moi = Opus orchestrateur (2 workflows + 1 agent de polish + QA).
- `compute_available_connections.py` corrigé bien que session "doc" : règle #9 non négociable PARTOUT, fix isolé+sûr, signalé.

### Validé / non validé
- **Validé** : doc Python-propre (0 tiret hors `0012` = exception assumée), 21 routes, en-têtes, liens internes ; plateforme rendue OK en **vrai navigateur** (2 thèmes, logo, ~30 diagrammes, recherche, persistance) ; `py_compile` du fix code.
- **⚠️ Découverte (L093)** : `grep -P` **échoue silencieusement** sur le BSD grep de ce Mac -> les audits em-dash par `grep -rlP '\xe2\x80\x9[34]'` donnaient des **faux négatifs**. **Scan Python = autorité.** Un sweep Python du dépôt révèle des em-dash **préexistants** (NON introduits par moi) hors livrable : `frontend/test/*.test.js` (13), `docs/cadrage/CHARTE_ORANGE_UI.md`, `docs/scaling/PLAN_WEBAPP.md`, `memory/*` (souvent citations de la règle), `.claude/skills/*` (exclus par ADR-0012), `docs/agentic-research/*` (corpus gitignoré), `style-reference/*` (user va supprimer) -> **à l'user de décider** (édition de code/tests hors périmètre de cette session).
- Aucune incidence DSS (doc + plateforme + 1 fichier de setup webapp = display strings). Déploiements DSS en attente (sessions précédentes) inchangés.

### Prochaines étapes
- Ouvrir `project-documentation/site/index.html` (double-clic) pour parcourir la doc en parcours.
- (User) décider du sort des em-dash **préexistants** hors livrable (surtout `frontend/test/*.test.js`) ; remplacer `grep -P` par Python (ou `ggrep`/GNU grep) dans le protocole de vérif règle #9.
- `project-documentation/.workdir/` = scratch supprimable ; `style-reference/` à supprimer (prévu user).

## Run 3 - Auth gate + impersonation admin + plugin DEV/PROD (✅ validé DSS)

**Objectif** : (1) écran dédié quand l'utilisateur n'est pas authentifié à DSS ; (2) permettre à
l'admin de consulter les conversations d'un autre user ; (3) séparer DEV/PROD du plugin sans
toucher la prod.

**Fait** (via sous-agents en parallèle, ownership par fichier + 3 revues adversariales sécu) :
- **Auth gate** : `frontend/src/components/shell/AuthGate.vue` (nouveau, charte Orange, vrai logo,
  EN/FR + thème + F5), `stores/session.js` (`authState` pending|ok|unauthenticated, set sur 401
  `unauthenticated`/`http_401` ; autres erreurs gardent le shell), `App.vue` (rend SEULEMENT
  AuthGate si unauthenticated -> aucune navigation ; splash si pending). Clés i18n `authgate.*`.
- **Impersonation admin (TEMPORAIRE)** : backend `python-lib/owismind/security/impersonation.py`
  (`effective_identity`, header `X-OWI-Impersonate`, honoré **seulement si caller réel = admin**) ;
  `api/routes.py` câblé + FENCÉ (READ swap vers user cible : /me,/usage,/conversations,/conversation,
  _evidence_guard ; WRITE 403 `impersonation_read_only` : chat start/feedback/stop ; /me ajoute
  `impersonating`+`real_user_id` et **ne record PAS** ; /chat/poll,/agents,/admin = identité RÉELLE).
  Front `frontend/src/features/admin-impersonate/` (impersonation.js sessionStorage + header injecté
  dans `services/backend.js`, UserPicker, ImpersonateBanner + Quitter), bouton AdminView -> picker,
  `session.impersonating`, `chat.canSend && !impersonating`, note read-only ChatView, banner
  AppLayout. Clés `impersonate.*`. Tests `tests/test_impersonation.py` (17).
- **Ancienne feature SUPPRIMÉE entièrement** : `admin_inspect.py` + `tests/test_admin_inspect.py` +
  `features/admin-inspect/` + route `/admin/inspect` + clés `inspect.*` (avait existé en v1 revue
  read-only, puis v2 sélecteur de source cross-env + scoping découverte - tout jeté).
- **DEV/PROD = source unique + cible de build** : `frontend/vite.config.js` (base via
  `OWI_PLUGIN_ID`, défaut prod inchangé), `tools/build_dev_plugin.py` (transform déterministe
  stdlib : id+package python+labels **plugin ET webapp** renommés, base DEV ; invariantes
  asservies), skill `.claude/skills/package-plugin-dev/`, note dans `build-plugin`. Produit
  `Plugin/ready-for-dataiku/owismind_dev-upload.zip` (id `owismind_dev`, 68 entrées).
- **Vérifs** : 455 tests backend + 124 front + compile Vite OK + 0 tiret (livrable) ; prod jamais
  touchée (artefacts + source webapp inchangés).

**Décisions** :
- Tables : on ne touche à RIEN (`CREATE TABLE IF NOT EXISTS` déjà partout via `ensure_*`) ;
  isolation DEV = `table_prefix` optionnel au déploiement (sans prefix = mêmes données que prod ->
  l'impersonation voit les vrais users ; `dev` = bac à sable). Renommage des tables prod = REFUSÉ
  (orphelinerait les convs live).
- Impersonation = **lecture seule** (envoi bloqué UI + serveur), conforme "jamais d'écriture sous
  le nom du user".
- DEV/PROD = source unique + cible de build (pas de dossier dupliqué). Le `webapp.json` doit AUSSI
  être marqué `(DEV)` (retour user : sinon 2 webapps "OWIsMind - AI Agents" identiques dans la liste DSS).

**Validé / non validé** : ✅ **validé DSS** (user « super tout fonctionne à merveille ») - auth gate
+ impersonation + zip DEV déployé. (Auth gate déjà confirmé « marche à merveille » plus tôt.)
**✅ PROMU EN PROD + DÉPLOYÉ** (user « super tout marche ») : `/build-plugin` + `/package-plugin` ->
`owismind-upload.zip` (id `owismind`, entry `index-CApWkAm7.js`, 80 entrées) uploadé + backend
redémarré. La même base validée en DEV est désormais live en prod. Commits `f5b2976` + `f967347`.

**Prochaines étapes** :
- Promotion prod (quand DEV ok) = `/build-plugin` + `/package-plugin` (prod) + upload. Workflow
  DEV->PROD durable désormais (webapp ET agents).
- L'impersonation est **temporaire** : suppression future = retirer `security/impersonation.py` +
  `features/admin-impersonate/` + les blocs FENCÉS (routes.py, backend.js, session.js, chat.js,
  ChatView.vue, AppLayout.vue, AdminView.vue) + clés `impersonate.*`.

## Run 4 - 2e SOUS-AGENT "tickets d'incidents" (TroubleTickets_year) + factory repo (✅ testé DSS « marche plutôt bien »)

**Objectif** : ajouter un 2e sous-agent expert sur le dataset `TroubleTickets_year` (83 738 lignes,
21 colonnes), super conscient de sa donnée et avec recherche de valeurs ; trancher modèle sémantique
séparé vs partagé ; définir les datasets/recipes Flow ; enrichir l'orchestrateur ; et **organiser
`dataiku-agents/` pour scaler à N sous-agents**.

**Analyse (2 workflows)** : compréhension exhaustive du moteur revenus (5 agents, cartes ligne-à-ligne)
puis revue adversariale du change-set (4 dimensions + vérif) = **0 finding réel confirmé**. Constat
clé : le sous-agent revenus est **~95% agnostique du dataset** (l'expertise vit dans profile +
value_index + modèle sémantique, pas dans le code).

**Fait (repo, tout testé local : 283 tests verts, 0 tiret, compile OK)** :
- **`agents/TroubleTickets_expert.py`** : copie du moteur revenus, **corps byte-identique** (contrats
  gelés KNOWN_*/AGENT_RESULT/span/caps préservés) ; seuls changent l'en-tête CONFIG (PROFILE/
  VALUE_INDEX datasets, `SEMANTIC_TOOL_ID=TODO`, `SEMANTIC_TOOL_NAME=tickets_semantic_query`) et les
  textes hiérarchie-d'offre revenus **neutralisés** (devenus génériques) dans `build_semantic_question`
  + `DISCLOSE_NOTE`. `FALLBACK_TO_DIRECT=True` -> utile dès J1 même sans modèle sémantique.
- **`agents/OWIsMind_orchestrator.py`** : entrée `CAPABILITIES["tickets_expert"]` (domaine `tickets`
  déjà dans BUSINESS_DOMAINS) + champ **`lookup_search_columns`** (allowlist) câblé via
  `lookup_domains()` -> `_run_lookup()`.
- **`tools/attribute_lookup_tool.py`** : support `searchable_columns` (recherche restreinte par
  domaine, fallback sûr si vide/non-matché, cache-key inclut l'allowlist) + domaine générique
  **`value`** dans `ENTITY_DOMAINS` et `alias_suggestions` (le catalogue générique alimente le "did
  you mean"). Revenus = prouvé inerte (aucune ligne `value`).
- **Modèle sémantique tickets DÉDIÉ** : `tools/semantic_model/update_tickets_semantic_model.py`
  (instructions tickets + 8 golden queries, table dérivée via `get_location_info`, items `[CONFIRM]`)
  + `dump_tickets_semantic_model.py`.
- **`recipes/` rendues génériques + NA-safe** : les 3 recipes auto-détectent INPUT/OUTPUT (le
  `value_catalog` était hardcodé -> auto-IO comme les 2 autres) ; **fix NA** (`infer_with_pandas=False`
  -> fallback `True`) ; `build_value_catalog_recipe` **dataset-adaptatif** (revenus = catalogue curé
  inchangé ; autre dataset = catalogue générique `value`).
- **Factory repo** : `registry.json` (source de vérité unique par domaine, dev-owned), `DATASETS.md`
  (inventaire colonnes), `PLAYBOOK_ADD_AGENT.md` (runbook). Test anti-dérive **généralisé à tous les
  agents** ; tests tickets + allowlist + catalogue générique ajoutés.
- Docs d'orientation à jour (CLAUDE.md, README.md, recipes/README.md, semantic_model/README.md).

**Décisions** :
- **Modèle sémantique SÉPARÉ par domaine** (pas une source ajoutée au modèle revenus) : règle "une
  table jamais de JOIN", instructions/golden focalisées, binding tool 1:1. (Question explicite user.)
- **Catalogue générique** plutôt que fork revenus : recipe dataset-adaptatif, schéma 12-col partagé
  (les 3 colonnes `canonical_*` restent vides pour un dataset non-revenus = normal, le tool ne les
  lit pas).
- `tickets_expert` shippé `enabled:True` + `agent_id` placeholder : remplir l'id AVANT de re-coller
  l'orchestrateur (sinon erreur technique gracieuse) ; sinon `enabled:False` en attendant.

**Validé / non validé** : ✅ **testé DSS, « marche plutôt bien »** (user) - recipes (profil +
value_index + value_catalogue) construites après le fix NA, catalogue tickets en mode générique OK.
⏳ À pofiner plus tard (curation : unité `Duration_ticket_total`, valeurs exactes `CurrentStatus`,
golden queries). Modèle sémantique tickets + recoll des 2 Code Agents = à finaliser quand voulu.

**Prochaines étapes** : finir le déploiement tickets selon `PLAYBOOK_ADD_AGENT.md` (override métrique
COUNT, modèle sémantique DSS + `update_tickets_*`, tool `tickets_semantic_query`, Code Agent +
`agent_id` réel, re-coll orchestrateur) ; puis fiche client 360 (fan-out revenus+tickets, pont
`Account_name`/`Customer_id`). Voir **L097** (NA-safe recipes) + **L098** (factory / 2e agent).
