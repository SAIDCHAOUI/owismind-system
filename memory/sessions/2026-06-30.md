# Session 2026-06-30 - Benchmark v2 : append mode + colonnes SQL/tool de reference

> Session autonome de nuit (l'user dormait, a repondu aux questions de cadrage avant). But : deux
> features sur le benchmark, livrees pretes + propres. Repo only, DEV repackage, NON valide DSS.

## Objectif
1. Ajouter au dataset golden 2 colonnes : un SQL de reference + un tool de reference, pour servir
   au benchmark MAINTENANT et comme donnees d'entrainement plus tard.
2. **APPEND MODE** : un benchmark NOMME unique par agent, qui s'enrichit dans le temps ; relancer
   ne joue que les questions pas encore faites et accumule dans le meme benchmark (score global =
   derniere tentative par question) ; + refaire tout / nouveau benchmark / refaire une question.

## Decisions de cadrage (AskUserQuestion, l'user a tranche avant de dormir)
- Colonnes `expected_sql`/`expected_tool` = **signal doux au juge** (stockees + affichees a cote du
  SQL/tool reellement generes ; passees au juge comme indice non contraignant). Pas de note dediee.
- Modele benchmark confirme : append accumule dans le MEME benchmark (10 q « said » + 5 -> 15 q,
  score sur les 15 derniere tentative). But = monter petit a petit jusqu'au seuil statistique.
- Nouvelle question ajoutee -> pending dans le **benchmark courant** uniquement.
- Perimetre : **LAB (launcher + results) ET consultation plugin**.

## Fait
**Spec figee** : `docs/superpowers/specs/2026-06-29-benchmark-v2-append-mode-design.md`.

**Decision d'archi cle (legerete DSS)** : le registre des benchmarks + l'appartenance des questions
+ les drapeaux « refaire » vivent dans la **variable projet `benchmark`** (`benchmarks` map +
`run_request`) -> **AUCUN nouveau dataset DSS**. Seules les tables de resultats gagnent des colonnes.

**Phase 1 - coeur pur (LAB lib), TDD :**
- NOUVEAU `benchmark/registry.py` (pur) : parse registre + run_request, CRUD (create/add/remove/
  redo/rename/archive, retourne un nouveau dict), resolution `resolve_to_run` (append = pending +
  redo ; full = tous les membres ; gate golden-actif ; ordre par added_at), `done_question_ids`,
  `attempt_numbers`, `next_attempt_no`, `benchmark_id_of_run`, `validate_benchmark_name`.
- `schemas.py` : golden +`expected_sql`/`expected_tool` ; raw/scored +`benchmark_id`/`benchmark_name`/
  `attempt_no`/`expected_sql`/`expected_tool`/`actual_tools` ; SUMMARY -> niveau benchmark
  (`benchmark_id`/`benchmark_name`/`last_run_id`/`last_run_timestamp`/`n_runs`) ; BREAKDOWN idem.
- `run_params.py` : resout `benchmarks` + `run_request` (via registry).
- `scoring.py` : NOUVEAU `latest_attempts` (derniere tentative par benchmark_id/question_id/agent/
  mode) ; `summarize`/`breakdown` regroupes par `(benchmark_id, agent, mode)` sur les dernieres
  tentatives ; meta last_run_*/n_runs.
- `judge.py` : `build_judge_prompt`/`run_llm_judge` +`expected_sql`/`expected_tool` (bloc indice NON
  contraignant) ; `_JUDGE_SYSTEM` precise « hints, jamais une exigence ».
- `agent_runner.py` : `_denormalized_golden` porte expected_sql/tool ; `_actual_tools` derive des
  artefacts (kinds chart/table/kpi).

**Phase 2 - steps DSS + backend webapp :**
- `dss_steps/history_io.py` : `write_history_dataset(key=...)` (merge par benchmark_id pour summary/
  breakdown) + NOUVEAU `read_history_rows` (lecture defensive fail-open pour le resolveur).
- `step_run_matrix.py` : benchmark-aware (lit run_request -> entity -> agent epingle + modes ;
  `_benchmark_agent` derive le bool modes ; lit le raw anterieur pour done+attempt ; resolve_to_run ;
  garde « rien a jouer » ; stampe benchmark_id/name/attempt_no par ligne).
- `step_judge.py` : passe expected_sql/tool au juge.
- `step_aggregate.py` : scope au benchmark du dernier run, filtre, summarize/breakdown, merge par
  benchmark_id.
- `benchmark_webapp/views.py` : view-models v2 (benchmarks_view, benchmark_detail_view + evolution,
  evolution_for_question, build_launch_request, benchmark_options, latest_benchmark_id) ;
  summary/breakdown/detail repasses en **benchmark_id** ; NOUVEAU `review_view` (toutes les
  tentatives, pour l'override par tentative) ; golden_view/detail_view portent expected_sql/tool.
- `benchmark_webapp/dss.py` : registre dans la variable (read_registry/_persist_registry sous
  `_REGISTRY_LOCK`) + CRUD (create/add/remove/redo/rename/archive) + `launch_benchmark` (ecrit
  run_request + consomme les drapeaux redo, puis tire ; **corrige apres revue** : persistance APRES
  les gardes single-flight, sinon un lancement rejete perdait l'intention redo).
- `webapps/benchmark_launcher/backend.py` : routes `api/benchmarks` + `api/benchmark/{detail,create,
  add-questions,remove-question,redo,rename,archive,launch}` ; `api/review` passe en benchmark.
- `webapps/benchmark_results/backend.py` : `api/results/benchmarks` (remplace runs) + summary/
  breakdown/detail en benchmark_id.

**Plugin (consultation) :**
- `benchmark_view/schemas.py` : LIGHT_COLUMNS + colonnes v2 OPTIONNELLES (`OPTIONAL_V2_COLUMNS`) ;
  REQUIRED_COLUMNS inchange (vieilles tables valident encore).
- `benchmark_view/lab_io.py` : `read_scored` lit **l'intersection** des colonnes desirees et des
  colonnes reelles (via table_columns) -> retro-compatible, pas de 500 sur table non-migree.
- `benchmark_view/aggregate.py` : benchmark-aware (latest_attempts + benchmarks_list + _evolution_map ;
  `results_view(scored, benchmark_id=...)` -> selecteur benchmark + evolution + attendu vs reel).
- `api/routes.py` : `/benchmark/results` param `benchmark_id`.

**Frontends (workflow 3 agents //) :**
- Launcher : onglet **Benchmarks** en PREMIER (liste + creation modale + detail membership avec
  badges pending/done/redo + evolution + toggle redo + add/remove + 3 boutons de lancement),
  Golden +expected_sql/tool, Review en selecteur benchmark, lancement global retire. MOCK + i18n
  EN+FR + style charte.
- Results : selecteur RUN -> BENCHMARK, hero avec nom, colonne evolution + historique tentatives +
  bloc « reference vs produit ».
- Plugin Vue : service (param benchmark_id), composable normalizeResults (benchmark_id/benchmarks +
  passthrough evolution), store (selectedBenchmarkId/selectBenchmark), vue (selecteur benchmark +
  evolution + attendu/reel ; override avec le **run_id de la LIGNE**), i18n extra.js, node test.

## Decisions
- Registre/membership/redo dans la VARIABLE (pas de dataset) = zero creation DSS, le launcher est
  l'unique writer (serialise par lock).
- Override = par tentative (cle run_id/question_id/agent_key/mode), donc la consultation/launcher
  envoient le run_id de la ligne, pas un run_id global de benchmark.
- `review_view` liste TOUTES les tentatives ; `detail_view` (consultation) = derniere tentative +
  evolution.
- Lecture plugin par intersection de colonnes = retro-compatibilite des tables non-migrees.

## Valide / non valide
- ✅ Tests : **LAB 329 + plugin 509 + node 134 verts**. JS `node --check` OK. **Build Vite propre.**
  **0 tiret** (scan Python byte-safe). i18n EN+FR complet (audit). Contrats front↔back spot-checkes.
  Perimetre git propre (aucun `resource/`/`ready-for-dataiku/` prod touche). DEV packagee.
- ✅ Revue adversariale (workflow 3 dim + verif sceptique de chaque finding) = **0 critical/high,
  2 medium = 1 bug** (redo consomme avant le verrou) **corrige + re-teste**.
- ⛔ **NON valide DSS** (intestable hors instance) : le run reel, la materialisation des colonnes,
  le rendu des 3 UIs.

## Prochaines etapes (DSS, l'user)
Guide : `OWIsMind_LAB/README.md` section v2 + `DEPLOY_GUIDE.md` bandeau v2.
1. **LAB** : re-coller la lib `benchmark/` (NEUF `registry.py` + changes) + `benchmark_webapp/`
   (`views.py`+`dss.py`) + les panes des 2 webapps ; variable : ajouter `"benchmarks": {}` +
   `"run_request": null`.
2. **Plugin** : uploader `owismind_dev-upload.zip` (**78 entrees, `index-DZ7yGIZO.js`**, Uploaded) +
   **redemarrer le backend** (python-lib change).
3. Creer un benchmark dans l'onglet Benchmarks, lancer (append), verifier l'accumulation + le score
   derniere-tentative + l'evolution sur un re-run ; cote consultation : selecteur benchmark + evolution
   + attendu vs reel. Un run frais materialise les colonnes neuves.
4. Promotion PROD du fix + des features quand DEV valide (rebuild + package prod).
Voir **L113**.
