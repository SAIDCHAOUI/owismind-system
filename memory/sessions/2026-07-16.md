# Session 2026-07-16

## Run 1 - mise en ordre repo + restructuration miroir DSS + double audit de securite factory

**Contexte** : l'user etait perdu dans l'arborescence (hub ? factory-docs ?) et voulait
(1) de l'ordre partout, (2) une arborescence qui reflete l'UI Dataiku, (3) des README simples
en francais, (4) un audit de securite (GPT-5.6 Sol autorise) avant de toucher l'instance.

**Git remis a plat (matin)** : branche de nuit `OWIsMind_PRD_V1_3-dev-night-20260712` mergee
fast-forward dans `OWIsMind_PRD_V1_3-dev` puis supprimee (local + remote) ; `refactor/deep-clean-v1.2`
supprimee (entierement mergee) ; TOUT pousse sur origin (premiere fois pour OWIsMind_PRD_V1_2 et
la v1.3-dev a jour). Restent 3 branches : OWIsMind_PRD_V1_2 (prod), OWIsMind_PRD_V1_3-dev (travail),
main (depreciee, default GitHub). Confirme par l'user : le clone DSS v1.3-dev EXISTE deja (phase A
du deploy = faite, sauf alignement).

**Restructuration miroir UI Dataiku** (commit 439308a) : `agents/ tools/ semantic-models/` ->
`genai/{agents,agent-tools,semantic-models}/` ; `hub/` -> `project-library/owismind_hub/` (la ou il
vit vraiment dans DSS : racine de la library, a cote de python/) ; `factory-docs/` + playbook ->
`docs/` (README factory renomme AGENT_FACTORY.md). Toutes les references migrees (tests avec
os.path.join relatifs, registry.json, regenerate_seeds.py profondeur _MIRROR_DIR, notebooks,
CLAUDE.md x2, memoire active, rules, LAB, docs racine). Piege zsh : `for f in $FILES` ne word-split
pas ; passer par `git grep -l | while read`.

**README tous reecrits en francais simple** (commit 1261abc) : workflow 11 agents Fable paralleles,
1 fichier chacun, format commun "Dans Dataiku / Dans ce dossier / Deploiement", zero tiret interdit,
liens croises normalises apres coup (les agents paralleles ne voyaient pas les README des voisins).

**Verification adversariale** (commit 4abe02d) : workflow 4 angles (refs mortes, docs vs disque,
executions reelles, verite des README). Rien de casse a l'execution ; corrige : playbook jamais
migre (arbo pre-genai + recettes fantomes `recipes/*.py` remplacees par les vrais fichiers flow/),
docs/architecture.md (arbre), PROJECT_STATE 7b, rules/agents.md L12, renvois "section 9",
emplacement python-lib (OWIsMind_PRD_V1_3_DEV/plugin/owismind/python-lib/), ResourceWarnings tests.

**Double audit de securite (READ-ONLY, avant tout premier deploiement)** :
- Interne (Claude) : DEPLOYABLE SOUS CONDITIONS, 3 gates + 4 P2 + 7 P3.
- GPT-5.6 Sol (codex) : NOT SAFE TO DEPLOY AS-IS, 6 P1. Episode fantome -> L156.
- Convergence forte (console authz, apply_config ecrasant, read-only = connexion DB, doctor PII).
  Ecarte avec justification : "profil -> LLM" (fonctionnement valide du systeme, meme frontiere
  de confiance que les agents live).

**Durcissement en 2 commits** :
- c141518 : scenario refresh force `active=False` (dans le save du trigger), denylist read-only
  +8 mots-cles (INTO attrape SELECT INTO), hub.write_prompt avec backup (console POST prompt).
- 7d5d2cc (5 lots paralleles Fable, fichiers disjoints, diffs re-verifies, 3 tests existants
  realignes) : marqueur owismindFactory + garde apply_config (refus sans marqueur ; backup de
  version pre-apply + re-pin avant mutation ; echec backup = refus) ; align allowlist
  expected_source_keys (vide = decouverte) + notebook 02 en 3 passes ; sonde fail-closed +
  redaction recursive des cles sensibles + backup des rapports ; console max 4 jobs / 1 execute /
  429 / ids uuid4 / caps de taille ; get_settings overlay strict ; doctor evidence retenue dans le
  persiste. Docs : SECURITY_AUDIT_2026-07-16.md + phase 0 "security gates" dans DEPLOY_V1_3_DEV.md.

**Tests** : 468 -> 516 verts (2 skips pandas), plugin 830 OK, LAB 343 OK, regenerate_seeds --check
OK a chaque etape. 5 commits au total sur OWIsMind_PRD_V1_3-dev (rien pousse depuis le merge du matin).

**Reste pour le deploiement (l'user)** : phase 0 gates (console admins-only, SQL_owi SELECT-only +
timeout, acces logs), puis DEPLOY_V1_3_DEV.md phases A2 (notebook 02, 3 passes) et B-D.
