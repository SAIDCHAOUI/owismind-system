# Session 2026-06-26 - Intégration du benchmark dans le système OWIsMind (2 pôles)

> Session autonome de nuit (user : « je vais dodo et donc tu vas tout implémenter »). Après cadrage
> (brainstorming + 2 questions), le user a délégué l'implémentation complète. Repo only, rien sur l'instance DSS.

> ## REVISION 2026-06-26b (retour user en cours de nuit, « tu prends les décisions ») - voir la section dédiée en bas de ce fichier
> Le webapp LAB unique est SPLITé en **2 webapps** (results publique lecture seule / launcher config+lancement),
> launcher = **vrai formulaire** (plus de JSON), results = **grand public langage clair**, les deux **bilingues
> EN défaut + FR**. **SQL lecture+append ultra-prudent** (audit sécurité dédié). + critique/amélioration du
> système benchmark + stockage suggestions. Détail complet : section « REVISION 2026-06-26b » plus bas.

## Objectif
Aller plus loin que le benchmark standalone (datasets + librairie + scénario dans `OWIsMind_LAB`) et
l'intégrer comme **brique du système**, avec **deux pôles de contrôle** :
1. **Pôle utilisateur (grand public)** : suggérer des questions de benchmark avec la bonne réponse,
   depuis une réponse de chat OU une page dédiée -> construire le golden collaborativement.
2. **Pôle admin (ingénieur)** : un seul endroit pour configurer, lancer et **lire** un benchmark, zéro
   hardcode (tout est réglable, jamais de code à toucher).

## Décision d'archi clé (valide les hésitations du user)
**Les deux pôles ne vivent pas au même endroit -> ça tranche « Dash ? plugin ? autre webapp ? ».**
- Pôle utilisateur = **DANS la webapp Vue (plugin)** : c'est du produit (chat, menu « ... », users).
- Pôle admin = **une webapp DSS STANDARD séparée dans `OWIsMind_LAB`** (HTML/CSS/JS + backend Python) :
  lit les 5 datasets en direct, lit/écrit la variable `benchmark`, lance le scénario. **Zéro Vite, zéro
  zip, zéro body.html, zéro restart** (édition dans le navigateur DSS). PAS Dash (dépend d'un code env,
  charte imparfaite), PAS dashboard natif (ne sait pas éditer la variable), PAS une 2e webapp dans le
  plugin (hérite du build lourd + lecture cross-projet). Le user a explicitement validé « Webapp DSS
  standard dans LAB » (AskUserQuestion). Spec : `docs/superpowers/specs/2026-06-25-benchmark-integration-design.md`.

Découpage en 3 lots indépendants. Le clic « Suggérer pour le benchmark » dans le chat **OUVRE la page
Benchmark préremplie** (comme demandé par le user), pas une modale -> une seule surface de suggestion.

## Fait

### Lot 2 - capture utilisateur (plugin Vue) - COMPLET, testé, build OK, DEV packagé
- **Backend** (`Plugin/owismind/python-lib/owismind/`) :
  - `storage/migrations.py` : nouvelle table owner-stamped **`webapp_golden_suggestions_v1`** (DDL =
    superset du golden lean-9 : question/agent_answer/answer_is_correct/reference_answer/missing/
    expected_value(+type)/category/language/generated_sql_json/status/source/exchange_id/...) + 2 index.
  - `storage/suggestions.py` (neuf) : `save_suggestion` (owner-stamped, paramétré, COMMIT, bornes,
    `_nullable_bool_literal` pour le verdict, statement_timeout) + `list_my_suggestions` (owner-scopé,
    read-only + timeout, LIMIT clampé).
  - `storage/chat_v5.py` : `read_exchange(user_id, exchange_id)` (owner-scopé, PK O(1)) = reconstruit
    la Q/R autoritative côté serveur (jamais le texte client).
  - `security/validation.py` : `validate_suggestion_manual` + `validate_suggestion_from_chat` (codes
    stables ; bool strict pour le verdict ; expected_value exige son type ; bornes).
  - `api/routes.py` : 3 routes (`POST /benchmark/suggest`, `POST /benchmark/suggest-from-chat`,
    `GET /benchmark/suggestions`). Les 2 WRITE **bloquées en impersonation** (read-only) ; la lecture
    swap sur l'identité effective. La route from-chat reconstruit Q/R+SQL+agent_key depuis l'échange
    (404 si non possédé).
  - `storage/sql_config.py` : `golden_suggestions` ajouté à `storage_status().tables` (la webapp LAB a
    besoin du nom physique exact) **+ `safe_index_name` (fix revue, voir L103)**.
  - **+28 tests backend** (`test_suggestion_validation.py`, `test_suggestions_storage.py`) -> **484 verts**.
- **Frontend** (`Plugin/owismind/frontend/src/`) :
  - `components/chat/MessageAgent.vue` : entrée de menu « ... » **« Suggérer pour le benchmark »**
    (gardée : seulement sur une réponse persistée AVEC texte) -> `setPrefill` + `router.push('/benchmark')`.
  - `views/BenchmarkSuggestView.vue` (neuf, **route `/benchmark` TOUS users, pas de garde admin**) :
    formulaire **bi-mode** (prérempli depuis le chat = question/réponse agent en lecture seule + dial
    Oui/Non + correction si Non ; ou manuel = question/réponse/valeur clé+type/catégorie) + table
    « Mes suggestions » avec statut. Charte respectée (carré, tokens, orange rare).
  - `stores/benchmark.js` (neuf) : prefill transient + mySuggestions + submit ; `services/backend.js`
    (3 fonctions) ; `router/index.js` (route) ; `Sidebar.vue` (lien de nav `bookOpen`) ; `i18n/extra.js`
    (~54 clés `bench.*` + `msg.suggest_benchmark`/`sb.benchmark`/`rail.benchmark`, fr+en).
  - **Build Vite OK, 124 tests node verts.** **DEV packagé** : `owismind_dev-upload.zip` (72 entrées,
    entry **`index-BoETXxLb.js`**, BenchmarkSuggestView bundlé). **Prod 100% intacte** (vérifié : zip
    prod / resource / body.html non modifiés).

### Lot 1 + 3 - webapp benchmark (`OWIsMind_LAB`, webapp standard) - repo artefacts, QA visuelle OK
- **`benchmark_webapp/`** (nouveau package ; repo = source de vérité, recollé en project-library LAB
  pour `views.py`, panes collés en webapp standard pour le reste) :
  - `views.py` (PUR, stdlib + `benchmark.run_params`) : `summary_view` (KPIs globaux + table agent x
    mode), `breakdown_view`, `detail_view` (trie-puis-tronque, fix revue), `runs_view`, `validate_config`,
    `config_view`, **+ Lot 3** `safe_table_name`, `suggestion_to_golden`, `promotable_golden_rows`,
    `suggestions_view`. **24 tests purs.**
  - `backend.py` (pane Python DSS) : `api/config` (GET resolved+raw+catégories+runs / POST écrit la
    variable `benchmark` en Local variables), `api/run` (lance `Run_Benchmark` **async + single-flight**,
    best-effort `run_scenario`/`run`) + `api/run/status`, `api/results/{runs,summary,breakdown,detail}`
    (lectures bornées, colonnes lourdes droppées), `api/suggestions` (lecture **cross-projet read-only**
    via `safe_table_name`, status='pending', LIMIT 500) + `api/suggestions/promote` (append idempotent
    au golden + log `benchmark_suggestions_promoted`). **Décorateur `_safe` (jamais de 500, fix revue).**
  - `body.html` / `style.css` / `script.js` / `preview.html` (sous-agent Sonnet, framework-free, mode
    MOCK pour QA offline) : 3 onglets **Resultats / Lancer / Suggestions**, charte Orange (carré, aplat,
    orange rare, barres SVG/CSS par mode, thèmes clair+sombre via `[data-theme]`, toggle persistant),
    tout le contenu user **échappé** (escapeHtml), 0 lib/CDN, 0 marque CSS.
  - `README.md` (montage DSS + permissions + bloc `benchmark.suggestions`) + `tests/`.
- `benchmark/run_params.py` : bloc **`suggestions` additif** (`_resolve_suggestions` + `suggestions_config`,
  vide si absent) ; `benchmark/SETUP_GUIDE.md` étape 4 -> pointe vers le webapp (dashboard natif en option).
- **QA visuelle Playwright** (serveur http local + preview MOCK) : 3 onglets + clair/sombre rendus
  correctement, **0 erreur console** (hors favicon 404), structure accessible (tablist/th/aria).

### Revue adversariale (Workflow, 4 dimensions) = 0 critical / 0 high
2 medium + lows, **tous corrigés + verrouillés par tests** :
- **medium** (plugin-backend) : nom d'index `webapp_golden_suggestions_v1_uc_idx` dépasse NAMEDATALEN
  (63 o) sous `table_prefix` / clé PROD -> `pg_identifier` LÈVE -> table jamais créée. **Fix `safe_index_name`
  (hash fallback), L103.** +2 tests.
- **medium** (lab-frontend) : après un lancement, le nouveau run invisible (recharge l'ancien run_id).
  **Fix** : on completion, `currentRunId=""` + `loadConfig()` (repeuple+sélectionne le plus récent) -> Resultats.
- **low** : `detail_view` tronquait AVANT de trier (lignes « à relire » perdues) -> trie-puis-tronque ;
  `api_promote` lisait le golden plafonné à 5000 sur un chemin read-modify-write -> lecture non plafonnée ;
  `loadResults` effaçait l'erreur + marquait le run chargé en cas d'échec -> flag `ok` ; submitChat
  envoyait une correction périmée sur un verdict Oui -> gate par verdict ; prefill non nettoyé en
  quittant -> `onBeforeUnmount(clearPrefill)` ; `import json` mort retiré de `suggestions.py`.

## Validé / non validé
- **Validé local** : 484 (plugin) + 173 (benchmark) + 24 (webapp) = **681 tests Python** + **124 node** ;
  build Vite OK ; **QA visuelle Playwright du webapp LAB OK** (3 onglets, 2 thèmes) ; **0 tiret sur 28
  fichiers** ; revue 0 crit/0 high (tout corrigé). DEV packagé, prod intacte.
- **NON validé DSS** (besoin de l'instance) : tout. Le plugin (Lot 2) doit être uploadé (DEV) + backend
  redémarré ; la webapp LAB (Lot 1+3) doit être créée en DSS (4 panes + project-library `views.py`) +
  permissions + bloc `suggestions`. La méthode async de lancement de scénario (`run_scenario`/`run`) est
  best-effort (dégrade proprement si l'API diffère).

## Prochaines étapes (DSS, prochaine session)
- **Lot 2** : upload `owismind_dev-upload.zip` (Uploaded) + **REDÉMARRER backend** (python-lib changé).
  Smoke : action « ... » -> page `/benchmark` préremplie ; page grand public ; « mes suggestions ». La
  table se crée à la 1re suggestion. Copier le nom physique exact (Admin > Storage > `golden_suggestions`).
- **Lot 1+3** : suivre `benchmark_webapp/README.md` (recoller `views.py` en project-library ; créer la
  webapp standard + coller les 4 panes ; dataset `benchmark_suggestions_promoted` ; bloc
  `benchmark.suggestions` ; permissions LAB write + connexion SQL). Vérifier le lancement de scénario sur
  l'instance (id de méthode dataikuapi). Puis : lire le dernier run, lancer un run, promouvoir des suggestions.
- **Reste benchmark 2026-06-25 toujours dû** : re-coller `judge.py`+`schemas.py` + corps de steps, relancer
  Judge+Aggregate, run complet. (Indépendant de cette session.)
- **Plus tard** : sync du statut « accepted/rejected » vers la table plugin (le user voit « pending » en
  v1) ; juge en panel ; runs planifiés ; promotion PROD de l'orchestrateur.

---

# REVISION 2026-06-26b - 2 webapps (results publique + launcher), bilingue, durcissement + audit sécurité

> Retour user après le 1er build, user reparti dormir, « tu prends les décisions ». Toujours repo only.

## Objectif (demandes user)
- **2 webapps DSS séparées** dans `OWIsMind_LAB` au lieu d'une seule à 3 onglets : `results/` (consultation
  publique, lecture seule) et `launcher/` (config + lancement + revue suggestions). Plus de gating admin :
  c'est physiquement séparé (le launcher n'est juste pas partagé aux consultants).
- **Launcher = VRAIE interface formulaire** (sélection agents/modes/filtre/concurrence/langue), **plus de JSON
  à éditer**. Le form fusionne dans la variable `benchmark` en **préservant** datasets/juge/bloc suggestions.
- **Results = grand public, langage clair, créatif, qui inspire confiance** : plus de jargon (« p50 », « taux
  d'erreur », « n=12 ») non expliqué -> verdict « X questions sur Y répondues correctement », libellés simples
  + explications/tooltips. « Bernadette de la finance » doit tout comprendre.
- **Bilingue EN par défaut + FR** (toggle), code en anglais.
- **SQL : lecture + append uniquement, ultra-prudent** (la connexion du projet voit toutes les tables).
- Critique/amélioration du **système benchmark** + du **stockage des suggestions** + audit **sécurité/danger** final.

## Fait

### Restructuration (moi)
- `benchmark_webapp/` -> lib partagée `views.py` (PUR) + **`dss.py` = chokepoint UNIQUE d'I/O dataiku/SQL**
  (commenté « READ + APPEND ONLY » : SELECT seul sur la connexion partagée ; écritures = append Flow via
  Dataset API uniquement) + **deux dossiers** `results/` et `launcher/` (chacun `body.html`/`style.css`/
  `script.js`/`backend.py`/`preview.html`). `results/backend.py` = **lecture seule** (4 routes, aucune écriture) ;
  `launcher/backend.py` = config GET + config POST **depuis le formulaire** (`build_config_object`) + run async
  single-flight + suggestions + promote. `views.py` étendu : `build_config_object` (form->variable, préserve le
  reste), `confidence_band` + KPIs « X sur Y », `minted_question_id`. `run_params` bloc `suggestions` (rappel).
- README réécrit (2 webapps + sûreté SQL + prérequis golden), SETUP étape 4 maj.

### Frontends (workflow : build -> critique panel -> correction -> re-critique -> re-correction, x2, 2 webapps)
- **Workflow 18 agents** (build + 2 rondes critique/correction par webapp, en parallèle). Résultat : 2 frontends
  framework-free, **bilingues EN/FR** (i18n JS, toggle persistant, **nombres formatés par locale**), charte Orange
  (carré/aplat/orange rare, thèmes clair/sombre via `[data-theme]`), MOCK pour QA offline, tout échappé (XSS).
- **QA visuelle Playwright** (serveur http local + previews MOCK) : **Results EN+FR x clair+sombre** (verdict en
  clair, donut de confiance, « HOW WE MEASURE THIS », libellés simples, OK/NOT OK, tags « à relire ») et
  **Launcher EN+FR x clair+sombre** (formulaire complet : agents éditables add/remove, modes en cases, catégories,
  concurrence, langue, réglages préservés lecture seule, lancement, suggestions). 0 erreur console.

### Critique/amélioration système benchmark + suggestions (workflow 3 agents -> j'applique)
0 critical/0 high. Appliqués : **(HAUT)** `agent_runner.run_matrix` : le timeout par-appel était **non
fonctionnel** (`as_completed` sans timeout) -> budget wall-clock global + lignes timeout pour les appels non
finis (vraie borne instance). **(MOYEN)** `step_run_matrix._load_golden_rows` appelle enfin `validate_golden_row`
(une ligne golden mal saisie corrompait l'ancre) + log. **(MOYEN)** `schemas.MODES` (obsolète eco/medium/high)
-> re-exporte `config.MODES` (Smart/Pro/Claude). **(MOYEN)** `judge.final_correctness` : verdict sans score ->
`needs_review` (plus de « correct » silencieux sans score). **(BAS)** regex `_NUMBER_RE` : retrait de l'espace
ASCII (anti faux-hit « 12 34 »), NBSP/narrow-NBSP gardés en échappements ASCII. **(BAS)** `_latest_run_id`
dropna aussi sur `run_timestamp`. **(BAS)** `question_filter` : avertit sur clé inconnue (typo -> tout le golden).
**(BAS)** `MAX_REFERENCE_CHARS` 8k->100k (un « Oui » stocke la réponse agent complète). **(BAS)** route from-chat
rejette un « Oui » sans réponse agent (`empty_agent_answer`).

### Audit SÉCURITÉ/DANGER dédié (workflow 4 audits + vérification adversariale par finding -> j'applique)
0 critical/0 high (la vérif a **réfuté** les faux positifs : pas d'auth = protégé au niveau DSS, injection prompt
juge non réelle). Findings confirmés (low/medium) corrigés :
- **(A, le principal)** le log des suggestions promues était réécrit depuis une lecture **qui avale les erreurs**
  (`read_dataset -> []`) -> truncation silencieuse (l'anti-pattern interdit, sur le log). FIX : le **golden devient
  la source de vérité** de « déjà promu » (`read_golden_question_ids` + `minted_question_id`, fail-open) ; le log
  `benchmark_suggestions_promoted` devient un **audit best-effort non-tronquant** (lecture qui lève, skip sur échec).
- **(B)** pas de verrou : `_PROMOTE_LOCK` autour de la promotion (anti lost-update concurrent) + `RUN_LOCK`
  single-flight sur le lancement (anti TOCTOU double-run) + doc « Prevent concurrent executions » sur le scénario DSS.
- **(C)** `read_dataset` chargeait toute la table scored en RAM avant de jeter les colonnes lourdes -> **projection
  de colonnes + limite à la lecture** (`get_dataframe(columns=, limit=)`, fallback si kwargs non supportés ;
  `SCORED_KEEP`).
- **(D)** si `golden_dataset` est une sortie de recette, la promotion serait effacée au rebuild -> **prérequis dur
  documenté** (golden_dataset = dataset managé autonome).

## Validé / non validé
- **Validé local** : **688 tests Python** (174 benchmark + 30 webapp + 484 plugin) + 124 node ; build Vite ;
  **QA visuelle Playwright** des 2 webapps (EN/FR x clair/sombre) ; revues = 0 crit/0 high (corrigées + tests) ;
  **0 tiret (31 fichiers)**. **DEV re-packagé** (`owismind_dev-upload.zip`, 72 entrées ; frontend Vue inchangé donc
  hash `index-BoETXxLb.js` identique ; python-lib à jour : routes.py + suggestions.py). **Prod intacte.**
- **NON validé DSS** : tout. Créer **2 webapps standard** en LAB (project-library `views.py`+`dss.py` ; 4 panes
  par webapp) + permissions + bloc `suggestions` + activer « Prevent concurrent executions » sur le scénario.
  Plugin : upload DEV + redémarrer backend.

## Prochaines étapes (DSS)
- Suivre `benchmark_webapp/README.md` (2 webapps). **golden_dataset DOIT être un dataset managé autonome** (sinon
  promotion effacée au rebuild). Vérifier la méthode dataikuapi de lancement de scénario sur l'instance.
- Plugin : upload `owismind_dev-upload.zip` + redémarrer backend (routes.py changé).
- Plus tard : guard programmatique anti-recette sur golden_dataset ; sync statut vers la table plugin ; juge en panel.

---

## Run (d) - Historique des benchmarks (append) + gestion CRUD du golden depuis le Launcher (L106)

**Repo only (benchmark/ + benchmark_webapp/ ; plugin NON touché). Suite directe des retours user post-test.**

### Objectif
Apres validation du test (renommage modes + benchmark), l'user a demande : (1) **garder l'historique**
des runs (chaque benchmark ecrasait le precedent) ; (2) **gerer les questions golden** (voir/ajouter/
modifier/supprimer + reponses attendues) **depuis la webapp Launcher**. Regle martelee : SQL read+append,
prudent.

### Fait
- **Historique (append)** : `benchmark/history.py` (pur : `merge_run_history` idempotent par run_id +
  `runs_to_keep` cap) + `benchmark/dss_steps/history_io.py` (read-merge-write, lecture gardee par schema
  qui LEVE = abort-safe L104). Les 3 steps appellent `write_history_dataset` au lieu de `write_with_schema`.
  Le modele portait deja `run_id`/`run_timestamp` + flags `score_all`/`aggregate_all` + filtre dernier-run :
  il suffisait de ne plus ecraser. `run_params` : `history_keep_runs` (defaut **50**, borne les tables
  LOURDES raw+scored ; summary+breakdown jamais bornees = historique complet pour Results ; `0`=illimite).
- **CRUD golden** : `views.py` pur (`golden_view`, `prepare_golden_save` + `mint_admin_question_id` prefixe
  `a_`, `apply_golden_upsert` preserve les colonnes extra, `apply_golden_delete`) ; `dss.py`
  (`read_golden_rows`, `save_golden_question`/`delete_golden_question` sous `_PROMOTE_LOCK` + `_golden_existing`
  schema-gated RAISING ; `_write_golden` via API Dataset) ; `launcher/backend.py` 3 routes `/api/golden*` ;
  `launcher/script.js` carte **Questions** (table + formulaire add/edit, toggle active, delete inline,
  bilingue EN/FR, charte). Ecritures = API Dataset sur le golden PROPRE du LAB, **jamais de SQL brut**.
- **Revue adversariale dediee (workflow, 4 dim + verif par finding) : 4 confirmes / 5 refutes, tous corriges** :
  (medium) cap par defaut borne sur tables lourdes ; (medium) bug flag `dirty` -> `refreshConfigMeta` ;
  (low) 1re ecriture sur golden jamais-construit -> `_golden_existing` ; + history_io durci pareil (schema
  ambigu -> lecture qui leve). astype redondant retire (RAM).

### Verifie
- **189 benchmark + 49 webapp + 484 plugin + 286 agents tests verts** ; JS valide ; build n/a (pas de plugin).
- **QA Playwright** : carte Questions rendue, editeur (8 champs, types lisibles), **save ajoute la question**,
  bascule EN/FR + theme sombre OK, confirm suppression OK, **flag dirty preserve apres save** (Launch reste
  desactive, edit config non sauve survit). 0 tiret.

### A faire DSS
Re-coller la librairie `benchmark/` (incl. **`history.py` + `dss_steps/history_io.py` NEUFS** + les 3 corps
de step) ; re-coller `views.py`+`dss.py` ; re-coller les 4 panes du Launcher (carte Questions). Guide :
`benchmark_webapp/DEPLOY_GUIDE.md` (C1/C3/C7/C8/D4). **Plugin inchange** (pas de zip/redemarrage pour CE lot).

## Run UI - Re-skin des 2 webapps LAB sur le mockup Orange fourni (Launcher + Results, rail retire)

### Objectif
L'user a fourni un mockup HTML/CSS/JS charte (`benchmark_webapp/mockup/OWIsMind_benchmark/`) pour le
**Launcher** puis pour **Results** : « implemente de cette maniere sans tenir compte de la barre laterale pour
le moment ». Refaire le frontend des 2 webapps DSS Standard pour coller au mockup, en gardant le backend reel.

### Fait
- **Launcher** (`benchmark_webapp/launcher/`) : `style.css` + `script.js` reecrits sur le mockup (header
  segments EN/FR + clair/sombre, onglets Configuration / Golden set / Suggestions, cartes tri-colonnes, aside
  Run + Reglages preserves, table golden + modale d'edition, toast), **branche au vrai backend** (`api/config`,
  `api/golden`, `api/run`, `api/suggestions`), MOCK conserve, EN/FR, dirty tracking, `agent_key` en coulisse,
  modale golden = 9 colonnes correctes (ancre valeur **+** type, categorie datalist, notes preservees),
  Suggestions = vraie revue/promotion.
- **Results** (`benchmark_webapp/results/`) : `style.css` + `script.js` + `body.html` + `preview.html` reecrits
  sur le mockup (donut + hero verdict, KPIs, cartes par configuration + sous-metriques, barres par sujet, table
  question-par-question + details depliables, aside de reference), **branche au vrai backend** (`api/results/
  runs|summary|breakdown|detail`), MOCK reutilise (memes formes), EN/FR, **nombres localises**, selecteur de
  run, filtre « a reverifier ». `body.html`/`preview.html` passes au montage `#bench-app` unique (comme le
  Launcher). Classification resultat reprise du backend (OK/Non OK/Plausible).
- **Rail retire** des deux (feature future). Tiret cadratin du mockup (`agent — mode`) supprime (regle #9),
  ellipses en `...`.

### Decisions
- Porter le mockup mais **garder le contrat backend** + MOCK pour la preview ; **0 Python touche** (les 49
  tests webapp restent verts, pas de build/zip - webapps Standard).
- Couleurs SVG/pastilles via **`style="stroke:var(...)"`** (CSS), pas en attribut SVG (`var()` non resolu en
  attribut de presentation) -> reactif au theme. Voir **L107**.
- Thème sur `<html>` ; shell construit une fois dans `#bench-app` ; branche d'erreur AVANT `ensureShell()`.

### Verifie (QA Playwright sur les 2 preview.html, MOCK)
- Launcher : config save -> toast + dirty efface ; creation golden via modale (+1 question, badge) ; lancement
  run (running -> poll -> Termine, FR) ; Suggestions select-all + promote ; EN/FR x clair/sombre + persistance.
- Results : hero/donut/KPIs/cartes/sujets/table rendus ; deplier detail (reponse attendue/agent) ; filtre « a
  reverifier » (7 -> 2) ; selecteur de run recharge sans erreur ; EN/FR x clair/sombre + nombres FR localises.
- **0 erreur console** sur les 2 ; **49 tests webapp Python verts** ; **0 tiret (7 fichiers webapp scannes)**.

### A faire DSS
Rien de neuf cote backend/datasets (frontend seul). Au deploiement des 2 webapps Standard (voir
`benchmark_webapp/README.md` / `DEPLOY_GUIDE.md`), coller les `body.html`/`style.css`/`script.js` mis a jour
dans les panes. Noms de fichiers et routes inchanges -> guide toujours valable. Le **rail** (nav inter-webapps)
reste pour une session ulterieure. Voir **L107**.

## Run nettoyage - Grand menage du repo (junk + scratch + maquettes) + doc sortie du contexte auto (L108)

### Objectif
L'user : « gros nettoyage sur le plan de travail, supprimer ce qui ne sert a rien (code mort, .md inutiles),
ne garder que l'essentiel et le code fonctionnel (webapps, agents, scenario, variables...) ».

### Reconnaissance (Workflow ultracode : 7 scouts paralleles + verification adversariale, 16 agents)
Fan-out d'un scout par region (frontend Vue, backend python-lib, dataiku-agents, benchmark+benchmark_webapp,
docs/, project-documentation/, root+memory), chacun rendant un inventaire structure {path, kind, confidence,
evidence, recommandation} ; puis 9 candidats « dead-code / obsolete-script / a supprimer » passes en
**verification adversariale** (greper tout le repo, prouver que c'est encore utilise avant d'autoriser la
suppression). **Constat capital : ZERO code mort.** Tout le frontend (composants/composables/stores/views/
features), tout le backend (28 modules + 24 tests), tous les agents DEV+PROD, recettes, tools, modeles
semantiques, et tout le benchmark + 2 webapps LAB sont cables et utilises. Le « gras » est uniquement :
junk OS, scratch de workflows, maquettes consommees, et doc EN periment/redondante.

### Fait (suppressions)
- **Junk pur (non-suivi, gitignore, regenerable)** : 16 `.DS_Store` + 31 dossiers `__pycache__` (incl. les
  `.pyc` orphelins `test_orchestrator_v3` et `backend.cpython` de l'ancienne archi). `git status` reste propre.
- **56 fichiers suivis supprimes** (tous docs/scratch/maquettes, 0 code) :
  - `docs/scaling/.workdir/` (25) + `project-documentation/.workdir/` (15) = scratch de workflows (marques
    « supprimable » dans le README / la memoire).
  - `style-reference/` (maquette HTML Account/Admin, remplacee par `docs/cadrage/CHARTE_ORANGE_UI.md`).
  - `benchmark_webapp/mockup/OWIsMind_benchmark/` (7) = maquette Orange du re-skin 2026-06-26 (termine + QA).
  - `docs/superpowers/plans/2026-06-11-orchestrator-expert-authority.md` (plan orphelin ; dossier `plans/`
    deja vide avant, specs gardees) -> dossier `plans/` retire.
  - `docs/screenshots/` (4 PNG Evidence v1, superseded par v2).
  - `docs/scaling/PLAN_AGENTS.md` + `PLAN_WEBAPP.md` + `README.md` (plans de scaling) -> dossier `docs/scaling/`
    retire (devenu vide).
- **Doc PAS supprimee (decision user)** : `project-documentation/` (91 fichiers) et tout `docs/` restent. A la
  place, sortie du contexte auto : `project-documentation/` ajoutee a **`.graphifyignore`** (plus dans le
  graphe -> plus en contexte auto, lisible a la demande) + note explicite dans **`CLAUDE.md`** section Reference
  (« a lire uniquement a la demande, exclue du graphe, potentiellement perimee, MAJ future »). Nettoye 2 entrees
  mortes du `.graphifyignore` (`style-reference/`, `docs/screenshots/` supprimes). `docs/agentic-research/` GARDEE.
- **Gardes (verif adversariale)** : `drop_column_and_reindex.py` (util reutilisable), `migrate_/remap_semantic_model.py`
  (promotion PROD pas finie), `build_aligned_semantic_model.py` (ref canonique des instructions), `tools/build_dev_plugin.py`.

### Decisions
- **On ne supprime JAMAIS la documentation** (user) : on la met a jour plus tard, on la sort juste du contexte
  auto des agents. Levier = `.graphifyignore` (exclusion du graphe) + note CLAUDE.md, PAS de suppression.
- Suppressions faites en `rm` simple (non-stagees) pour rester revisables ; restaurables via git.

### Valide / non valide
- **VALIDE (local)** : **1132 tests verts** apres nettoyage (484 plugin + 286 agents + 189 benchmark + 49
  benchmark_webapp + 124 node) ; controle de surete = 0 fichier de code/test supprime. Aucune action DSS.

### Prochaines etapes
- A la reprise de la doc : mettre a jour `project-documentation/` (perimee : renommage modes, vieux chemins
  agents `dataiku-agents/agents|tools|recipes`, benchmark non couvert) - ou decider de la regenerer.
- Le graphe purge `project-documentation/` au prochain `/graphify --update` (fait dans ce log-session).

## Run final - Fix nom de table trop long (NAMEDATALEN) + reorg archi du benchmark sous `OWIsMind_LAB/` (L109 + L110)

### Objectif
- L'user a remonte un **500 en DSS** sur `/benchmark/suggest-from-chat` : nom de table physique trop long.
- Puis : terminer la session/semaine par un **bon nettoyage d'archi** : que le repo **reflete l'archi Dataiku**
  du benchmark, qu'on voie que c'est un **projet a part (`OWIsMind_LAB`)**, sans changer le code.

### Fait - (1) Fix table-name length-safe (L110)
- Cause : `webapp_golden_suggestions_v1` (28 c.) + prefixe DEV `webapp_devtest` + project key = **65 octets > 63**
  -> `pg_identifier` LEVE (anti-troncature) -> `ensure_golden_suggestions_table` plante. Invisible en prod
  (sans prefixe = 50 octets), se voit **seulement en DEV**. L103 avait corrige les INDEX, pas le nom de TABLE.
- Fix dans `Plugin/owismind/python-lib/owismind/storage/sql_config.py` : helper `_shorten_identifier` ;
  `physical_table` y passe. **<=63 = inchange** (0 donnee orpheline) ; **>63 = tete lisible + hash 10c du nom
  complet** (deterministe car create-if-not-exist, anti-collision). Coherent partout (full_table, storage_status,
  safe_index_name). +4 tests de regression (`test_suggestions_storage.py`).
- **DEV re-package** (`owismind_dev-upload.zip`, 72 entrees, `index-pktQ-ICh.js`, prod intacte) -> uploade par
  l'user -> **CONFIRME en DSS** : table creee `OWISMIND_DEV_webapp_devtest-owismind_webapp_golden_s_90f625c2f8`
  (63 octets), INSERT committe, suggestion visible. (La grille SQL paraissait vide a cause du filtre "alge" tape
  dans la recherche du notebook, pas un vrai vide.)

### Fait - (2) Reorg `OWIsMind_LAB/` (L109)
- `git mv` (46 renommages, historique garde) : `benchmark/` -> `OWIsMind_LAB/project-library/python/benchmark/` ;
  `benchmark_webapp/` -> `OWIsMind_LAB/project-library/python/benchmark_webapp/` (lib `views.py`+`dss.py`) ;
  panes web SORTIS de la lib -> `OWIsMind_LAB/webapps/{benchmark_launcher, benchmark_results}/` (fidele au
  decoupage DSS lib != webapp). Junk purge avant (`.DS_Store`, `__pycache__`).
- **Packages inchanges** (`from benchmark ...`, `from benchmark_webapp ...`) = **zero recoll DSS**.
- Nouveaux : **`OWIsMind_LAB/README.md`** (la carte repo<->DSS : tableau fichier->objet DSS, connexions au plugin
  + aux agents, pointeurs vers les 2 guides) + **`OWIsMind_LAB/local-variables.example.json`** (template complet
  de la variable `benchmark`, valeurs reelles DEV dont le nom de table raccourci).
- Docs/commandes mises a jour : test command (`-t` sur la lib root) dans les 2 README + SETUP_GUIDE + DEPLOY_GUIDE
  + docstring `test_views.py` ; chemins repo des panes web dans README + DEPLOY_GUIDE ; **`CLAUDE.md`** (Identifiants
  + Reference -> `OWIsMind_LAB/README.md`) ; note de localisation en tete de `PROJECT_STATE.md` 8c.

### Valide / non valide
- **VALIDE DSS** : le fix table-name (table creee + suggestion ecrite + relue, logs user).
- **VALIDE local** : reorg = **726 tests Python verts** (238 LAB [189 benchmark + 49 webapp] + 488 plugin) ;
  0 tiret dans mes ajouts ; git voit des **renommages** (R), pas delete+add.
- **NON encore fait DSS** (cote LAB) : creer les 2 webapps Standard + recoller la lib + bloc `suggestions` avec
  le **nom de table raccourci** (`benchmark.suggestions.table`) -> alors l'onglet Suggestions du Launcher verra
  la suggestion `pending` (read cross-projet `WHERE status='pending'`).

### Prochaines etapes
- Cote LAB : suivre `OWIsMind_LAB/project-library/python/benchmark_webapp/DEPLOY_GUIDE.md` (PARTIE C) pour creer
  les 2 webapps + coller le nom de table raccourci dans la variable `benchmark`.
- Promotion prod du fix sql_config quand voulu (rebuild + package prod + upload).
