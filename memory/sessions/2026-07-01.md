# Session 2026-07-01 - LAB benchmark launcher : nettoyage + 4 fixes + 1 feature (repo only, partiellement validé DSS)

> Tout dans `OWIsMind_LAB/` (projet DSS séparé du plugin). Frontend launcher construit/QA contre son
> MOCK embarqué -> plusieurs décalages de contrat MOCK vs vrai backend débusqués. Code déjà commité par
> la session concurrente/user ("benchmark improuvinguuu" x3). Voir **L115**.

## Objectif
Corriger les soucis remontés par l'user en itérant sur la webapp Launcher du benchmark : structure de
fichiers (webapp DSS standard), affichage des questions golden, identité d'agent, formulaire d'édition
complet, et le bouton "à refaire" qui échouait.

## Fait (dans l'ordre)

### 1. Nettoyage webapp launcher = vraie webapp standard (4 fichiers)
- **Fusion `journey.js` -> `script.js`** (un seul onglet JS en DSS) : la logique pure `Journey`
  (`runnableLabel`/`benchmarkListState`/`createGate`/`cellChip`/`evolutionToken`) inline en tête de
  l'IIFE. `journey.js` supprimé (`git rm`). Bootstrap navigateur gardé par `if (typeof document !==
  "undefined")` + `module.exports = Journey` -> `script.js` requerable en Node sans planter.
- `test/journey.test.js` repointé sur `../script.js` (5/5 vert). `body.html` sans balises `<script src>`
  (DSS injecte le pane JS ; parité avec la webapp Results). `preview.html` charge seulement `script.js`.
- `DEPLOY_GUIDE.md` remis à l'histoire simple à 4 fichiers. `__pycache__` retiré.

### 2. Fix contrat golden `questions`/`rows` (questions golden invisibles) - ✅ VALIDÉ DSS
- **Cause** : `/api/golden` (`views.golden_tag_view`) renvoyait `{rows}` sans `agents`, scope `this` ;
  le front `loadGoldenTag` + le MOCK attendent `{questions, agents}` scope `agent`. Contrat jamais
  matché (le front marchait en preview via le MOCK). `gt.list = d.questions || []` = `[]` -> "Aucune
  question correspondante", même sur l'onglet "Toutes".
- **Fix** : `golden_tag_view` renvoie `questions` (+ scope `agent`, alias `this`) ; la route ajoute
  `agents = dss.agents_catalog()`. Test `test_golden_tag_view_scope` réécrit sur le bon contrat.
- User : "ça marche beaucoup mieux".

### 3. agent_key = l'ID de l'agent PARTOUT (jamais un slug) - ✅ VALIDÉ DSS
- **Directive user** : « jamais orchestrator, toujours l'id de l'agent ; le nom c'est juste pour
  l'affichage ; tout géré depuis la webapp (on récupère les agents + leurs id, on clique, pas de
  hardcode) ». 3 clés divergeaient pour le même agent : slug `orchestrator` (variable/exemple/MOCK),
  slug projet dérivé `owismind_dev_038g7mlf` (connect flow), et l'id `038G7mlF` (le golden). Le golden
  avait raison.
- **Fix** : `registry.agent_catalog_key(project, agent_id)` = **agent_id sans préfixe `agent:`,
  verbatim** (drop du slug + prefixe projet). `registry.normalize_agent` **dérive toujours** la clé de
  l'id (ignore toute `agent_key` hardcodée). `run_params._resolve_agents` idem (dérive l'id, ignore la
  clé, ne drop plus sur clé vide mais sur project/agent_id vides). Purge des `orchestrator` en dur :
  `local-variables.example.json`, MOCK launcher (`agent_key`/`agent_tagged`), MOCK results, docstrings
  `run_params.py`, `SETUP_GUIDE.md`. Tests registre + run_params mis à jour (clé = id).
- Les fixtures de tests gardent `orchestrator` comme chaîne opaque (aucun impact).

### 4. Formulaire golden : 3 colonnes manquantes (édition de TOUT le dataset) - non validé DSS
- Ajout au form `buildGoldenTagForm` + au payload de save : **`expected_value`** (texte),
  **`expected_value_type`** (select sur l'enum `numeric/currency/date/string/list` + "(aucun)"),
  **`notes`** (textarea). i18n EN/FR (`gt.formValue`/`gt.formValueType`/`gt.formValueTypeNone`/
  `gt.formNotes`). Le form couvre maintenant les 12 colonnes golden (sauf `question_id` = clé immuable).
- Backend inchangé (déjà bon : `prepare_golden_save` -> `normalize_golden_row` gère les 12 col).
- **Bug bonus corrigé** : le payload inline (`gtInlinePersist`, toggle actif/agent) reconstruisait la
  ligne mais **oubliait `notes`** -> un toggle inline écrasait les notes. `notes` ajouté (les autres
  colonnes y étaient déjà).

### 5. Fix redo `value`/`include_next` ("nothing to run" au redo) - non validé DSS
- **Cause** : `/api/benchmark/redo` lisait `body.get("include_next")` ; le front (et le MOCK) envoient
  **`value`** -> `bool(None)=False` -> **chaque clic "à refaire" = décocher** -> le flag n'est jamais
  stocké -> `resolve_to_run` ne voit aucun redo -> `ValueError: nothing to run for benchmark ...`.
- **Fix** : le backend lit `value` (`include_next` en alias défensif). Scan des autres endpoints
  benchmark (create/delete/modes/launch) = **aucun autre décalage** de nom de champ, `redo` était le seul.
- **Défaut secondaire trouvé (non corrigé, proposé)** : `reconcile_redo_after_run` efface le redo des
  questions du **dernier run scored existant** sans vérifier que ce run a bien consommé le flag (postérieur
  au flag). Sur un run qui échoue / ne produit aucune ligne scored, il nettoie le redo sur l'ANCIEN run.
  Le chemin normal (run réussi) marche. Fix propre = snapshot du dernier `run_timestamp` au launch +
  ne nettoyer que les questions dont une ligne scored est plus récente. En attente d'accord user.

## Décisions
- **Aligner le vrai backend sur le contrat front/MOCK** (pas l'inverse) : le front est construit/QA
  contre le MOCK, donc le MOCK fait foi de contrat (golden `questions`/`agents`/scope `agent` ; redo `value`).
- **Identité d'agent = l'agent_id, dérivé, jamais un slug** ; enforce dans le code (registry + run_params)
  pour que tout soit piloté par la sélection webapp sans hardcode. Décision user 2026-07-01.
- Fixes focalisés + vérifiés ; `reconcile` durci proposé mais non fait (fix ciblé d'abord).

## Validé / non validé
- **✅ Validé DSS** : affichage des questions golden (contrat `questions`) + agent_key = id (onglet
  "Cet agent" matche le golden). User : "ça marche beaucoup mieux".
- **⏳ Non validé DSS** (à recoller) : les 3 champs du formulaire golden (script.js) ; le fix redo
  `value` (backend.py). Le défaut `reconcile` reste ouvert.

## Prochaines étapes
- **Recoller en DSS** (Launcher webapp) : onglet **JS = `webapps/benchmark_launcher/script.js`** (champs
  du form) + onglet **Python = `webapps/benchmark_launcher/backend.py`** (fix redo). Recharger la webapp.
  Smoke : éditer une question (valeur attendue + type + notes persistent), cocher "à refaire" -> "Run
  pending" -> la question repasse (2e tentative + évolution).
- **Décider** du durcissement de `reconcile_redo_after_run` (snapshot run_timestamp au launch).
- Recoller aussi `views.py` (golden_tag_view) + `registry.py` + `run_params.py` + MOCK results
  `script.js` s'ils ne le sont pas déjà en DSS (le contrat golden + agent_key en dépend).

## Fichiers touchés (tous commités par la session concurrente/user, "benchmark improuvinguuu" x3)
- `OWIsMind_LAB/project-library/python/benchmark/registry.py` (agent_catalog_key + normalize_agent)
- `OWIsMind_LAB/project-library/python/benchmark/run_params.py` (_resolve_agents + docstring)
- `OWIsMind_LAB/project-library/python/benchmark_webapp/views.py` (golden_tag_view)
- `OWIsMind_LAB/webapps/benchmark_launcher/backend.py` (route golden + redo)
- `OWIsMind_LAB/webapps/benchmark_launcher/script.js` (journey merge, form fields, MOCK id)
- `OWIsMind_LAB/webapps/benchmark_launcher/{body.html,preview.html,test/journey.test.js}` ; `journey.js` supprimé
- `OWIsMind_LAB/webapps/benchmark_results/script.js` (MOCK id)
- `OWIsMind_LAB/local-variables.example.json` ; `SETUP_GUIDE.md` ; `benchmark_webapp/DEPLOY_GUIDE.md`
- tests : `test_registry.py`, `test_run_params.py`, `test_views.py`

Vérifs : 335 tests LAB + node 5/5 verts, 0 tiret.

---

## Run 2 (session parallèle) - LAB launcher : clarté benchmark detail + galerie d'agents + flux "ajouter un agent" + GRAND NETTOYAGE ultracode

> Session menée **en parallèle** de la Run 1 ci-dessus (deux sessions Claude sur `OWIsMind_LAB/`, commits
> entrelacés par l'user "benchmark improving"/"improuvinguuu"). Tree fusionné re-vérifié cohérent avant log.
> Repo only, DSS partiellement validé. Voir **L116**.

### Objectif
Itérer sur la webapp Launcher d'après retours user (captures) : écran blanc à l'ouverture, agents mieux
présentés, vue d'un benchmark "flou total" à clarifier ; puis pouvoir **ajouter des agents sans toucher au
code** ; enfin, sur demande explicite ("ultracode, tout clean"), **nettoyer tout le code obsolète**.

### Fait
1. **Benchmark detail RE-CLARIFIÉ** (`script.js`/`style.css`) : suppression du **double** "Run pending"
   (bandeau + bouton) ; **carte de statut unique** qui explique l'unité (« test = question x mode :
   {q} x {m} = {t} »), 3 tuiles (Testées X/T · À lancer N · À refaire) ; **un seul** bouton primaire
   « Lancer en attente (N) » dont le compteur = cellules (résout « affiche plus que le nb de questions »,
   + fix i18n : le label était toujours anglais) ; **titre section + légende** OK/MISS/En attente au-dessus
   du tableau ; pastille « Pending » localisée. Mock `bm_said01` rendu cohérent (ledger 3/6, pas 1/3).
2. **Accueil = galerie d'agents en cartes** (fin de l'écran blanc) : `home-mode` masque le rail latéral à
   l'accueil ; le rail **revient seulement à l'ouverture d'un agent**. Cartes carrées (tuile + nom + projet
   + id + Ouvrir), refresh async branché.
3. **Flux "Ajouter un agent" (ajout SANS toucher au code)** : couche catalogue dans la variable `benchmark`
   (clé `agents`). NOUVEAU dans `registry.py` (pur) : `parse_agents`/`normalize_agent`/`upsert_agents`/
   `remove_agent`/`serialize_agents`/`agent_catalog_key`. `dss.py` : `agents_catalog`/`list_projects`/
   `list_project_agents` (découverte **read-only bornée** `list_llms` filtré `agent:`, id + nom best-effort)/
   `connect_agents`/`remove_agent_from_catalog` ; **ancien bloc dump-all `discover_agents`/`connect_agent`
   supprimé**. `backend.py` : routes `/api/agents(+/projects,/project-agents,/connect,/remove)`. Front :
   panneau "projet -> liste des agents (id+nom éditable) -> multi-sélection -> ajouter" + retrait avec
   confirm inline ; remplace le "discover tout" aveugle. **QA preview EN+FR** (ajout + retrait bout-en-bout).
4. **GRAND NETTOYAGE (ultracode)** : Workflow 7 scouts + **vérif adversariale** par candidat (défaut =
   garder ; règles dures : docs jamais supprimées, artefacts prod intouchés, sources recollées-DSS ≠ mortes) ->
   58 candidats (25 sûrs / 21 à valider / **12 faux positifs écartés**). Appliqué : junk disque ; code mort
   symbol-level (LAB lib + plugin `aggregate`/`schema_check`/`useMarkdown`/`useChatStream`) ; fonctions v1
   mortes LAB (`existing_names`/`read_promoted_ids`/`validate_config`/`config_view` + tests dédiés) ; et via
   sous-agent le **vieux sous-système "config/run" + cluster v2-benchmark mort** du launcher (**-945 l.
   script.js**, -50 style.css : 37 fonctions, 13 champs d'état, 125 clés i18n, ~15 CSS, 7 branches mock).
   Sous-agent **discipliné** : a refusé de retirer `renderCats`/`markDirty`/... (encore atteignables depuis
   la fonction VIVANTE `refreshConfigMeta`, no-ops confirmés au smoke-test).

### Décisions
- **Catalogue d'agents dans la variable `benchmark` (clé `agents`)** : ajouter un agent = data, pas de code.
- **Sélection manuelle** (projet -> agents -> cocher) plutôt que dump des ~40 agents de l'instance (retour user).
- **Nettoyage : garder** (choix user via AskUserQuestion) les ~130 clés i18n inutilisées du `messages.json`
  plugin (pristine, inoffensives, échafaudage possible) **et** `golden_view`/`read_golden_rows` (v1 mortes
  mais testées).
- **Dead-code d'un webapp = atteignabilité depuis les entrées réelles** (`render`/`init`/wiring), pas grep
  textuel (un cluster mort s'auto-référence et paraît vivant). Cf L116.

### Validé / non validé
- **✅ QA preview navigateur** (EN+FR+thème, tous les flux) : benchmark detail clair, galerie, ajout/retrait
  d'agent, Golden/toggle/Suggestions/Review = 0 erreur console (hors favicon).
- **Filet** : LAB **335** tests · plugin **509 + 134** · build Vite OK · node launcher/results OK · **0 tiret**.
- **⏳ NON validé DSS** : tout ce Run 2 (recoll nécessaire). Le flux d'ajout d'agent + le catalogue n'ont
  jamais tourné sur l'instance.

### Interaction avec la Run 1 (à savoir)
- La Run 1 a changé `agent_catalog_key` -> **id-based** (agent_id sans `agent:`). Mon `mockAgentKey`
  (preview du flux d'ajout) utilise encore le slug `project_+id` -> **divergence preview-only, inoffensive**
  (le vrai backend passe toujours par `registry.agent_catalog_key`). À aligner un jour si on veut la parité mock.
- Mon nettoyage a tourné **par-dessus** les commits de la Run 1 (tree fusionné re-vérifié vert).

### Prochaines étapes
- **Recoller en DSS (Launcher)** : `registry.py`, `dss.py`, `views.py` (lib) + `backend.py` + `script.js`
  + `style.css` (panes) ; **redémarrer le backend** (python-lib changé). Côté plugin : `benchmark_view/
  {aggregate,schema_check}.py` + `frontend/src/composables/*` (rebuild pour effet, 0 changement de comportement).
- **Vider/initialiser la variable** `benchmark.agents` (elle contient encore les ~40 agents auto-découverts
  de l'ancien dump) puis ajouter les agents voulus via le nouveau flux.
- Smoke DSS : accueil galerie -> Ajouter un agent (projet -> agents id+nom -> cocher) -> ouvrir -> benchmark
  detail clair -> run pending. Optionnel : aligner `mockAgentKey` sur `agent_catalog_key` (parité preview).

### Fichiers touchés (Run 2)
- LAB lib : `benchmark/registry.py` (catalogue), `benchmark/config.py`, `benchmark/dss_steps/step_run_matrix.py`,
  `benchmark_webapp/{views.py,dss.py}` + tests (`test_registry.py`, `test_views.py`)
- LAB webapps : `benchmark_launcher/{backend.py,script.js,style.css}`, `benchmark_results/script.js`
- Plugin : `python-lib/owismind/benchmark_view/{aggregate.py,schema_check.py}`,
  `frontend/src/composables/{useMarkdown.js,useChatStream.js}`
- Junk purgé (git-ignoré) : `.DS_Store`, `__pycache__`, `.playwright-mcp`, `.claude/worktrees` vide

---

## Run 3 - Visibilité complète des résultats benchmark (réponse + SQL généré + tableau) + override + extension plugin

### Objectif
Retour user : "il me manque de la visibilité sur les résultats du benchmark". Trois demandes : (1) voir la
**réponse complète** de l'agent (pas le preview tronqué), le **tableau de données** qui a servi, et le **SQL
réellement généré par l'agent** (pas seulement le SQL de référence), pour bon comme mauvais, afin de
comprendre où ça a marché ou déraillé ; (2) pouvoir **override le verdict du juge** en tant qu'humain,
depuis la **webapp launcher** ; (3) (2e tour) étendre cette visibilité à la **page results du plugin**.

### Fait - Partie A (LAB : results + launcher Review) - commit `511e4bd` (user/session concurrente)
Constat clé : **les données étaient DÉJÀ capturées et stockées** (schemas RAW/SCORED) : `generated_sql_json`
= liste `[{sql, success, row_count, result:{columns, rows}}]` (le SQL réel de l'agent + le tableau capturé),
`answer_text` (réponse complète), `full_answer`, `total_rows`. Donc **aucun re-run nécessaire** : c'était un
trou d'**exposition + affichage**. Cause racine : `dss.SCORED_DROP` droppe volontairement les colonnes
lourdes à la lecture (sûreté instance ~100k chars/ligne) et `detail_view` ne renvoyait qu'un `answer_preview`
tronqué à 280c. **Override : DÉJÀ présent** dans le launcher (onglet Review, routes `/api/review` +
`/api/override`, survit aux runs) - il manquait juste de quoi décider.
- `benchmark_webapp/views.py` : NOUVEAU `full_detail_view(row)` PUR (parse `generated_sql_json` -> sql_items +
  answer complet, bornes défensives) + helpers `_parse_json_list`/`_cap_cell`/`_shape_result`/`_shape_sql_item`.
- `benchmark_webapp/dss.py` : NOUVEAU `read_scored_row_full(...)` = lecture **à la demande, une ligne** via
  `iter_rows` (streaming, arrêt à la 1re ligne matchant les 4 clés, backstop 50000). Pas de nouveau SQL brut,
  1 ligne en RAM -> invariant de sûreté préservé.
- Route `/api/results/attempt` (results) + `/api/review/attempt` (launcher).
- Front : `benchmark_results/script.js` (rendu au dépli : réponse complète + SQL généré + tableau, + i18n
  EN/FR + MOCK + CSS) ; `benchmark_launcher/script.js` (même chose dans l'onglet Review, détail chargé dans
  `S.review.detail[rk]`, au-dessus des boutons override) + CSS (`--green`/`--red`, pas `--good`/`--bad`).

### Fait - Partie B (plugin : page consultation) - À COMMITER ce log (8 fichiers)
Extension de la même visibilité à `BenchmarkSuggestView.vue` (le plugin lit la table scored du LAB en
**cross-projet SQL**, pas via Dataset API).
- `benchmark_view/aggregate.py` : `full_detail_view(row)` porté (constantes locales, pas de dépendance
  agent_capture) + 4 tests.
- `benchmark_view/lab_io.py` : `read_scored_row_full(block, run_id, question_id, agent_key, mode)` =
  `SELECT` paramétré borné (colonnes lourdes intersectées avec le schéma LIVE), WHERE 4 clés, `LIMIT 1`,
  read-only + statement_timeout. Bloc agent résolu côté serveur (jamais de table depuis le front).
- `api/routes.py` : route `GET /benchmark/attempt` (tous users, même sécurité que `/benchmark/results`).
- `services/backend.js` : `fetchBenchmarkAttempt`. `stores/benchmark.js` : état `attemptDetail` (par rowKey)
  + action `loadAttempt(row)` (reset au reload des résultats). `BenchmarkSuggestView.vue` : `toggleRow(row)`
  charge le détail au dépli, section de rendu (réponse complète + SQL généré + tableau) + CSS scopé tokens.
  `i18n/extra.js` : `bench.ev.*` FR+EN.

### Décisions
- **Chargement à la demande, une ligne** = seule option sûre pour l'instance (les colonnes lourdes restent
  droppées de la lecture de liste). LAB : `iter_rows` streaming ; plugin : `SELECT ... LIMIT 1` paramétré.
- **Ne PAS re-capturer / re-run** : la donnée est déjà en base (schemas v2). Un vieux run d'avant la capture
  de `generated_sql_json` dégrade proprement ("aucun SQL capturé").
- L'override du juge **existait déjà** côté launcher : on n'en a pas recréé, on a ajouté la visibilité pour
  décider. (Dit à l'user par transparence.)

### Validé / non validé
- **LAB** : 342 tests (335+7), node launcher 5/5, **harnais de rendu headless 16 checks** sur le code réel
  des 2 script.js (réponse/SQL/tableau + cas dégradés + **échappement XSS**). 0 tiret.
- **Plugin** : 513 tests backend (509+4), 134 node, **build Vite OK**, 0 tiret, **zip DEV construit**
  (`owismind_dev-upload.zip`, 78 entrées, `index-d58-B9PB.js` / `BenchmarkSuggestView-Dnkuoloc.js`, PROD
  intacte, invariants DEV OK).
- **NON validé DSS** (les deux parties) : QA navigateur live impossible (profil Chrome occupé pour le LAB ;
  le plugin a besoin du backend DSS -> pas de preview offline). Réactivité Vue de `attemptDetail` raisonnée
  saine (ref déep, mutation de clé trackée), non vue live.

### Prochaines étapes (DSS)
- **LAB** : recoller project-library (`views.py`, `dss.py`) + les panes des 2 webapps
  (`backend.py`+`script.js`+`style.css` de results et launcher) ; recharger. Pas de re-run.
- **Plugin** : uploader `owismind_dev-upload.zip` (DEV) + **redémarrer le backend** (python-lib changé).
  Valider en DEV puis demander la promotion prod.
- Smoke : déplier une question (bonne ET mauvaise) -> réponse complète + SQL généré + tableau ; override
  dans le launcher avec le contexte visible.
