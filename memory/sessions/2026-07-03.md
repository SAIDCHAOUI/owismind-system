# Session 2026-07-03 - Grand nettoyage / optimisation du repo (mémoire, docs, code mort, LAB)

## Objectif
Session de nettoyage et d'optimisation avant la phase production / bêta : mémoire projet,
sessions et contenus obsolètes, gestion des connaissances, plugin, système agentique,
benchmark, code mort / fichiers inutiles / doublons. Méthode imposée : auditer, classifier,
proposer ; ne rien supprimer de risqué sans validation explicite.

## Fait

### Audit (Workflow : 7 scouts Sonnet en lecture seule + vérification adversariale Opus par candidat, 18 agents)
- Zones : memory/, docs+CLAUDE.md+skills, dataiku-agents/, backend plugin, frontend, OWIsMind_LAB/, hygiène+xref.
- **Verdict code : déjà remarquablement sain** (2 grands nettoyages 2026-06-26 + 2026-07-01 et revues
  adversariales par session) : backend plugin = 0 code mort / 0 import inutile (scan AST 46 fichiers),
  frontend = 89 fichiers tous consommés (0 orphelin), dataiku-agents = rien d'orphelin post-réorg,
  hygiène git = 0 scratch tracké, 0 fichier > 500 KB. Le gras était dans la MÉMOIRE et la DOC.
- Baseline vérifiée AVANT toute modification : 629 back + 316 agents + 342 LAB + 186 node, tout vert.

### Mémoire (objectif n°1)
- **`CONTEXT.md` : 1546 -> 431 lignes (-72 %)** (patch Opus contrôlé). Supprimé : ~20 blocs de session
  pleins du « Focus courant » (chacun couvert par la chaîne condensée ET `sessions/*.md`) ; chaîne 🧭
  gardée depuis le 2026-06-24 + bloc « historique antérieur (archivé) » pointant sessions/ + LESSONS +
  PROJECT_STATE ; items DONE des Prochaines étapes réduits à 2 lignes. Gardé verbatim : Run 6 + Run 5,
  règles #9/#10, section « Top gotchas » byte-identique.
- **`PROJECT_STATE.md` : 773 -> ~720 lignes, sections périmées réparées** : §2b gelée-datée + stockage
  corrigé v5, §4 = snapshot 2026-07-03 réel de l'arborescence (api/agents/storage/evidence/security/
  benchmark_view...), §11 = matrice PAR SOUS-SYSTÈME (remplace la table par session), §12 = pointeur
  vers CONTEXT.md. + mes 6 correctifs chat_v4 -> chat_v5 dans le §5 (présentés comme courants).
- **`LESSONS.md` : 5 marqueurs de péremption ajoutés** sous les titres : L062 (obsolète -> L063),
  L067 (partiellement : auto-escalade retirée -> L071, answer_delta reste valide), L068 (obsolète ->
  L071), L073 (reverté -> L075), L086 (supplanté -> L087). Motif : une session lisant LESSONS.md seul
  pouvait réappliquer un design reverté.
- `sessions/*.md` : INTACTS (archive de référence, jamais supprimés).

### Code mort supprimé (chaque cible vérifiée adversarialement à 0 référence)
- `frontend/src/i18n/extra.js` : **14 clés mortes** (fr+en, 32 lignes) : ev.table.next/prev/page,
  ev.column, src.column, sb.account, rail.chat, set.usage.tokens, bench.desc, bench.consult.run_label,
  ag.new_conv_with, ag.tools_section_empty, ag.generic_desc, ag.generic_tagline. `fb.reason.other`
  GARDÉE (usage dynamique `t('fb.reason.'+code)` détecté par le vérificateur). `messages.json` intact
  (décision user 2026-07-01). + retrait de 'src.column' de SRC_KEYS dans `test/sourceModel.test.js`.
- LAB `benchmark/registry.py` : **`slug_agent_key()` supprimée** (+ son test + `import re` devenu
  inutile) : supplantée par `agent_catalog_key()` id-based (L115/L116), 0 appelant production.

### Bug corrigé (validé user) : LAB launcher `GET /api/config` manquant
- L'audit a trouvé : `script.js refreshConfigMeta()` (4 call sites) appelle `GET /api/config`, le MOCK
  l'a, le vrai `backend.py` NON -> 404 silencieux en DSS (best-effort), compteurs/catégories périmés
  après édition golden. 3e instance de la dérive MOCK vs backend (L115).
- Fix (choix user « implémenter la route ») : `views.config_meta_view` pure (+2 tests) + route
  `@_safe` read-only dans `backend.py` (2 lectures projetées via `dss.read_dataset` existant, 0 SQL
  neuf), contrat = miroir exact de `MOCK.config` (status/config/categories/question_count/mode_options/
  runs newest-first). LAB : 343 tests verts.
- Décision user : `/api/benchmark/rename` (chaîne backend complète testée, 0 appelant UI) = GARDÉE.

### Docs remises au niveau du code
- Mécanique (moi) : `chat_v4` -> `chat_v5` dans les 6 docs vivants (README, architecture, backend-api,
  data-model, security, evidence-trust-layer) : les specs `docs/superpowers/specs/` restent gelées ;
  `ensure_chat_v4_table` -> `ensure_chat_table` ; `CONV_TITLE_MAXLEN` 140 -> 56 ; `ev.column` retiré de
  frontend.md.
- Complétion (patch Opus, choix user « compléter maintenant ») : **backend-api.md = 34/34 routes**
  (13 ajoutées : /track, /chat/stop, /usage, /source/meta|rows|distinct, /admin/sources/datasets,
  /admin/budget(+/users), /benchmark/* x8) + table des modules +11 + convention impersonation + codes
  402/403 ; fix contrat périmé /evidence/rows (page -> limit/offset + q). **data-model.md = 8/8 tables**
  (+5 sous-sections §2.4-2.8 : usage_monthly, artifacts, user_quota, golden_suggestions, events) +
  colonnes usage/mode de chat_v5 + compteurs lifetime users_v1 + exception ADD COLUMN IF NOT EXISTS
  documentée. Caveat : les refs file:line des parties ANCIENNES non rafraîchies (add-only).
- `dataiku-agents/README.md` : liens sous-dossiers réparés (agents/tools -> OWISMIND/README.md +
  registry ; recipes/semantic_model -> chemins réels). READMEs semantic_model DEV+PROD : ligne
  `drop_column_and_reindex.py` ajoutée (utilitaire réel non documenté).
- Skill `agentique-python-dataiku` : note « chemins historiques » en tête des 2 références citant des
  fichiers déplacés/supprimés (dataset_expert_agent.py etc.) ; tirets préexistants du skill non touchés
  (exclusion ADR-0012).
- Junk local purgé : .DS_Store + __pycache__ (non trackés).

## Décisions
- **User (AskUserQuestion)** : (1) /api/config -> implémenter la route (aligner backend sur MOCK, L115) ;
  (2) /api/benchmark/rename -> garder ; (3) docs -> compléter maintenant (pas juste un caveat).
- Sessions/*.md jamais supprimés (archive) ; condensation = CONTEXT.md seulement.
- `sb.account` supprimée d'extra.js : la décision « garder les clés inutilisées » ne couvrait que
  messages.json (pristine) ; extra.js est notre fichier maintenu.
- Specs `docs/superpowers/specs/` = gelées par design, chat_v4 y reste (historique).
- Modèle d'agents : scouts/exploration = Sonnet, patches ciblés + vérifs adversariales = Opus
  (consigne user de session, cohérente avec `subagents-use-opus-not-fable`).

## Validé / non validé
- ✅ Local : 629 back + 316 agents (2 skipped) + 343 LAB (+2) + 186 node verts ; build Vite OK ;
  0 référence résiduelle aux 15 symboles supprimés ; 0 tiret ajouté (scan Python du diff complet) ;
  git status = exactement 22 fichiers modifiés attendus, 0 parasite.
- ⏳ NON validé DSS : la route `/api/config` (recoller `benchmark_webapp/views.py` + pane Python
  `benchmark_launcher/backend.py`, recharger la webapp ; smoke = éditer une golden -> compteurs/
  catégories se rafraîchissent au lieu de rester périmés).
- Rien de ce nettoyage ne touche le zip plugin ni les Code Agents (aucun redéploiement plugin requis).

## Prochaines étapes
- Recoller en DSS LAB : `views.py` + `backend.py` launcher (route /api/config) - peut se faire avec le
  recoll déjà en attente des fixes L115/L117.
- Rappels hors nettoyage inchangés : deploy audit agents L118, visibilité benchmark L117, fixes LAB
  L115, 2 webapps LAB, finalisation agent tickets.
- `owismind-relaunch-email.html` : placeholder `[Insert OWIsMind link]` toujours non rempli (rappel).
- Optionnel futur : rafraîchir les refs file:line anciennes de docs/ ; câbler une UI pour
  /api/benchmark/rename.
