# Session 2026-07-03 - Grand nettoyage / optimisation du repo (mémoire, docs, code mort, LAB)

## Objectif
Session de nettoyage et d'optimisation avant la phase production / bêta : mémoire projet,
sessions et contenus obsolètes, gestion des connaissances, plugin, système agentique,
benchmark, code mort / fichiers inutiles / doublons. Méthode imposée : auditer, classifier,
proposer ; ne rien supprimer de risqué sans validation explicite.

## Fait

### Audit (Workflow : 7 scouts Sonnet en lecture seule + vérification adversariale Opus par candidat, 18 agents)
- Zones : memory/, docs+CLAUDE.md+skills, dataiku-agents/, backend plugin, frontend, OWIsMind_LAB/, hygiène+xref.
- **Verdict code : déjà remarquablement sain** (2 grands nettoyages 2026-06-26 + 2026-07-01 et revues
  adversariales par session) : backend plugin = 0 code mort / 0 import inutile (scan AST 46 fichiers),
  frontend = 89 fichiers tous consommés (0 orphelin), dataiku-agents = rien d'orphelin post-réorg,
  hygiène git = 0 scratch tracké, 0 fichier > 500 KB. Le gras était dans la MÉMOIRE et la DOC.
- Baseline vérifiée AVANT toute modification : 629 back + 316 agents + 342 LAB + 186 node, tout vert.

### Mémoire (objectif n°1)
- **`CONTEXT.md` : 1546 -> 431 lignes (-72 %)** (patch Opus contrôlé). Supprimé : ~20 blocs de session
  pleins du « Focus courant » (chacun couvert par la chaîne condensée ET `sessions/*.md`) ; chaîne 🧭
  gardée depuis le 2026-06-24 + bloc « historique antérieur (archivé) » pointant sessions/ + LESSONS +
  PROJECT_STATE ; items DONE des Prochaines étapes réduits à 2 lignes. Gardé verbatim : Run 6 + Run 5,
  règles #9/#10, section « Top gotchas » byte-identique.
- **`PROJECT_STATE.md` : 773 -> ~720 lignes, sections périmées réparées** : §2b gelée-datée + stockage
  corrigé v5, §4 = snapshot 2026-07-03 réel de l'arborescence (api/agents/storage/evidence/security/
  benchmark_view...), §11 = matrice PAR SOUS-SYSTÈME (remplace la table par session), §12 = pointeur
  vers CONTEXT.md. + mes 6 correctifs chat_v4 -> chat_v5 dans le §5 (présentés comme courants).
- **`LESSONS.md` : 5 marqueurs de péremption ajoutés** sous les titres : L062 (obsolète -> L063),
  L067 (partiellement : auto-escalade retirée -> L071, answer_delta reste valide), L068 (obsolète ->
  L071), L073 (reverté -> L075), L086 (supplanté -> L087). Motif : une session lisant LESSONS.md seul
  pouvait réappliquer un design reverté.
- `sessions/*.md` : INTACTS (archive de référence, jamais supprimés).

### Code mort supprimé (chaque cible vérifiée adversarialement à 0 référence)
- `frontend/src/i18n/extra.js` : **14 clés mortes** (fr+en, 32 lignes) : ev.table.next/prev/page,
  ev.column, src.column, sb.account, rail.chat, set.usage.tokens, bench.desc, bench.consult.run_label,
  ag.new_conv_with, ag.tools_section_empty, ag.generic_desc, ag.generic_tagline. `fb.reason.other`
  GARDÉE (usage dynamique `t('fb.reason.'+code)` détecté par le vérificateur). `messages.json` intact
  (décision user 2026-07-01). + retrait de 'src.column' de SRC_KEYS dans `test/sourceModel.test.js`.
- LAB `benchmark/registry.py` : **`slug_agent_key()` supprimée** (+ son test + `import re` devenu
  inutile) : supplantée par `agent_catalog_key()` id-based (L115/L116), 0 appelant production.

### Bug corrigé (validé user) : LAB launcher `GET /api/config` manquant
- L'audit a trouvé : `script.js refreshConfigMeta()` (4 call sites) appelle `GET /api/config`, le MOCK
  l'a, le vrai `backend.py` NON -> 404 silencieux en DSS (best-effort), compteurs/catégories périmés
  après édition golden. 3e instance de la dérive MOCK vs backend (L115).
- Fix (choix user « implémenter la route ») : `views.config_meta_view` pure (+2 tests) + route
  `@_safe` read-only dans `backend.py` (2 lectures projetées via `dss.read_dataset` existant, 0 SQL
  neuf), contrat = miroir exact de `MOCK.config` (status/config/categories/question_count/mode_options/
  runs newest-first). LAB : 343 tests verts.
- Décision user : `/api/benchmark/rename` (chaîne backend complète testée, 0 appelant UI) = GARDÉE.

### Docs remises au niveau du code
- Mécanique (moi) : `chat_v4` -> `chat_v5` dans les 6 docs vivants (README, architecture, backend-api,
  data-model, security, evidence-trust-layer) : les specs `docs/superpowers/specs/` restent gelées ;
  `ensure_chat_v4_table` -> `ensure_chat_table` ; `CONV_TITLE_MAXLEN` 140 -> 56 ; `ev.column` retiré de
  frontend.md.
- Complétion (patch Opus, choix user « compléter maintenant ») : **backend-api.md = 34/34 routes**
  (13 ajoutées : /track, /chat/stop, /usage, /source/meta|rows|distinct, /admin/sources/datasets,
  /admin/budget(+/users), /benchmark/* x8) + table des modules +11 + convention impersonation + codes
  402/403 ; fix contrat périmé /evidence/rows (page -> limit/offset + q). **data-model.md = 8/8 tables**
  (+5 sous-sections §2.4-2.8 : usage_monthly, artifacts, user_quota, golden_suggestions, events) +
  colonnes usage/mode de chat_v5 + compteurs lifetime users_v1 + exception ADD COLUMN IF NOT EXISTS
  documentée. Caveat : les refs file:line des parties ANCIENNES non rafraîchies (add-only).
- `dataiku-agents/README.md` : liens sous-dossiers réparés (agents/tools -> OWISMIND/README.md +
  registry ; recipes/semantic_model -> chemins réels). READMEs semantic_model DEV+PROD : ligne
  `drop_column_and_reindex.py` ajoutée (utilitaire réel non documenté).
- Skill `agentique-python-dataiku` : note « chemins historiques » en tête des 2 références citant des
  fichiers déplacés/supprimés (dataset_expert_agent.py etc.) ; tirets préexistants du skill non touchés
  (exclusion ADR-0012).
- Junk local purgé : .DS_Store + __pycache__ (non trackés).

## Décisions
- **User (AskUserQuestion)** : (1) /api/config -> implémenter la route (aligner backend sur MOCK, L115) ;
  (2) /api/benchmark/rename -> garder ; (3) docs -> compléter maintenant (pas juste un caveat).
- Sessions/*.md jamais supprimés (archive) ; condensation = CONTEXT.md seulement.
- `sb.account` supprimée d'extra.js : la décision « garder les clés inutilisées » ne couvrait que
  messages.json (pristine) ; extra.js est notre fichier maintenu.
- Specs `docs/superpowers/specs/` = gelées par design, chat_v4 y reste (historique).
- Modèle d'agents : scouts/exploration = Sonnet, patches ciblés + vérifs adversariales = Opus
  (consigne user de session, cohérente avec `subagents-use-opus-not-fable`).

## Validé / non validé
- ✅ Local : 629 back + 316 agents (2 skipped) + 343 LAB (+2) + 186 node verts ; build Vite OK ;
  0 référence résiduelle aux 15 symboles supprimés ; 0 tiret ajouté (scan Python du diff complet) ;
  git status = exactement 22 fichiers modifiés attendus, 0 parasite.
- ⏳ NON validé DSS : la route `/api/config` (recoller `benchmark_webapp/views.py` + pane Python
  `benchmark_launcher/backend.py`, recharger la webapp ; smoke = éditer une golden -> compteurs/
  catégories se rafraîchissent au lieu de rester périmés).
- Rien de ce nettoyage ne touche le zip plugin ni les Code Agents (aucun redéploiement plugin requis).

## Prochaines étapes
- Recoller en DSS LAB : `views.py` + `backend.py` launcher (route /api/config) - peut se faire avec le
  recoll déjà en attente des fixes L115/L117.
- Rappels hors nettoyage inchangés : deploy audit agents L118, visibilité benchmark L117, fixes LAB
  L115, 2 webapps LAB, finalisation agent tickets.
- `owismind-relaunch-email.html` : placeholder `[Insert OWIsMind link]` toujours non rempli (rappel).
- Optionnel futur : rafraîchir les refs file:line anciennes de docs/ ; câbler une UI pour
  /api/benchmark/rename.

## Run 2 (nuit) : refonte du systeme benchmark (launcher LAB + page plugin), session autonome

### Objectif
Session dediee OWIsMind_LAB / benchmark, user absent (dodo, zero question possible) :
(1) reequilibrer le launcher LAB (script.js 4177 l. quasi single-file -> markup statique dans
body.html, styles dans style.css, JS = logique seulement) + clarifier l'UX de gestion des
benchmarks ; (2) refondre la page de consultation benchmark du plugin (BenchmarkSuggestView.vue) :
clarte, remplissage ecran, modes conditionnels (mono-mode = pas d'appareil de modes), questions
et details lisibles (fin du mur de texte). Orchestration multi-agents (5 scouts Sonnet -> 3
builders Opus), specs ecrites par l'orchestrateur (scratchpad spec-launcher.md / spec-plugin.md).

### Fait
**Track A - launcher LAB** (`OWIsMind_LAB/webapps/benchmark_launcher/`) :
- Panes reequilibres : `tpl-shell` (header/nav/panels/footer/toast/drawer) en `<template>` dans
  body.html ; `ensureShell()` = clonage + message clair si template absent ; `applyI18n()` etendue
  (data-i18n / -ph / -title) ; `preview.html` = injection `fetch("body.html")` puis script.js
  (necessite http.server, zero duplication) ; styles inline -> classes (sauf width% dynamique).
- Code mort A1 (verifie grep + ids inexistants) : renderCats/renderAside/renderSaveError/
  setDirtyUI/markDirty/chkBtn + champs d'etat ; `S_bench4ActiveBid` -> `bench4ActiveBid` ;
  CSS dupliques morts purges ; vert succes unifie ; couleur redo stabilisee.
- UX (A2) : detail benchmark en 3 zones (tete+cluster Manage / RUN avec micro-label + tuiles +
  sous-texte expliquant N / Resultats+legende) ; **UI Rename** cablee sur la route existante
  `/api/benchmark/rename` (+ handler MOCK miroir : vide/80c/unicite par agent case-insensitive) ;
  lien mort `#results` -> bouton "Review the results" ouvrant Review PRE-FILTRE sur le benchmark
  (S.review.benchmarkId + route de retour S.reviewReturn, Back restaure le detail) ; crTagBtn +
  bd4TagQBtn -> golden-tag agent-scope (une seule destination) ; getting-started masque sur la
  home ; poubelle agent visible en permanence ; badge compteur suggestions en attente (best-effort).
- **Fusion golden (orchestrateur, apres decouverte QA)** : le reroutage UX-1 rendait le panneau
  Golden global legacy INATTEIGNABLE (le lien header pointait DEJA golden-tag avant la session ;
  seuls crTagBtn/bd4TagQBtn y menaient encore). Champs modal legacy = memes colonnes que le form
  golden-tag (submitModal mappait anchor -> expected_value). Donc suppression complete du panneau +
  modal (renderGolden/goldenTableHtml/qRefHtml/qRowHtml/wireGolden/findGolden/openModal/closeModal/
  submitModal/editorNotes/toggleActive/deleteQuestion/loadGolden, S.golden/S.editor, tpl-modal,
  goldenBack, 46 cles DICT mortes, 68 regles CSS mortes) ; le datalist categories migre sur le
  form golden-tag (`gtCatList`, rempli par refreshConfigMeta = /api/config garde un consommateur
  REEL et atteignable ; live-refresh du datalist) ; breadcrumb golden-tag corrige (segment agent
  vide + segment benchmark perime gardes par route).
- Bilan lignes : script.js 4177 -> **3917**, body.html 9 -> 67, style.css 947 -> 871,
  preview.html 18 -> 36. backend.py INTOUCHE (contrat + MOCK preserves, redo envoie `value`).
**Track B - page plugin** (`BenchmarkSuggestView.vue` 2487 -> 2618, `benchmarkResults.js`
128 -> 290, `extra.js` +34 = 17 cles fr+en, tests +2 fichiers) :
- B-1 : 21 helpers purs extraits/testes (hasMultipleModes, modeClass/Color/Label, rowAccentKind,
  formatCell locale-aware >= 10000 seulement, attemptHistory...) + 18 tests node + NOUVEAU test
  de parite i18n fr/en (i18nExtraParity.test.js, 3 tests).
- B-2 modes conditionnels : mono-mode => zero badge mode, KPI configurations masque, legende
  modes retiree de l'aside, agent unique = label statique, benchmark unique = picker cache.
- B-3 : liste de questions en cartes-lignes scannables (chips categorie/mode/tentatives/evolution,
  pill resultat + score + drapeau a reverifier, rail gauche 3px danger/warn).
- B-4 : detail par onglets Tabs.vue reutilise sans modification (Full answer / SQL & data /
  Reference si presente / History si n>1) + bandeau meta + reponses attendue/produite cote a cote
  + override admin conserve ; cellules numeriques formatees (12,100,000 ; 2025 intact).
- B-5/B-6/B-7 : bande haute hero+KPI auto-fit sur une rangee ; aside sticky 320px contextuelle ;
  skeletons plats charte + etats vides/erreur en cartes.
**Verification** (quota subagents MORT en cours de session -> tout inline par l'orchestrateur) :
- Checks statiques scriptes : i18n complet (t()+data-i18n+fr/en), byId 100% resolus, classes CSS
  croisees, miroir mock/backend rename, patterns bannis charte, 0 tiret (9 fichiers).
- QA runtime Playwright (skill verify) : launcher preview MOCK = parcours complet (home, rename
  vide/doublon/valide, run mock, Review pre-filtre + Back, golden-tag + datalist categories
  rempli, settings, suggestions, EN/FR x clair/sombre, 0 erreur console).
- **Harnais stub plugin** (scratchpad/qa/stub_server.py) : dist Vite servi sous le base path +
  fixtures /owismind-api (me admin, agents has_benchmark, results multi/mono/empty/notconf,
  attempt) + injection `window.getWebAppBackendUrl` dans index.html -> page pilotee en vrai :
  multi-mode OK, MONO-MODE = tous les conditionnels verifies, onglets/History/formatage OK,
  dark OK, etats non-configure/vide en cartes sans crash.
- **1 bug trouve et corrige par la QA** : `.qside` (flex 0 0 auto) ecrasait le titre de question
  a 54px quand la colonne contenu est etroite (aside a cote, 1080-1400px) -> `.qhead` flex-wrap:
  wrap + `.qmain` min-width min(240px,100%).
- Suites : **629 back + 316 agents + 343 LAB + 207 node plugin (186+21) + 5 journey**, parse OK,
  build Vite OK, zip DEV `owismind_dev-upload.zip` (81 entrees, `index-B61pkfo9.js`), PROD intacte.

### Décisions
- Suppression du panneau Golden legacy : decision orchestrateur (user absent), justifiee par
  "une seule destination" + "alleger le js" demandes explicitement ; AUCUNE capacite perdue
  (parite champs verifiee, datalist migre) ; /api/config (decision user 2026-07-03 matin) garde
  un consommateur reel.
- Refonte nav lourde (Golden/Suggestions/Review en onglets par agent, rail permanent) NON faite :
  trop structurant sans feedback user ; propose en prochaine etape.
- Revue adversariale multi-agents (5 lentilles + refuteurs) + QA agents : ECHOUEE (quota session,
  reset 4:10am) -> remplacee par verification inline orchestrateur (checks scriptes + QA manuelle
  Playwright). A relancer en debut de prochaine session si souhaite.
- `owismind-relaunch-email.html` : modifie par l'user (annonce beta 8 juillet, demo 10:00) hors
  session -> EXCLU du commit de session, laisse en working tree.

### Validé / non validé
- Valide LOCAL (runtime QA reelle) : tout ce qui precede. **NON valide DSS** (rien recolle).
- Artefact mock preexistant note : apres un run mock, les compteurs du detail benchmark
  s'incoherent (mockTotal=3 fige, cells non synchronisees) - preview only, fonctions ledger
  byte-identiques a HEAD, sans impact DSS.
- Note : dropdown Review affiche le nom stampe dans scored (ancien nom apres rename, jusqu'au
  run suivant) - comportement du VRAI backend, mock fidele.

### Prochaines étapes
- **Recoller en DSS (batch)** : LAB launcher = 3 panes (body.html + script.js + style.css,
  backend.py inchange mais la route /api/config du Run 1 reste a coller) ; plugin = upload zip
  DEV `index-B61pkfo9.js` + redemarrer backend (python-lib inchange ce run mais zips anterieurs
  en attente L117). Smoke : rename, Review pre-filtre, badge suggestions, datalist categories ;
  page benchmark mono-mode vs multi-mode, onglets detail, dark.
- Relancer la revue adversariale multi-agents sur les diffs de ce run (quota reset 4:10am).
- Differe propose : refonte nav launcher (onglets scopes agent) ; titre section "Results by agent
  and mode" a conditionner en mono-mode ; port des ameliorations plugin vers la webapp LAB
  results (meme visuel).

## Run 2b (matin) : hotfix CSS launcher (purge qui avait casse la syntaxe)
- **Retour user au reveil : la webapp launcher DSS "ne ressemble plus a rien"** (rail affiche sur
  la home, icone geante, header en vrac). Cause : la purge CSS du Run 2 avait MANGE l'accolade
  fermante de `@keyframes fade` (mon parseur de regles prenait le premier `}` interne pour la fin
  de la regle sur les at-rules a blocs imbriques) -> 479 `{` vs 478 `}` -> le navigateur avalait
  TOUT le CSS apres la ligne 202 (~670 lignes : header, rail, cartes, toast, drawer).
- **Echec de verif assume** : la casse etait deja visible dans `launcher-05-goldentag-final.png`,
  pris SANS le regarder ; les screenshots propres dataient d'avant la purge ; les checks post-purge
  (computed style ponctuels + console) n'ont pas suffi.
- **Fix** : suppression du bloc `@keyframes fade` + 2 reliquats modal orphelins (`.overlay.on`,
  `.modal-body .full`), `textarea.input` conserve (vivant). 474/474 accolades, 0 tiret, 5 journey +
  343 LAB verts. Re-verif visuelle COMPLETE en regardant chaque capture : home, detail benchmark,
  golden-tag + drawer settings (fixed + scrim), toast 16px, dark. Rendu identique aux bonnes
  captures du Run 2.
- **A recoller DSS : UNIQUEMENT le pane CSS** (style.css) du launcher ; HTML + JS deja en place
  sont bons. Voir **L130**.

## Clôture (matin)
- Working tree propre : l'user a commite lui-meme ses editions du mail de relance
  (`a95efbd`, `7c5c3be` "email html") ; le point ouvert du Run 2 est resolu.
- Recoll DSS launcher en cours cote user (HTML + JS visiblement en place sur son screenshot) ;
  le pane CSS doit etre recolle depuis style.css POST-hotfix (`a571474`).
