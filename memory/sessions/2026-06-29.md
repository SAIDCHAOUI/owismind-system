# Session 2026-06-29 - Benchmark phase finale : juge contextuel + override humain + consultation dans le plugin

## Objectif
Perfectionner le systeme de benchmark : (A) juge plus strict mais contextuel + colonne commentaire ;
(B) humain dans la boucle (revoir/override le verdict du juge) ; (C) integrer la CONSULTATION des
resultats dans la webapp plugin owismind (dropdown d'agent, pour tous) + cabler un benchmark a une
fiche d'agent (table + validation de schema). Generaliser a n'importe quel agent Dataiku.

## Decisions (brainstorm + retours en cours de route)
- **Juge = autorite contextuelle** : l'ancre objective redevient un SIGNAL (un HIT confirme, un MISS
  ne force plus "faux", le juge tranche) ; parsing des ordres de grandeur ; la note humaine = contrat
  de severite ; colonne `judge_comment` concise. (Spec = `docs/superpowers/specs/2026-06-29-benchmark-final-phase-design.md`.)
- **1 table `scored` par agent** : le plugin lit cette table en cross-projet et recalcule la
  restitution (agregateur dedie `benchmark_view/aggregate.py`, pas de copie de `scoring.py`).
- **Override = UPDATE dans `scored`** (colonnes `human_*`). Robuste : la table empile l'historique par
  `run_id`, donc un override survit aux runs suivants (seul re-juger le MEME run_id l'efface).
- **Plugin = CONSULTATION uniquement** (decision user en cours de session) : AUCUN launcher dans le
  plugin ; le lancement (config + golden + suggestions + run) reste sur les webapps LAB. L'override
  admin par question reste dans le plugin ("laisser tel quel").
- **Reproduction NATIVE du design LAB results** dans la consultation du plugin (exigence user : "pas
  d'iframe"). Port du CSS de la webapp LAB `results` mappe sur les tokens semantiques du plugin.

## Fait
### LAB (recolle = project-library + webapp launcher) - tout en TDD
- `benchmark/judge.py` : `normalize_number` conscient des magnitudes (k/M/Md, FR+EN, garde `\b`
  anti-faux-positif "36 minutes") ; `_NUMBER_RE` harvesting avec suffixe magnitude ; `final_correctness`
  ancre-signal (HIT confirme, MISS delegue au juge, desaccord -> needs_review) ; `JUDGE_OUTPUT_SCHEMA`
  + `_coerce_judge_payload` + `_safe_failure` gagnent `comment` (<=200c) ; `build_judge_prompt` +
  `run_llm_judge` + `_JUDGE_SYSTEM` prennent la note humaine (contrat de severite).
- `benchmark/schemas.py` : `notes` dans `RAW_COLUMNS` ; `judge_comment` + `human_verdict`/`human_correct`/
  `human_comment`/`reviewed_by`/`reviewed_at` dans `SCORED_COLUMNS` ; helper pur `effective_correct`.
- `benchmark/scoring.py` : `_accuracy` (donc summarize+breakdown) sur le verdict EFFECTIF (override prime).
- `benchmark/agent_runner.py` : `_denormalized_golden` porte `notes` sur la ligne RAW.
- `benchmark/dss_steps/step_judge.py` : passe `notes` au juge, ecrit `judge_comment`, initialise les
  colonnes `human_*` vides. (`step_run_matrix` : `notes` via le runner.)
- `benchmark_webapp/views.py` : `detail_view` surface `agent_key` + `judge_comment` + verdict effectif
  + champs override ; helpers purs `validate_override` + `apply_override`.
- `benchmark_webapp/dss.py` : `write_override` (read-modify-write du dataset `scored` sous
  `_OVERRIDE_LOCK`, schema-gated abort-safe ; meme profil RAM que le merge d'historique). Invariant SQL
  doc mis a jour (rewrite via Dataset API, jamais de DML brut).
- `webapps/benchmark_launcher/backend.py` : routes `api/review` + `api/override` (identite relecteur
  best-effort + horodatage backend). `script.js` + `style.css` : onglet "Review" (sous-agent), QA navigateur OK.

### Plugin (zip DEV)
- NOUVEAU `python-lib/owismind/benchmark_view/` : `schemas.py` (effective_correct + listes de colonnes,
  porte), `aggregate.py` (view-model consultation purpose-built + override pur), `schema_check.py`
  (colonnes requises -> manquantes), `agent_profile.py` (validation du bloc benchmark), `lab_io.py`
  (DSS : lecture cross-projet bornee + listing tables + UPDATE override parametre + COMMIT).
- `security/validation.py` : `validate_agent_meta` gagne le bloc `benchmark {enabled, connection, table,
  agent_key}` (borne/clamp, table invalide -> "").
- `api/routes.py` : `/agents` expose `has_benchmark` ; `GET /benchmark/results` (tous) ;
  `GET /admin/benchmark/tables`, `POST /admin/benchmark/validate-table`, `POST /admin/benchmark/override`
  (admin, fence impersonation).
- Frontend (sous-agent puis moi) : `BenchmarkSuggestView.vue` = onglet Benchmark (consultation pour tous
  + dropdown agent + sous-section suggest + override admin par question) ; `AdminView.vue` = section
  Benchmark dans l'editeur de fiche d'agent (toggle + connexion + sélecteur de table + agent_key +
  bouton Valider le schema) ; `services/backend.js` (4 fns) ; `stores/benchmark.js` (consultation +
  override) ; `composables/benchmarkResults.js` + `test/benchmarkResults.test.js` ; `i18n/extra.js`.
- **Restyle (moi, au 1er plan apres un sous-agent tombe sur une erreur 529 transitoire)** : la
  consultation reproduit la mise en page de la webapp LAB `results` (hero donut + verdict pill + note +
  meta ; rangee de 5 KPIs ; cartes par config avec badge mode + barre + 4 sous-metriques ; bloc topic
  par sujet ; table `rtable` + details avec boites reponse attendue/agent + commentaire juge ; aside
  reference + legendes). Tokens semantiques, pas de hex en dur, pas d'iframe. Gotcha `var()` en attribut
  SVG respecte (couleurs via `:style` / classes CSS).

## Validé / non validé
- ✅ Local : **276 tests LAB + 508 plugin + 133 node** verts. Build Vite OK. Zip DEV repackage
  (`owismind_dev-upload.zip`, 78 entrees, `index-BK29Kqtv.js`). **Prod intacte** (git status vide).
  **0 tiret** sur tous les fichiers touches (46+ Python/launcher + 3 frontend).
- ⏳ **NON valide DSS** : rien deploye. La fidelite visuelle de la consultation se confirme en DSS
  (la vue Vue a besoin du backend pour `/agents` + `/benchmark/results` ; pas de rendu local possible).

## Prochaines étapes (DSS)
1. **LAB** : recoller `benchmark/{judge,schemas,scoring,agent_runner}.py` + `dss_steps/{step_run_matrix,
   step_judge}.py` + `benchmark_webapp/{views,dss}.py` + le launcher (`script.js`+`style.css`+`backend.py`).
   Relancer un run pour materialiser les nouvelles colonnes `scored` (judge_comment + human_* + notes raw).
2. **Plugin** : uploader `owismind_dev-upload.zip` (Uploaded) + **redemarrer le backend**. Puis
   Administration > Agents > profil : activer le benchmark, choisir connexion + **table physique du
   scored LAB**, valider le schema, poser `agent_key`.
3. **Verifier** : consultation (dropdown -> donut/verdict/KPIs/cartes/sujets/Q-par-Q, design = LAB
   results) ; validation de schema (table OK vs colonnes manquantes) ; override admin (flip + survie au
   run suivant) ; revue/override sur le launcher LAB.
4. Promotion prod quand DEV valide (build + package prod + upload). Le fix `sql_config` (L110) sera promu
   au meme moment.

## Notes
- Le launcher embarque cross-projet dans le plugin est DIFFERE (decision user) : il vit sur les webapps LAB.
- Un sous-agent de restyle est tombe sur une 529 (overload transitoire, 0 action) -> restyle fait au 1er plan.
