# Session 2026-07-21 : deploiement v1.3 guide + assistant guide persistant dans la console

## Contexte
L'user deploie la v1.3 en DSS (suite des sessions du 2026-07-20). Clone aligne VALIDE
("le clonage est parfait"), console reparee (L163). Il teste alors la creation d'un
domaine "opportunities" (2 tables : DRIVE.Opportunities_Won + DRIVE.Opportunities_Open).

## Ce qui s'est passe (terrain)
1. Explication v1.3 vs v1.2 reformulee simplement (3 problemes concrets) : OK.
2. Parcours "Nouveau domaine" console : datasets/modele/scenario crees, MAIS
   scenario en echec sur une PARTITION (dataset partitionne en amont), rapport +
   etapes manuelles PERDUS au rechargement (etat 100 % RAM JS), et le Code Agent
   jamais genere : `templates/dataset_expert.py` ABSENT du hub (le notebook 01 ne
   le pousse que si la sonde a confirme les cles ; sinon simple print MANUAL, rate).
3. User agace, ABANDONNE opportunities (2 datasets = 1 stack prealable requis, la
   regle 1 modele = 1 table). Il supprime les objets DSS crees.
4. Demande explicite : ameliorer la webapp (avec GPT Sol) : session persistante,
   etat en table SQL (pattern du backend plugin), parcours guide de la selection
   du dataset a l'outil actif dans l'orchestrateur, chaque etape manuelle dit quoi
   faire, "c'est fait" -> VERIFICATION -> etape suivante.

## Livre (working tree, commit de session)
- `owismind_factory/guided.py` : machine 13 etapes (preflight avec BLOCAGE dataset
  partitionne + collisions + hub ; plan dry-run ; infra ; first_build ; relecture
  profil ; wizard ; brain+tool ; template ; generation code agent ; collage Code
  Agent ; capability (enabled=false) ; smoke ; enable). Etapes manuelles = 
  instructions FR exactes + verification DSS obligatoire ; prechecks POSITIFS
  uniquement (un "je ne peux pas verifier" n'avance jamais). Ids captures de
  proche en proche (model -> tool -> agent -> capability).
- `owismind_factory/guided_store.py` : persistance 1 ligne SQL par run
  (`{PK}_owismind_factory_guided_v1`, CREATE IF NOT EXISTS, valeurs parametrees
  Constant/toSQL, COMMIT, garde identifiants 63 octets, caps state_json), +
  `table_has_rows` (SELECT 1 LIMIT 1, timeout, tri-state True/False/None).
- Console backend : 5 routes `/api/guided/*` (confirm partout, 1 run actif, 1
  mutation a la fois, job fond + journal live, self-heal d'une etape RUNNING
  apres restart backend).
- Front (GPT-5.6 Sol, brief `.codex/briefs/guided-ui.md` sur contrat fige,
  re-verifie cote Claude + reviewer charte Opus = conforme) : onglet Assistant
  1er + defaut, formulaire (liste datasets rafraichissable), stepper 13 etapes,
  panneau par statut, wizard integre, reprise apres reload, abandon sans
  suppression.
- Docs : README console (ecran Assistant, permissions SQL), AGENT_FACTORY.md
  (module map). Memoire : L164, CONTEXT (focus + next steps).

## Preuves
- Suites : agents+factory 667 (48 nouveaux), backend 965, LAB 343, front 365 :
  toutes vertes. `node --check` + dash-check + audit ES5 sur les 3 panes front.
- 2 faux departs Codex : sandbox lecture seule (L164 : `--write` obligatoire, et
  un `--resume-last` HERITE du sandbox du thread d'origine -> `--fresh --write`).

## Reste a faire (repris dans CONTEXT)
- Recoller en DSS : lib owismind_factory (2 nouveaux fichiers) + 4 panes console ;
  tester le parcours complet sur un dataset simple non partitionne.
- User : suppression des objets opportunities (zone, scenario, modele, tool).
- Etendre notebook 01 (template + run_settings + workflow prompt) ou corriger le guide.

## Run 2 (apres-midi) : deploiement reel de l'assistant + VALIDATION DE BOUT EN BOUT

### Objectif
L'user recolle l'assistant en DSS (via push/pull, demande explicite de PUSH desormais)
et cree un vrai domaine : `delivery_snapshot` (dataset Delivery_Snapshot, 20 col / 2237 lignes).

### Fait (4 fixes terrain, chacun commite + pousse)
1. `5c3b26a` : le wizard ne force plus AUCUN knob de sampling (temperature=0 cassait la
   connexion Claude thinking : "temperature may only be set to 1") + cle hub `llm_wizard`
   (choix du modele par l'operateur, vide = llm_sonnet) + seeds regeneres.
2. `7dd5fff` : lecture du profil via iter_tuples + schema (pattern VALIDE du sous-agent
   revenus) au lieu d'iter_rows (lignes dict-like -> le wizard voyait une entree opaque
   "payload" et posait des questions absurdes) + garde anti profil degenere (L165).
3. `caf2ed9` (run 1 tardif) : provenance du brouillon wizard (base_dataset stampe, brouillon
   perime refuse, precheck saute seulement sur correspondance prouvee).
4. `cb07b78` : preflight detecte un orchestrateur live NON hub-aware (lit son code via les
   hints confirmes de la sonde) : le dernier bloqueur terrain etait l'oubli du re-collage
   v1.3 des 3 agents (orchestrateur v1.2 = capabilities embarquees, hub ignore, expert
   invisible malgre capability activee).

### Valide (preuves terrain, par l'user)
- Parcours guide COMPLET jusqu'a "Le nouvel expert est actif" : agent `DeliverySnapshot_expert`
  (`agent:PFf8xkij`) AUTO-CREE (sonde confirmee -> injection directe, generated/ vide = normal),
  tool `DM4Yx4A`, capability activee.
- Apres re-collage des 3 agents v1.3 : l'orchestrateur liste et route le domaine delivery,
  repond correctement aux questions delivery ("ca a marche").
- Reprise apres reload + apres restart backend : constatee en reel (etat SQL).
- Suites : 676 agents+factory / 965 backend / 343 LAB / 365 front.

### Decisions
- PUSH actifs desormais sur demande explicite de l'user (il pull depuis GitHub vers DSS) ;
  les regles memoire "jamais de push" restent la valeur par defaut hors demande.
- Le wizard respecte la connexion LLM admin (aucun override de sampling, jamais).

### Prochaines etapes
- Smoke metier delivery (Evidence sur la bonne table, non-regression revenus/tickets) +
  curation du profil si besoin (overrides, prompt doctor).
- Suppression des objets opportunities par l'user.
- Notebook 01 a etendre (template + run_settings + workflow) ; gates securite phase 0.
