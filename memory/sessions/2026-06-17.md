# Session 2026-06-17 - Run 6 : modèles par mode revus + argent €/transparence + désambiguïsation déférée + source Evidence cliquable + nettoyage prod

## Objectif
Suite au test DSS du Run 5 : (1) abandonner gpt-5.4-mini en Éco (cher car il « réfléchit », résultats faibles)
et changer les modèles par mode ; (2) corriger le rendu argent (€ + mise en forme) et la transparence sur le
périmètre filtré ; (3) régler la désambiguïsation « Roaming Hub » (le sous-agent interroge au lieu de déférer) ;
(4) retirer le bloc « Sources » du chat ; (5) rendre la source Evidence cliquable vers le dataset Dataiku ;
(6) nettoyer le code des agents pour la prod (zéro empreinte IA, commentaires simples, pas d'« à vérifier »).

## Fait
- **Modèles par mode (L080)** : `GEMINI_FLASH_LITE_ID = …/gemini-3.1-flash-light` (eco, **défaut**),
  `GEMINI_FLASH_ID = …/gemini-3.5-flash` (medium), `SONNET_ID` (high). `gpt-5.4-mini`/`GPT_MINI_ID`
  **supprimés** des 2 agents. `DEFAULT_MODE="eco"` (orchestrateur + sous-agent) ; frontend
  `MODELMODE_DEFAULT='eco'` + badge « Recommandé » déplacé sur Éco + i18n FR/EN (`mode.*`).
- **Argent € (L080)** : helper `metric_unit(metric)` dérive la devise du **nom de colonne**
  (`amount_eur → €`, `_usd → $`…) - plus aucune config de profil requise. Utilisé dans `build_sql`,
  le moteur sémantique (`n_query`) et `build_scope_note`. Prompt orchestrateur (PERSONA `# MONEY,
  NUMBERS & TRANSPARENCY`) impose : `€` + séparateurs de milliers + analyse rédigée.
- **Transparence périmètre (L080)** : `build_scope_note(u, profile, filters, lang)` → le sous-agent
  préfixe sa réponse d'une ligne `[Périmètre]/[Scope]` (scénario + « par défaut », période + « aucun
  filtre d'année », filtres d'entité, devise). L'orchestrateur la restitue en prose (`_subagent_tool_output`).
- **Désambiguïsation (L081)** : `defer_multicolumn_offer_terms(resolutions)` - un terme ambigu sur
  **≥2 colonnes distinctes** passe en statut `deferred` (plus de clarification) ; `n_resolve` le propage
  via `u["offer_terms_for_model"]` ; `build_semantic_question` émet un bloc `AMBIGUOUS OFFER TERM (no
  confident match)` (terme brut → Sonnet tranche, « NEVER default to sirano_product ») ;
  `build_disclosure_notes` divulgue. Mono-colonne (2 clients) demande encore.
- **Bloc « Sources » retiré** du chat (orchestrateur `node_finish` ; fonction `sources_block` +
  constantes supprimées). La source vit dans Evidence.
- **Source Evidence cliquable (L082)** : champ registre `source_url` (capability `revenue_expert`,
  vide). Tamponné sur chaque item SQL (`_find_generated_sql`) → propagé **additif** dans le pipeline
  de capture (`streaming._normalized_sql_event` → `stream_manager` map `sourceUrl/source_url` →
  `capture._CORE_ITEM_KEYS`+cap de tag) → `service.py` (`source_url_for_run`, `with_source_urls`)
  attache `url` à `meta.source`/`sources` → `EvidenceSources.vue` rend un `<a target="_blank">`
  (orange AA), i18n `ev.proof.sources.open`.
- **Nettoyage prod** : commentaires des 2 agents réécrits en anglais simple ; retirés : dates, refs
  session/leçons (L0xx/ORCH-07), « VERIFY/ACTION REQUIRED/<--/Flip after DSS test », « root cause /
  proven / seen live / user decision / the #1 / ported / validated in DSS », ⚠️ dans les commentaires
  (gardés dans les **chaînes utilisateur**). Exemple de date dans le prompt UNDERSTAND = fonctionnel, gardé.
- **Tests** : MAJ (model ids, défaut eco, retrait `TestSourcesBlock`/`GPT_MINI_ID`) + ajouts
  (`defer_multicolumn_offer_terms`, `build_scope_note`, `metric_unit`, offer-terms dans semantic_question/
  disclosure, `source_url` stamping, `source_url_for_run`/`with_source_urls`). **227 agents + 384 backend
  + 116 frontend verts**, py_compile OK.
- **Revue adversariale** (Workflow : 4 dimensions × 2 sceptiques, 26 agents) → **0 bug de code**,
  uniquement 3 commentaires périmés (corrigés par le nettoyage).
- **Build + zip** : `npm run build` → bundle **`index-8spQsYzC.js`** + CSS `index-CTHQ_2Yr.css` ;
  `body.html` recâblé via **Write** (le `cp` reste refusé par les permissions, cf. F10) ; zip
  `ready-for-dataiku/owismind-upload.zip` = **77 entrées**, propre, fichiers requis OK, python-lib à jour.

## Décisions
- **Défaut = Éco** (Gemini 3.1 Flash-Lite) - choix user (« mode éco par défaut »). Badge « Recommandé » sur Éco.
- **Désambiguïsation = déférer au modèle sémantique** (choix user) plutôt que pin déterministe sur Product
  (le pin réintroduirait le piège budget=0 quand le terme n'est QUE Solution, ex. « Roaming Hub »).
  Règle P3-clean : décision par **nombre de colonnes**, jamais de colonne en dur.
- **€ dérivé du nom de colonne `amount_eur`** (exigence user : pas de métrique de profil « amount euro »
  à configurer ; la donnée porte déjà la devise).
- **URL source dans l'orchestrateur** (choix user) malgré le fait qu'Evidence soit construit côté backend
  → on fait voyager l'URL via les items SQL capturés (additif, contrats gelés respectés). Lien attaché au
  cas **mono-source** uniquement (pas de mapping par-dataset multi-source - dégradation propre).
- Narration toujours OFF en Éco (act-first + ticker déterministe) - révisable après test DSS.

## Validé / non validé
- ✅ Local : 227 + 384 + 116 tests, build Vite, py_compile, zip propre, revue adversariale (0 bug).
- ⏳ **NON validé DSS** : qualité Flash-Lite en Éco, restitution du périmètre, désambiguïsation réelle,
  et le **lien cliquable réel** (dépend de `source_url` rempli + URL Dataiku valide).

## Prochaines étapes (DSS)
1. **Recoller les 2 Code Agents** (env 3.11) : `OWIsMind_orchestrator` + `SalesDrive_revenue_expert`.
2. **Upload zip** (77 entrées, `index-8spQsYzC.js`) → ⚠️ **backend python-lib changé** (Evidence/source) →
   **REDÉMARRER le backend**.
3. **Vérifier `GEMINI_FLASH_LITE_ID`** (best-effort `…/gemini-3.1-flash-light` ; si Mesh = `flash-lite`, corriger).
4. **Remplir `source_url`** dans la capability `revenue_expert` (orchestrateur) avec le lien Dataiku du dataset
   avant de recoller, pour activer le lien cliquable.
5. Smoke-tests : (a) « revenu réel du compte X » en Éco → réponse en € + ligne périmètre restituée ;
   (b) « budget 2026 Roaming Hub … » → ne demande PAS, Sonnet résout + divulgue ; (c) source Evidence cliquable.

---

## Run 7 - Polish UI chat : largeur du texte, mode picker, titres de conversation, prompt bar

### Objectif
Retour user sur l'UI du chat : (1) le texte des réponses est plus étroit (760px) que la barre de prompt
(920px) → désaligné ; vouloir ~90% de largeur, aligné bord-à-bord comme ChatGPT/Claude ; (2) point du mode
Éco gris = moche, autre couleur ; (3) pop-up de mode « se voit trop généré par IA » → caler le style webapp ;
(4) noms de conversations « sans nom / sans limite » → titre court = début du prompt tronqué (DB évoquée pour
le futur titre IA). Puis 2e vague : (5) barre de prompt trop large quand seule sur l'écran d'accueil ;
(6) guidage « soyez précis » (placeholder + écran d'accueil) ; (7) nom d'agent tronqué → plus d'espace ;
(8) micro à droite à côté d'Envoyer.

### Fait (frontend + 1 point backend ; agents NON touchés)
- **Largeur partagée** : token `--chat-col: 90%` / `--chat-col-max: 1200px` (`tokens.css`). `.conv-inner`
  (760→token), `.prompt-wrap` (920→token), `.empty` (920→token) utilisent la **même mesure** + même padding
  `--s-7` → le texte s'aligne bord-à-bord avec la zone de saisie. Empty stage : `.prompt-wrap.in-empty`
  plafonné à **760px** (la barre seule paraissait surdimensionnée).
- **Mode picker (`ModelModePicker.vue`)** : échelle « feu tricolore » - **Éco vert** (`--success`), Medium
  orange, High rouge (`--danger`) ; dot + jauge + teinte de carte sélectionnée keyés par `lvl-` + COST_LEVEL.
  Pop-up dé-IA : icône `sparkles`→`sliders` ; badge texte « Mode actuel »→**coche** (Icon `check`) ; note
  enveloppe : ⚠️ emoji retiré → **icône `wallet`** ; copie FR/EN réécrite (ton produit sobre) ; clé morte
  `mode.current` supprimée (fr+en).
- **Teintes thème-aware** : tokens `--success-soft` / `--danger-soft` (clair + sombre) dans `tokens.css`
  (comme `--orange-soft-dark`) - remplacent des rgba codées en dur qui étaient **invisibles en dark mode**
  (bug remonté par la 1re revue adversariale).
- **Guidage « soyez précis »** : `prompt.placeholder` surchargé dans `extra.js` (FR/EN) + nouveau `empty.tip`
  rendu dans `ChatEmpty.vue` (encart sobre + icône `info`). `messages.json` resté **pristine** (override via
  merge `extra.js`).
- **`AgentPicker.vue`** : `max-width` 240→**320px** (noms longs type « OWIsMind_orchestrator » sans coupe ;
  ellipse = filet).
- **`PromptBar.vue`** : **micro déplacé** de `.prompt-left` vers `.prompt-right` (avant Envoyer).
- **`Modal.vue`** : `aria-label="Fermer"` (en dur FR) → `:aria-label="t('x.close')"` (+ `useI18n`, clé
  `x.close` FR/EN) - corrige tous les modals (bug a11y pré-existant remonté en revue).
- **Backend titre (`sql_builders.py`)** : `build_conversation_list_query` nettoie le 1er message -
  `LEFT(BTRIM(regexp_replace(...,'[[:space:]]+',' ','g')), {tlen})` (une ligne propre) ; `CONV_TITLE_MAXLEN`
  140→**56** (`chat_v5.py`). **Dérivé** du message déjà stocké → rétroactif, **sans migration**. Test ajouté.
- **Build + zip** : frontend rebuildé, `body.html` recâblé via Write (cp refusé, F10/L033), zip repackagé.

### Décisions
- **Titre = dérivé, pas de colonne DB maintenant** : la troncature « pour le moment » est fonctionnellement
  identique au dérivé (LEFT du 1er message), rétroactive et sans risque instance. La **colonne `title` en base
  prendra son sens avec la feature titre-résumé-IA** (stockage/édition). Proposé à l'user → il garde le dérivé.
- **Vert pour Éco** autorisé : la règle « pas de vert » ne concerne QUE l'Evidence Studio, pas le mode picker.
- **Modal i18n** corrigé bien que pré-existant : c'est le bouton de fermeture du pop-up justement poli, et le
  fix profite à toute l'app (risque nul, attribut a11y).
- **2 revues adversariales** (Workflow) : R1 sur le 1er lot → 4 défauts (dont dark-mode invisible) **tous
  corrigés** ; R2 qualité **+ sécurité** sur tout le diff de session → **0 défaut, 0 finding sécurité**.

### Validé / non validé
- ✅ Local : **116 tests frontend + 385 backend** (+1 ajouté), build Vite, zip propre (77 entrées,
  `index-CrvKHGTt.js`), 2 revues adversariales (R2 = 0 défaut / 0 sécurité).
- ⏳ **NON validé DSS** : rendu réel des largeurs (90%/760px), couleurs en clair/sombre sur l'instance,
  troncature des titres sur vraies données.

### Prochaines étapes (DSS)
1. **Upload zip** `ready-for-dataiku/owismind-upload.zip` (77 entrées, `index-CrvKHGTt.js`).
2. ⚠️ **REDÉMARRER le backend** - `python-lib` changé (titres : `sql_builders.py` + `chat_v5.py`).
3. **Pas de recoll des Code Agents** (agents non touchés ce tour).
4. Smoke-tests UI : alignement texte↔prompt (~90%), barre de prompt étroite sur écran vide, point Éco vert,
   pop-up de mode (icônes/coche/copie), conseil « soyez précis », nom d'agent long, micro à droite, titres
   courts/propres dans la sidebar, fermeture de modal localisée (EN).

---

## Run 7b - Bannissement du tiret cadratin + refonte pop-up de mode (mood DSS) + écran d'agent rempli

### Objectif
Retour user : (1) le tiret cadratin `—` est une signature d'IA -> l'interdire PARTOUT et pour toujours
(repo + sessions) ; (2) le pop-up de mode "se voit trop comme généré par IA" -> reprendre le style sobre
de la boîte DSS "Paramètres de la conversation" (liste + détail, dots de coût), charte Orange, quelques
couleurs/effets ; mettre Éco en avant comme LA recommandation (best perfs + bonne qualité) ; (3) l'écran
de description d'agent est vide alors qu'il ne devrait pas l'être, et à styler charte OWIsMind/Orange.

### Fait
- **Ban `—`/`–` (règle NON NÉGOCIABLE #9, CLAUDE.md + mémoire feedback)** : balayage byte-safe de tout le
  contenu source (frontend src, python-lib, tests, memory, docs, cadrage, dataiku-agents, CLAUDE.md) ->
  `—`/`–` remplacés par `-`. Hors générés (resource/owismind-app, ready-for-dataiku, body.html), vendored,
  .claude/. Vérifs : 0 corruption U+FFFD, tous .py compilent, tokens spéciaux intacts (`⟦owi:mode⟧`, `·`,
  `…`, `→`), 116 front + 385 back + 227 agents verts, build OK, 0 `—`/`–` restant en i18n/.vue.
- **Pop-up de mode refondu (`ModelModePicker.vue`)** : 2 volets facon DSS - liste des 3 modes à gauche
  (Éco/Medium/High, ligne sélectionnée = **gris + barre Orange**, pas de teinte orange), panneau détail à
  droite avec 2 jauges 5-dots **Coût** + **Vitesse** (Éco 1/5 coût, 5/5 vitesse ; Medium 3/3 ; High 5/2).
  Sobre, bordures fines, dots de coût SOMBRES, charte Orange 80/20. **Interaction = clic-pour-sélectionner
  + footer Annuler/Valider** (pattern DSS), PAS de hover : le 1er jet avec `preview` au survol VIBRAIT (le
  détail change de hauteur -> modale recentrée -> ligne survolée bouge -> boucle mouseenter/leave). Fix =
  clic seul + `min-height` détail. Éco mis en avant : badge "Recommandé" + ligne `mode.reco_line`. Copie
  FR/EN réécrite (sans tiret), nouvelles clés `mode.cost_label/speed_label/reco_line/cancel/validate/*_speed`.
- **Écran d'agent rempli (`agentMeta.js`)** : `resolveAgentMeta` ajoute un repli **substring sur la plus
  longue clé du registre (>=4 char)** -> le label décoré "Agent - OWIsMind_orchestrator" résout enfin la
  carte `owismind` (tagline, desc, capacités, tools, accents Orange) au lieu d'une fiche générique vide.
  Match exact prioritaire ; longest-first pour éviter qu'une clé courte gagne.
- **Build + zip** refaits : `index-BxmN4Txj.js`, zip propre (77 entrées). `body.html` recâblé via Write.

### Décisions
- Ban appliqué au **contenu** (source/docs/mémoire) ; exclus : générés (rebuild), vendored, `.claude/`
  (outillage). Les fichiers qui DÉFINISSENT la règle (CLAUDE.md #9, mémoire `no-em-dash-ever`) citent
  forcément `—`/`–` entre backticks - exception assumée. Hook pre-commit anti-`—` = proposé, non installé.
- Pop-up : "un peu dans le mood" DSS, pas un clone ; clic = applique + ferme (pas de bouton Valider).
- Sweep **byte-safe** obligatoire (`LC_ALL=C` / sed octet) : `perl -CSD` plante sur les fichiers qui
  portent d'autres glyphes multioctets (agents : `⟦⟧ ⊥ ⇒`). Empiriquement aucune corruption ici.

### Validé / non validé
- Local : 116 front + 385 back + 227 agents, build Vite, zip propre. Pop-up et écran d'agent = rendu
  visuel **NON validé DSS** (intestable hors navigateur/instance).

### Prochaines étapes (DSS)
1. Upload zip (77 entrées, `index-BxmN4Txj.js`) + REDÉMARRER backend (python-lib titres, inchangé ce
   sous-run mais changé dans la session).
2. Smoke-tests : pop-up de mode (sobre, Éco recommandé, jauges coût/vitesse), écran "Agents" -> détail de
   l'orchestrateur **rempli**, et vérifier visuellement zéro `—` à l'écran.
