# Session 2026-07-12

## Run 1 (session de nuit, autonome) : durcissement adversarial de l'Agent Factory v1.3

> Session de nuit demarree 3h11, user endormi, mandat autonome. Branche
> `OWIsMind_PRD_V1_3-dev-night-20260712` creee depuis `OWIsMind_PRD_V1_3-dev`.
> Checkpoint memoire de MI-SESSION : la session continue apres ce log (reprise Codex
> programmee 8h21). AUCUN commit de cloture ici (il viendra a la fermeture).

### Objectif
Soumettre l'Agent Factory v1.3 (livree Run 2 du 2026-07-10) a un cycle multi-agents
d'audit adversarial + durcissement des modes de defaillance, sans rien executer contre
l'instance DSS.

### Fait (4 vagues multi-agents)
- **Vague 1 - audit (6 agents)** : Codex Terra (revue de code : 1 P0 + 6 P1 + 3 P2),
  Codex Sol (critique de design, top 5), Codex Luna (sweep mecanique), Opus (revue console :
  1 P1 + 4 P2), Opus (idees produit), Sonnet (analyse des gaps).
- **Vague 1 - fixes** : durcissement des failure-modes de la factory (`ExistenceCheckError`,
  post-import verify, `scenario_trigger` separe, etat `BLOCKED`, capability sans placeholder,
  `apply_config` explicit-empty, hub `RLock`, doctor tri par timestamp, garde delete probes) ;
  console (invalidation du plan, poll retry, path traversal, gates hub-only, eviction des jobs).
- **Vague 2** : wizard trap-shapes + `validate_golden_queries` branchee dans le pipeline ;
  `wizard_config` transmis au plan/execute et persiste `/owismind_hub/wizard/<domain>-config.json` ;
  validateurs anti `agent:FILL_ME` cote factory ET orchestrateur ; labels FR de `registry.py`
  re-accentues.
- **Vague 3** : re-audit Opus (tout PASS sauf `tool_exists`/`agent_exists`, corriges) ;
  `spec.preflight` + `runbook_markdown` exposes a la console ; couverture builders (28 tests).
- **Vague 4** : docs synchronisees + revue Charte Orange (PASS).

### Qualite
- Suite factory **379 -> 464 tests verts**. Plugin **830** et LAB **343** inchanges, verts.
- 6 commits sur la branche de nuit : `1f06b90` (vague 1), `6cb9a61` (vague 2), `241696a`
  (vague 3), `fcab4d6` (tests builders), `ec13f76` (vague 4 docs). NON commite au checkpoint.

### Incidents / lecons
- **L152** : un agent delegue (subagent plugin Codex, background) ne remonte pas toujours
  son rapport -> exiger `SendMessage to: "main"` + garde-fou cron de polling + aller lire
  les fichiers de sortie / l'etat du processus. L'user avait prevenu.
- **L153** : sous-agent en sandbox harness -> `Write` hors repo (scratchpad) peut etre
  bloque ; faire remonter le contenu par message.
- **L154** : rate limit GPT partage entre sessions Claude paralleles (une session SaiGet
  consommait le meme quota, saut ~3h35, reset 8h14) ; durcir un `exists()` partage sans
  casser la tolerance dry-run (regression attrapee par `test_factory_core`).

### En attente / reprise
- Codex a atteint sa limite d'usage ~3h35 (reset 8h14). Le **regenerateur de seeds hub**
  (idee retenue) est EN ATTENTE cote Codex ; reprise programmee 8h21 (cron one-shot) ;
  prompt sauvegarde dans le scratchpad de session.

### Prochaines etapes
1. Merger la branche de nuit dans `OWIsMind_PRD_V1_3-dev` apres review user.
2. Lancer le regenerateur de seeds hub via Codex.
3. Backlog differe (propositions Sol, a discuter avec l'user) : machine d'etat persistante
   par domaine ; environnement de validation pre-live ; manifeste canonique versionne du
   registre ; controle operationnel.
