# Session 2026-06-10 → 2026-06-11 — Evidence Studio v2 « Trust layer » (mission complète)

## Objectif
Mission « repenser et implémenter Evidence Studio » : transformer le panneau preuves en couche de
confiance GÉNÉRIQUE pour commerciaux non techniques (sources, périmètre, filtres, calcul expliqué en
langage métier, résultat exact utilisé par l'agent, niveau de vérification honnête, drill-down vers les
lignes contributrices), + audit et durcissement de l'orchestrateur Code Agent v2.1 → v2.2.

## Fait
- **Contrat gelé** : `docs/superpowers/specs/2026-06-10-evidence-trust-layer-design.md` (modèle stocké,
  meta enrichie, niveaux, drill, protocole événements, frontières de fichiers) + amendements post-revue.
- **Backend** (python-lib/owismind/) :
  - `evidence/sql_explain.py` (NOUVEAU, pur, ~700 l.) — explication structurée générique : scanner de
    scopes enrichi, pré-passe WITH, classifieur d'expressions (agg/window/case/ratio/share/percent/diff),
    lineage identité des clés GROUP BY (nom SOURCE), steps `{kind, params}` (enum gelé ~40 kinds),
    flags `where_complete/single_source/select_understood/calc_resolved`, never-raises.
  - `evidence/capture.py` (NOUVEAU, pur) — extraction opportuniste du résultat exact depuis les outputs
    du tool (clés candidates rows/records/data/result_rows/values), caps miroir (200 lignes × 50 col,
    cellule 256c, result ≤100k, sql/item ≤20k + flag `sql_truncated`, 20 items, budget global 262 144
    avec drop newest-wins en dernier recours).
  - `evidence/service.py` — meta enrichie (source/queries/verification/explanation/result/drilldown),
    niveaux déterministes (`declared→source_identified→scope_partial→scope_exact→calc_decomposed` +
    `result_captured` orthogonal), drill re-validé serveur (group keys re-dérivées du SQL stocké,
    refus >8 clés), `SET LOCAL transaction_read_only`, TTL-cache `read_schema`.
  - `agents/streaming.py` — pass-through whitelisté eventData (label/stepIndex/stepCount/agentKey/status),
    émission mid-stream des generated_sql relayés par AGENT_DONE (un run stoppé garde son SQL), fusion
    footer-trace ONE-SHOT via pop (chemin sans orchestrateur byte-identique au validé DSS).
  - `agents/stream_manager.py` — sql_list enrichi (sql_id/step_index/agent_key/result), copie polée légère.
  - `storage/chat_v4.py` — cap JSON au point d'écriture (jamais `_bounded()` sur du JSON), projection
    de `result` HORS du readback `/conversation`.
  - `security/validation.py` — clé `drill` (≤8, valeurs bornées y compris entiers géants), `api/routes.py` câblé.
- **Orchestrateur v2.2** (`orchestrator/orchestrator_agent.py` + `AUDIT.md` + `README.md` + tests stub) :
  ORCH-01..11 + ORCHV22-02 — tagging sql_id/step_index/agent_key + capture result capée, agentId/str(e)/URL
  intranet retirés des eventData/texte, depth-guards, `_is_footer` partagé, steps purgés non-BUSINESS,
  PLAN_READY tronqué, marqueur de troncature synthèse, greet flush, labels métier sources, append_trace gardé.
- **Frontend** : `EvidenceTrust/Sources/Calc/Result.vue` (NOUVEAUX) + `EvidencePanel` réorchestré
  (badge → sources → chips → calcul → résultat+drill → bandeau drill → exploration → SQL replié),
  `composables/evidenceProof.js` (pur), store `evidence.js` (drill snapshot/restore, re-drill préserve le
  snapshot), `evidenceModel.js` (buildRowsPayload+drill, buildDrillLabels abort >8), i18n `ev.proof.*`/
  `ev.exp.*` fr+en, timeline labels backend prioritaires (`timelineSteps`/`timelineModel`/`MessageAgent`).
- **Docs** : `docs/evidence-trust-layer.md` (architecture, cycle de vie, niveaux, limites, extension,
  tests), `docs/backend-api.md` rafraîchie, spec amendée.
- **Build/package** : `/build-plugin` (entrée `index-DF9WrJFi.js`, body.html identique) +
  `/package-plugin` → zip propre **74 entrées** (sql_explain/capture présents).

## Méthode
Exploration (workflow 4 spécialistes) → contrat gelé → implémentation (workflow 6 chantiers à frontières
disjointes ; sql_explain repris EN DIRECT après 2 morts d'agents à 64k output tokens) → revue adversariale
(workflow 26 agents, 6 lentilles + vérificateurs sceptiques) : 20 findings bruts → **17 confirmés → 17
corrigés** (dont FP-01/FP-02 high : where_complete divergent du fragment réellement appliqué ; self-join
via CTE passant single_source) → re-tests complets.

## Validé / non validé
- ✅ LOCAL : **304 unittest** backend + **59 unittest** orchestrateur (stub dataiku) + **97 node:test**
  front ; `compileall` + syntaxe py3.9 (`ast feature_version`) ; `vite build` ; zip 74 entrées propre ;
  validation visuelle Chrome DevTools light+dark (états complet/partiel/drill — captures
  `.demo-screens/trust-01..07`), console 0 erreur/warning (i18n complète).
- ⏳ NON validé DSS : tout le trust layer (upload + **REDÉMARRER le backend** + coller l'orchestrateur
  v2.2 dans le Code Agent). La clé exacte des LIGNES dans les outputs du tool semantic-model-query n'est
  PAS confirmée sur l'instance → vérifier sur une trace réelle du dataset traces ; sinon
  `result_captured:false` partout (dégradation honnête prévue).
- Reportés de Run 4 : L040 (New conversation) / L041 (chrono) toujours à re-tester DSS.

## Prochaines étapes
1. Upload zip + restart backend + coller orchestrateur v2.2 ; rejouer la démo (top-10, 80 %…) et vérifier
   badge/calcul/résultat/drill + timeline labels FR.
2. Inspecter une trace réelle : la clé des rows dans outputs (ajouter à `_ROW_KEYS`/orchestrateur si différente).
3. `git init` (toujours en attente) ; 2ᵉ task du 2026-06-09 à clarifier.

## Retour utilisateur (fin de session)
« **Ça marche bien mais pas encore comme je veux.** » — le trust layer fonctionne après déploiement
(pas de crash, panneau opérationnel), mais le comportement/rendu ne correspond pas encore aux attentes
de l'utilisateur. **Les ajustements précis n'ont pas été détaillés** — à recueillir en début de
prochaine session (demander : qu'est-ce qui ne correspond pas ? badge ? wording des étapes ? résultat
capturé ? drill ? layout ?) avant de toucher au code.

---

# Run 2 — Grand nettoyage, knowledge graph, git init, fraîcheur auto

## Objectif
Repo « ultra clean » : purger l'obsolète, remettre docs/mémoire en cohérence, piloter **graphify**
(réduction des tokens de navigation), initialiser **git** et câbler la mise à jour automatique du graphe.

## Fait
- **Purge** (suppression directe, décision user) : `maquette/` (~12 k lignes + docs de transmission),
  `docs/superpowers/plans/` (4 plans ~7,3 k lignes), `.demo-screens/` (7,9 Mo), `.DS_Store`, tous les
  `__pycache__` (source + staging). Specs gelées conservées (`docs/superpowers/specs/`).
- **Refs recousues** (18 retouches) : CLAUDE.md ×2, docs/README+architecture+frontend (plages F1–F21 /
  L045), PROJECT_STATE (§9 réécrit « maquette supprimée », en-tête, §2b, §13), CONTEXT, note
  « chemins historiques » en tête de LESSONS.
- **Knowledge graph** (pilote concluant) : corpus filtré 161 fichiers (~173 k mots) → AST 1 377 nœuds +
  9 sous-agents sémantiques (~1,4 M tokens) → 1 969 nœuds / 3 443 arêtes / 156 communautés (30 nommées).
  Bench : **18,4× moins de tokens/requête**. Sorties `graphify-out/` (git-ignoré).
- **Purge pilotée par le graphe** : requête « qui cite les docs maquette supprimés ? » → 6 fichiers
  (graphe) + grep d'exhaustivité → **13 fichiers source** purgés des citations mortes (docs/0X §,
  assets-v5) en 19 retouches ; provenance et refs vivantes (Lxxx) conservées. Vérifié : vite build OK
  (bundles inchangés → rien à redéployer), **97/97 node:test**, greps vides. Réponse re-sauvée dans le
  graphe (`save-result`).
- **git init** (main) : `3bd804f` (initial, 211 fichiers / 31 457 lignes) → `3de1b51` (câblage) →
  `60bdbcf` (.graphifyignore). `resource/owismind-app/` trackée (NO-INSTALL) ; ready-for-dataiku /
  graphify-out / node_modules ignorés.
- **Fraîcheur du graphe (3 couches)** : hooks git `post-commit`/`post-checkout` (rebuild AST détaché,
  sans LLM, log `~/.cache/graphify-rebuild.log`) ; `/log-session` étendu (étapes 6-7 : `/graphify
  --update` + commit de session) ; session-start.sh + CLAUDE.md → « interroger le graphe d'abord »
  (sessions ET sous-agents).
- **Incident corrigé en test réel** : 1er post-commit → graphe pollué (3 251 nœuds : le hook rescanne
  TOUT, bundles minifiés réingérés) → **`.graphifyignore`** natif versionné + purge des nœuds pollués +
  `_rebuild_code(force=True)` → **2 494 nœuds / 3 931 arêtes**, stables au commit suivant (re-testé).

## Décisions
- Suppression directe sans filet (la maquette originale survit hors repo : `~/projects/maquette_owismind/`).
- Plans supprimés / specs gelées conservées ; screenshots de validation supprimés (régénérables).
- Commentaires code : chemins/sections morts purgés, prose de provenance gardée.
- Sourcegraph (plugin officiel) : sans objet pour ce repo local non indexé — utile plus tard pour du code public.
- **Autorisations permanentes user (2026-06-11)** : `/log-session` met à jour le graphe ET committe ;
  **jamais de push** (l'user pushe).

## Validé / non validé
- ✅ Zip déployable intact (74 entrées, 0 pycache) · 97/97 node:test · vite build · hook stable 2×.
- ✅ Pilote graphify : découverte = graphe / exhaustivité = grep (6/13 fichiers trouvés par le graphe seul).
- ❌ Rien de nouveau validé en DSS (aucun upload ; bundles inchangés).

## Prochaines étapes
Inchangées : 1) recueillir les ajustements trust layer (prio 1), 2) clé des ROWS sur trace réelle,
3) re-tester L040/L041 en DSS, 4) Evidence v3 (différé), 5) 2ᵉ task user à clarifier. + (user) remote git.

# Run 3 — SalesDrive v2 : portage du sous-agent visuel en Code Agent (✅ validé DSS par l'user)

## Objectif
Porter le sous-agent visuel SalesDrive (`agent:rNTZ781a`, flow resolve→routing→semantic→format) en
**Code Agent déterministe** travaillant en collaboration étroite avec l'orchestrateur, pour éliminer
les dérives d'interprétation (semantic_question rédigée par LLM) et la maintenance des blocs visuels.

## Fait
- **`salesdrive/salesdrive_agent.py`** (nouveau, standalone stdlib+dataiku, à coller dans le Code
  Agent DSS `agent:MODpGFcC`) : pipeline UNDERSTAND (1 appel LLM JSON strict : scope/langue/intent
  enum gelé/phases/période/group_by/top_n/termes) → RESOLVE (tool `aNxeOc4` resolver, routing Python
  pur) → COMPOSE (semantic_question par **templates gelés**, jamais LLM) → QUERY (tool `v4oqA6R`
  semantic-model-query via `get_agent_tool().run()`, **SQL + rows capturés depuis la valeur de
  retour**, clé d'entrée auto-détectée du descriptor) → RENDER (table markdown + montants par code ;
  accroche LLM **vérifiée** : tout nombre cité doit exister dans les rows, sinon fallback déterministe).
- **Politique de désambiguïsation générique** (incident boucle IPL, diagnostiqué sur traces CSV
  réelles) : `parse_qualified_term` (« VALEUR (Colonne) » → filtre direct), préférence valeur exacte
  stricte (neutralise les collisions du `norm()` catalogue : « IPL + » → « ipl »), auto-pick par
  priorité de colonne (Product>Solution>SolutionLine>sirano_product) quand valeur unique ; hint
  d'écho parseable dans la clarification.
- **Continuité conversationnelle** (couche intelligente) : orchestrateur v2.3 transmet le message
  assistant précédent + réponse brute (`pass_context`, `build_subagent_context`) ; l'UNDERSTAND
  mappe toute formulation (« le produit », « la première ») sur **un candidat de la liste uniquement**.
- **Orchestrateur v2.2 → v2.3** : entrée registre `salesdrive_v2` (pass_context, labels réutilisés),
  capture `AGENT_RESULT` {status,intent,resolvedFilters,sqlCount,rowCount} (jamais relayé, exposé
  dans AGENT_DONE.agentResult), skip du bloc Sources sur need_clarification/out_of_scope.
- **Tests** : `salesdrive/tests/` 55 unittest (validation, templates, politique, extraction payload,
  formats, gate headline, continuité) ; orchestrateur 62 (3 ajoutés build_subagent_context) ; front 97 intact.
- `salesdrive/README.md` (déploiement, contrat, politique) ; CSV de traces analysé puis laissé
  en local (`salesdrive/webapp_devtest-…csv`, non destiné au repo).

## Décisions
- Approche A « plan structuré + composition déterministe » (vs portage 1:1 / SQL direct) — le LLM ne
  fait QUE le linguistique, le code compose tout le SQL-bound. Déploiement **en parallèle** (visual
  v1 intact, bascule = 2 flags `enabled`). Formatting **hybride vérifié**. **Catalogue conservé**
  (pas de recherche dans les 175k lignes : lent, charge instance, ne résout pas l'ambiguïté).
- Anti-« règles par bug » : 3 étages — comprendre (LLM+contexte), décider sans déranger (politiques
  génériques), refuser plutôt qu'halluciner. Aucune valeur métier en dur dans la logique.

## Validé / non validé
- ✅ DSS (traces réelles + retour user « super tout marche ») : chaîne complète tour 1 (resolver
  case-fix `Product='Roaming Sponsor'`, input_key `question`, sql_count=1, row_count=10, rows
  capturées dans le span `semantic-model-query`, headline verified=true) ; boucle IPL corrigée
  après re-collage des 2 fichiers.
- shape_keys réels du tool sémantique : `artifacts, output, parts, sources, toolValidationRequests,
  trace` → la question « clé des ROWS » de la mémoire est **résolue** (capture depuis le retour du
  tool, plus de devinette de trace).
- ⏳ Non re-testé : multi-étapes (agent+tool dans un même plan) avec v2 ; cas « IPL + » (vraie
  ambiguïté de valeur) en réel.

## Prochaines étapes
1) Recueillir les ajustements trust layer (toujours prio 1, non couvert ce run). 2) Bascule
définitive v2 (retirer l'entrée visual `salesdrive` quand confiance OK) + supprimer le CSV de traces
local. 3) Re-tester L040/L041 en DSS. 4) Evidence v3 (différé). 5) 2ᵉ task user (2026-06-09) à clarifier.
