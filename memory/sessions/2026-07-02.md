# Session 2026-07-02 - Audit + durcissement des 2 agents (orchestrateur + expert revenus + cerveau semantique)

## Objectif
Auditer et ameliorer les deux agents OWIsMind (orchestrateur + SalesDrive_revenue_expert, incluant
le modele semantique et le tool attribute_lookup) : fiabilite, routage, comprehension des questions,
moins d'hallucinations, meilleurs filtres/SQL/tools, transparence pour l'utilisateur final.
Contrainte : architecture existante respectee, pas de refactor hors scope, implementation directe.
Mode ultracode (workflows multi-agents), DEV uniquement (PROD intacte, promotion apres validation DSS).

## Methode
1. **Audit workflow** : 6 auditeurs Sonnet (routing orchestrateur / UNDERSTAND-RESOLVE /
   COMPOSE-QUERY-RENDER / transparence / cerveau semantique + lookup / coherence cross-agents)
   + 1 verificateur adversarial PAR finding -> 26 findings, **20 confirmes, 6 refutes**.
2. **Patches** : 2 sous-agents Opus (1 par fichier agent, listes de fixes precises, zones de tests
   disjointes), moi sur cerveau semantique + lookup + integration.
3. **Revue adversariale finale** du diff integre (3 lentilles : regression / contrats geles /
   coherence des prompts) -> 4 defauts, tous traites (dont 1 revert structurel, voir Decisions).

## Fait (tout en DEV, `dataiku-agents/OWISMIND/OWISMIND_DEV/`)
**Orchestrateur (`OWISMIND_DEV_OWIsMind_orchestrator.py`)** :
- node_finish : strip inconditionnel des tables markdown quand un artefact est rendu -> une reponse
  100% table tombe sur le fallback honnete "les donnees sont dans le panneau" (avant : livree brute).
- `_run_subagents` : le drain attend TOUS les workers (le `with ThreadPoolExecutor` bloquait deja en
  shutdown(wait=True), le break au deadline ne raccourcissait rien : il fabriquait des faux
  "specialist temporarily unavailable" pour des resultats pourtant calcules). + filet de vivacite
  try/finally dans worker() (un worker mort sans son put final aurait bloque le tour a l'infini).
- BUSINESS_DOMAINS `billing` reformule "documents de facturation (factures, lignes de facture)" :
  ne contredit plus la planner_description du revenue expert qui revendique le billing.
- `_lookup_evidence_item` : row_count = lignes reellement capturees/affichees (plus le
  `rows_matched` brut du scan ILIKE, jusqu'a 1000).
- Description d'outil ask_* : promesse non tenue "returns a rendering hint" retiree.

**Expert revenus (`OWISMIND_DEV_SalesDrive_revenue_expert.py`)** :
- Branche no_data : prefixe [Perimetre]/[Scope] ajoute (une reponse "aucune donnee" dit maintenant
  CE QUI a ete cherche).
- Resultat trop volumineux (> `_RESULT_JSON_MAX_CHARS`) : nouveau `OVERSIZE_RESULT_TEXT` fr/en
  ("le resultat existe mais est trop volumineux, precisez") au lieu du faux "Aucune donnee" ;
  `shape_result`/`extract_tabular_node` gardent `row_count` pre-drop ; les 3 moteurs (semantic tool,
  SQL deterministe, SQL custom) stampent le row_count coherent dans les spans + `executed`.
- Periode explicite ininterpretable : `_validate_period` flague `invalid_explicit` ; note bilingue
  `INVALID_PERIOD_NOTE` sur TOUTES les branches de reponse (tool_answer, table, no_data), gated sur
  `intent != compare_periods` (la liste `periods` gouverne alors la requete, pas le singulier).
- Cap MAX_TERMS=8 signale : `terms_dropped` + NOTE au semantic tool ("ground les valeurs restantes
  depuis la question").
- `extract_semantic_payload` : row_count attache au MEME node que le resultat capture (plus de
  comptage orphelin d'un autre message du transcript Agent-mode).
- `guard_custom_sql` : `LIMIT n OFFSET m` reconnu (avant : 2e LIMIT appende -> SQL invalide ->
  1 tentative brulee sur MAX_CUSTOM_SQL_ATTEMPTS) ; OFFSET preserve au cap.
- Chemins d'erreur : langue epinglee (`forced_language`) honoree partout (avant : INTERNAL_ERROR/
  PROFILE_MISSING toujours en francais) ; `lang` resolu tot -> plus d'UnboundLocal possible.
- Spans multi-SQL : row_count seulement sur la DERNIERE requete (les probes intermediaires n'ont
  plus le count final mensonger) ; success=True garde (pas de signal contraire ; None s'affiche
  comme echec cote plugin).
- Marqueur d'ambiguite generalise : `AMBIGUOUS OFFER TERM` -> **`AMBIGUOUS TERM`** dans les 2
  messages de build_semantic_question, wording neutre (hierarchie d'offre SI colonnes d'offre,
  roles d'identite SI account/partner/group) -> une collision Account_name vs Parent_Group ne recoit
  plus des consignes sirano_product.

**Cerveau semantique (`semantic_model/update_aligned_semantic_model.py` + `build_...` resynchronises)** :
- Clarif billed/accrual : qualifiers booking_type = filtres DANS un scenario, a combiner avec la
  regle par defaut Phase=ACTUALS (pipeline/to-bill portent deja FORECAST) + golden query
  "Billed revenue in 2025" (10 -> 11 golden, `LIKE 'Bill%'` + Phase ACTUALS).
- Alignement YTD cross-phase : comparer ACTUALS vs BUDGET/FORECAST/Q3F/HLF en YTD = restreindre
  TOUTES les phases a la fenetre de mois ou ACTUALS existe + divulguer la fenetre.
- Contrat "AMBIGUOUS TERM" aligne (offer levels vs colonnes d'identite) + README du dossier.
- **Rattrapage** : la section "Exact values, NEVER ILIKE / anti-fabrication" (ajoutee au script
  update le 2026-06-22) n'avait JAMAIS ete portee dans build_aligned_semantic_model.py malgre la
  note "keep in sync" -> portee ; `NEW_INSTRUCTIONS` des 2 scripts verifiees byte-identiques.

**Lookup (`tools/OWISMIND_DEV_attribute_lookup_tool.py`)** : `catalog` entre dans `_cache_key`
(2 catalogs sur le meme dataset ne peuvent plus se servir mutuellement un not_found/suggestions).

**Tests** : 286 -> **296 verts** (+10 nets : cap termes, periode invalide flaguee/non flaguee,
LIMIT OFFSET x2, row_count same-node, oversize x2, note terms_dropped, cache catalog, row_count
lookup ; -3 tests du revert `source`). AST OK, 0 tiret banni, PROD 0 fichier touche.

## Decisions
- **`source` sur show_chart/show_table/show_kpi : implemente puis RETIRE** (revert complet). La
  revue finale a prouve que le plugin ne lie qu'UN `result_block` a TOUS les artefacts d'un echange
  (`api/routes.py:804-809` : meta["result"] unique -> build_chart_payload pour chaque artefact) ->
  dans un tour a 2 specialistes, le 2e graphique se serait affiche VIDE (pire que l'actuel "skipped"
  avec message au modele). Le vrai fix = artefact tagge `agent_key` + binding par artefact dans
  `/evidence/meta` = chantier PLUGIN (zip + restart) -> hors scope sans feu vert user, documente
  comme suite possible.
- **AGENT_DONE relay du SQL lookup (stop-safety) : SKIP** (finding confirme mais benefice marginal :
  fenetre de perte etroite, et un run stoppe n'a pas de reponse finale de toute facon ; risque UX
  timeline d'un AGENT_DONE sans agent).
- Spans multi-SQL : garder success=True (un None s'affiche en echec cote `_normalize_sql_item` ;
  on n'a aucun signal par-SQL), ne retirer QUE le row_count mensonger.
- Marqueur d'ambiguite : generique plutot que 2 variants (impossible de savoir cote agent si les
  colonnes sont des colonnes d'offre sans hardcoder du metier, P3) ; le cerveau semantique porte la
  distinction (il connait ses colonnes).
- Moteur tickets (copie du moteur revenus) : PAS patche (hors scope explicite de l'audit) -> les
  fixes moteur sont a PORTER dans `OWISMIND_DEV_CSSO_Trouble_Tickets_Expert.py` apres validation
  DSS des fixes revenus (meme discipline que la promotion PROD).

## Valide / non valide
- **Valide (local)** : 296 unittest verts, ast.parse sur tous les fichiers touches, 0 em/en dash,
  PROD intacte, contrats geles intacts (anti-drift verts), NEW_INSTRUCTIONS build==update.
- **NON valide DSS** : rien n'est recolle. Les comportements LLM (wording des notes, reaction du
  semantic model au nouveau marqueur, golden query billed) sont intestables hors instance.

## Prochaines etapes
1. **Deployer en DEV DSS** : recoller les 2 Code Agents (orchestrateur + revenue expert, env 3.11)
   + le Custom Python tool attribute_lookup ; executer `update_aligned_semantic_model.py` en
   notebook (modele `AHUh9hb`) puis `dump_semantic_model.py` (rafraichit MODEL.md + .v1.json).
   **Pas de zip, pas de restart backend** (python-lib intouche). Deploiement agent + modele ENSEMBLE
   (le marqueur AMBIGUOUS TERM est un contrat sous-agent <-> instructions du modele).
2. **Smoke-tests DSS** : (a) question sans donnee -> la reponse porte le [Perimetre] ; (b) "billed
   revenue 2025" -> Phase ACTUALS + booking_type Bill% + divulgation ; (c) "budget vs actuals YTD
   2026" -> fenetre de mois alignee + divulguee ; (d) periode farfelue -> note "periode non
   interpretee" ; (e) terme ambigu offre (EVPL) inchange, terme ambigu identite (nom present dans
   account + partner) -> divulgation du role choisi ; (f) 2 specialistes en 1 tour -> plus de faux
   "indisponible".
3. **Apres validation DSS** : porter les fixes moteur dans le tickets expert DEV, puis promotion
   PROD des 2 agents + tool + scripts modele (twins `OWISMIND_PROD_V1_*`).
4. **Chantier plugin propose (feu vert user requis)** : binding par artefact (artefact tagge
   `agent_key`, `/evidence/meta` choisit le bon item generated_sql par artefact) pour permettre le
   rendu multi-specialistes en un tour ; ALORS re-introduire le param `source` (le code revert est
   dans l'historique de cette session).

## Run 2 - Artefacts natifs + narration + recall des resultats precedents (session interactive avec l'user)

### Objectif
Evaluer la faisabilite puis implementer une utilisation native/agentique des artefacts (plusieurs
graphiques propres par reponse, metadonnees, tracabilite), un comportement "boucle Claude Code"
(narration interactive fiable, y compris sur le mini modele smart), et la reutilisation des
resultats des tours precedents sans re-questionner le sous-agent.

### Fait
1. **Exploration** : 3 scouts Sonnet lecture seule (agents / backend plugin / frontend) + graphe.
   Constat cle : le backend construisait DEJA un payload data PAR artefact ; l'exemple cible
   (2 charts ACTUALS vs BUDGET d'UNE requete) ne cassait qu'au frontend (dedup par kind).
2. **Artefacts (fix L118 complet)** : frontend empile TOUS les charts/KPIs (EvidencePanel
   `.art-stack`) ; metadonnees de chart bout en bout (`x_label`/`y_label`/`unit` dans le bloc chart,
   `description` + `sql_id` top-level) portees par _record_artifact -> event ARTIFACT ->
   streaming -> stream_manager -> storage._sanitize -> /evidence/meta -> ArtifactChart (titres
   d'axes, ticks localises, legende de description) / ArtifactKpi (unite + caption) / ArtifactTable ;
   **binding par artefact** : orchestrateur stampe `latest_sql_id`, service construit
   `results_by_sql_map` (ids ambigus exclus), routes lie par artefact avec repli legacy pour les
   non-stampes ; prompt multi-charts ("tout demander en UNE tache, un chart par scenario").
3. **Revue adversariale #1 (23 agents)** : 7 confirmes -> 4 fixes racines : (a) regex nudge
   resserree (verbes prospectifs seulement, les verbes d'analyse matchaient de VRAIES reponses ;
   garde `?` = question jamais nudgee) ; (b) binding honnete (stampe-mais-manquant = etat vide,
   JAMAIS le resultat d'une autre requete) + table bindee ; (c) unicite sql_id (bump du step apres
   fan-out + exclusion des doublons cote lecture) ; (d) dedup des specs d'artefacts identiques.
4. **Narration** : bloc NARRATE enrichi pro/claude (annonce a CHAQUE phase, y compris "les donnees
   sont la, je cree 2 graphiques...") ; filet narrate-and-stop durci (cap 480c, 2 nudges max) ;
   **nouveau tool `tell_user` smart-only** (narration-as-tool : une promesse en tool call ne
   termine JAMAIS le run ; notes streamees AVANT les appels specialistes, 2/batch, ack qui pousse
   l'action ; pattern valide par la doc Anthropic "send_to_user" pointee par l'user).
5. **Recall des resultats precedents** (`recall_prior_result`, valide user apres explication
   "mallette + post-it") : backend = `chain_context_for_agent` (MEME lecture de chaine que
   l'historique, zero SQL en plus) -> `extract_prior_results` (3 derniers tours, item actif par
   echange, dedup par signature sql+colonnes contre le recall-de-recall, caps + flags de
   troncature) -> `build_prior_data_block` (index [PRIOR DATA] visible + jeton machine
   `⟦owi:prior=json⟧` borne DUR : drop oldest -> moitie lignes -> moitie colonnes -> abandon),
   gate par profil `modes` (meme opt-in que le jeton mode) ; orchestrateur = parse_prior AVANT
   parse_mode, last-token-wins, tool expose seulement si donnees presentes, branche recall
   (instantane, seed latest, re-emission span semantic-model-query pour l'Evidence du tour).
6. **Revue adversariale #2** : CRITIQUE confirme (parse_mode strippait le jeton avant parse_prior
   -> feature MORTE en live, invisible aux tests unitaires du parseur) + 7 autres -> tous corriges.
7. **Verification finale Opus** (12 agents, consigne user "Opus pour les fan-outs") : batterie
   100% verte ; 1 medium corrige (flag `recalled` -> filets node_finish couvrent les tours
   recall-seul) ; 1 low documente (recall+lookup+chart meme tour -> etat vide honnete).
8. Zip DEV reconstruit 2x (`owismind_dev-upload.zip`, entry `index-D3ATZvv5.js`, 78 entrees,
   PROD intacte). Memoire globale : `subagents-use-opus-not-fable.md`.

### Decisions
- Binding artefact : stampe-mais-introuvable degrade en vide honnete ; repli legacy reserve aux
  artefacts sans sql_id. Table du panneau bindee aussi (copie evitee quand c'est le resultat actif).
- Narration smart : JAMAIS de narration texte (narrate-and-stop prouve) -> tool `tell_user`.
- Recall : donnees dans un jeton machine invisible au modele (index 1-3 lignes seulement), 3
  derniers tours (demande user), pas d'acces DB direct de l'agent (couplage/droits refuses).
- Consigne user gravee : workflows/sous-agents en Opus (Fable = boucle principale seulement).

### Valide / non valide
- **VALIDE DSS par l'user** ("ca marche vraiment pas mal" puis "tout fonctionne super bien") :
  multi-charts + metadonnees + binding, tell_user/narration, recall. Code commite par l'user
  (`0368dd7` artefacts+narration, `0275ae9` recall).
- Tests finaux : 316 agents + 535 plugin + 134 node, Vite OK, py_compile OK, 0 tiret.
- Non fait (differes, discutes) : artefacts inline dans le message (reducer gele), fenetre recall
  > 3 tours, insight/warning cards, upgrade du modele smart.

### Prochaines etapes
- Promotion PROD de tout l'arc quand l'user le demande (copier vers OWISMIND_PROD_V1 + rebuild
  package prod) ; le tickets expert n'est pas concerne (features orchestrateur/plugin).
- Envisager : description des artefacts dans le bloc ON SCREEN ; onglet kpi dans _defaultTab.

## Run 3 - Source Data Explorer v1+v2 (exploration des donnees brutes des agents) - VALIDE DSS

### Objectif
Derniere feature du MVP avant prod : rendre EXPLORABLES les datasets bruts que les agents
utilisent, pour que les users sachent quelles donnees sont manipulees et ecrivent des prompts
precis (moins de devinettes agent). 3 surfaces : bouton sur l'ecran New Conversation (panneau
droite), onglet "Source data" dans Evidence Studio, bloc admin "Source datasets" par agent.

### Fait
**v1 (commit user `030689b`)** : bloc `sources` [{dataset,label}] max 8 dans le profil d'agent
(`validate_sources_block`, pattern benchmark) ; routes read-only `/source/meta|rows|distinct` +
`/admin/sources/datasets` (garde = miroir `_evidence_guard` sans ensure_chat_table, throttle,
timeout 30s, read-only) ; recherche globale accent/casse-insensible = 1 ILIKE sur concat_ws de
TOUTES les colonnes via `translate()` (pattern attribute_lookup, module pur
`evidence/source_search.py`) ; front : store `sources.js` + `SourceExplorer/SourceChips/
SourceTable/SourcePanel` + CTA ChatEmpty + grille AppLayout partagee (mutual exclusion avec
Evidence) + section AdminView. Revue adversariale v1 : 6 findings reels (drill/chips separes de
leur table, binding au picker live) -> fix = cluster explore legacy deplace EN BLOC dans
l'onglet sources + auto-switch du drill + panneau standalone ferme si agent sans sources.
**v2 (retours user, ce diff)** : recherche déclenchee UNIQUEMENT par Entree/bouton (debounce
supprime) ; `q` aussi sur `/evidence/rows` ; onglet Source data = UN selecteur fusionnant
tables detectees de la reponse (mode legacy chips+drill+recherche) et datasets configures de
l'AGENT DE L'ECHANGE (agentKey retenu sur l'exchange : rowToExchange + _runExchange +
agentKeyForExchange ; jamais le picker live) via `EvidenceSourcesTab.vue` + SourceExplorer
`embedded` ; contrat page -> **limit/offset** (limit 1..100 def 50, offset 0..500, clamp jamais
d'erreur ; 100 lignes puis +20 au scroll, cap client 500) ; pleine hauteur (ev-body--fill) ;
fenetrage colonnes 30 puis +20 (sentinelle horizontale, footer "X / Y colonnes") ; `/agents`
sources exposent `dataset` (dedup front, display-only). Revue v2 : justesse 0, securite 0, UX 2
confirmes corriges (flex:1 du conteneur de scroll ; selection persistee `evidence.sourceTabKey`).

### Decisions
- Datasets configures PAR AGENT sur la fiche admin (pas de liste globale) ; bouton = agent du picker.
- Evidence onglet Source data lie a l'agent de l'ECHANGE (pas le picker) ; dedup par nom de
  dataset, l'entree detectee (mode riche) gagne.
- Le front n'envoie JAMAIS de nom de table : agent key + source id entier, resolution serveur
  (regles #3/#4) ; `dataset` expose est purement display.
- Charge instance discutee avec l'user : chaque recherche = 1 requete SQL (seq scan borne
  LIMIT+timeout+read-only+throttle 15/10s par user) ; passage Entree-only reduit le QPS ;
  leviers additionnels (timeout court, semaphore global, throttle par route, cache TTL)
  proposes NON implementes (a la demande).

### Valide / non valide
- ✅ VALIDE DSS par l'user (« tout fonctionne super bien ») apres upload DEV + restart.
- 576 tests backend + 147 node verts, build Vite OK, 0 tiret, PROD intacte.
- Zip DEV final : `owismind_dev-upload.zip`, 80 entrees, `index-Sd9XWyIM.js`.
- Process : scouts Sonnet -> implementeurs Opus // -> revue adversariale 3 lentilles + verif
  par finding -> fix -> contre-verif. Incident process : prompts de verif v1 mal interpoles
  (L121) -> findings recuperes au journal, re-verifies.

### Prochaines etapes
- Promotion PROD de tout l'arc quand l'user la demande (rebuild+package prod + upload).
- Optionnel : leviers de charge (timeout 10s routes source, semaphore global, throttle dedie).
- Config DSS : remplir le bloc Source datasets des agents (revenus + tickets) au besoin.

## Run 4 - Source Data : filtres cherchables + "Use this value for agent" + fix popover Evidence + DataLoader - VALIDE DSS

### Objectif
Rendre l'explorateur Source Data plus ergonomique et agentique (demande user) :
1. filtres aeres et cherchables (colonnes ET valeurs, grandes listes 1000+ valeurs) ;
2. selection d'une valeur de cellule comme contexte pour la prochaine question a l'agent
   (avec recommandation UX/technique AVANT d'implementer) ;
3. (2e vague, retours user apres test DSS) fix du popover cellule decale dans Evidence
   Studio + animation de chargement premium chartee a la place du simple grisage.

### Fait
**Arc 1 (commite par l'user `a2754ca` "source data") :**
- **Filtres (SourceChips.vue + EvidenceChips.vue, jumeaux)** : popover "Add filter" en 2
  etapes (recherche de colonne autofocus -> picker de valeurs avec retour), recherche de
  valeurs instantanee cote client (fold accents partage), **recherche serveur sur Entree**
  quand la liste est tronquee, footer Annuler/Appliquer + compteur, popover aere 280-380px,
  chips Evidence passees en geometrie carree (charte). Flag `serverFiltered` (mon fix sur le
  patch Opus) : apres une recherche serveur retrecissante, Entree (champ vide) restaure le
  top-N et un terme plus large re-requete ; hint dedie ; etat d'erreur `pickerError` affiche.
- **Backend (seule adaptation API)** : param optionnel `q` sur GET `/source/distinct` et
  `/evidence/distinct` (validators `_clean_source_query`, `build_search_condition` sur LA
  colonne via `conditions` de `build_distinct_query` ; predicats verrouilles + exclude_id
  Evidence conserves ; caps/timeout/throttle inchanges ; q absent = byte-identique).
- **Cellule -> agent (option 3 hybride retenue)** : `promptContextModel.js` (pur, caps 12
  valeurs / 200 chars, dedup, normalisation) + store `promptContext.js` + popover
  `CellActionPopover.vue` (fixed, clamp viewport, close scroll/resize/Escape/dehors) branche
  sur SourceTable ET EvidenceTable (cellules nulles/blanches non cliquables) ; chips visibles
  au-dessus du textarea (PromptBar) ; a l'envoi le front appende un bloc lisible
  "Contexte de donnees :"/"Data context:" + `- Col = "v" (source : Label)` au message
  (stocke brut -> visible dans le fil + rejoue dans l'historique) ; purge sur changement de
  conversation et d'agent pre-conversation ; toasts added/exists/full.
- i18n ~20 cles `src.picker.*`/`src.cell.*`/`prompt.ctx.*` fr+en ; getter
  `sources.activeSourceLabel` ; `foldSearchTerm` exporte par `sourceModel.js` (map accents
  VERBATIM du serveur `_ACCENTS_FROM/TO`).
- Tests : +16 node (`promptContextModel.test.js` + fr-label) = 163 ; +6 backend = 582.
**Arc 2 (ce commit de session) :**
- **Fix popover decale dans Evidence Studio** : cause = `ev-slide-in`/`ev-rise`
  (`fill-mode: both`, keyframes transform) sur `.evidence` et `.ev-body > *` -> containing
  block pour `position:fixed` -> popover decale de la position X du panneau. Fix =
  `<Teleport to="body">` (pattern Modal/ToastHost ; tokens sur `:root` + `body[data-theme]`
  donc theme OK). Scout Sonnet a confirme la chaine exacte avant patch.
- **`DataLoader.vue` (design Fable, charte relue)** : overlay "mini bar-chart" 5 barres
  carrees encre dansant en vague sur baseline 2px, UNE barre orange, carte carree 1px +
  ombre 1px, micro-label MAJ "ON FOUILLE LES DONNEES…"/"DIGGING THROUGH THE DATA…",
  fade-in retarde 150ms (pas de flash), `prefers-reduced-motion` = chart statique.
  Branche sur les 2 tables (condition = busy) ; le dim 0.55 deplace sur les ENFANTS
  (`.src-table-scroll`/`.ev-table-scroll` + foot) pour que le loader reste net ;
  `position:relative` ajoute aux wrappers ; export via `ui/index.js` ; cle `src.crunch`.

### Decisions
- **"Use this value for agent" = option 3 hybride, transport frontend-only** (comparatif 3
  options livre a l'user avant d'implementer) : chips visibles + bloc lisible appende au
  message DANS `PromptBar.submit()`. Zero changement de `/chat/start` ni du store chat ;
  le bloc est persiste avec le message donc rejoue pour les suivis ; le grounding
  value_index exploite la valeur exacte. Option 2 (payload structure) rejetee : plomberie
  backend + bloc non persiste = contexte perdu de l'historique.
- Seule adaptation backend justifiee : `q` sur les distinct (cap serveur 100 rend le cas
  1000+ clients insoluble cote client).
- Recherche du picker : filtrage client instantane, reseau UNIQUEMENT sur Entree
  (coherent avec la decision Entree-only du Run 3) ; garde min 2 chars miroir de
  MIN_NEEDLE_CHARS ; `serverFiltered` commite seulement quand la requete ATTERRIT.
- Popover cellule : Teleport body plutot que retirer les animations du panneau (le fix
  local aurait degrade l'entree animee validee).
- Process : 3 scouts Sonnet (cartographie) + 1 scout Sonnet (root cause popover) -> 3
  patchs Opus // (backend q, popovers filtres, cell flow) -> relecture integrale Fable
  (2 vrais defauts corriges avant revue) -> **revue adversariale Workflow 15 agents Opus
  (4 lentilles + verif par finding) : 8 confirmes tous LOW, tous corriges ; 3 refutes** ;
  animation DataLoader = design Fable direct (demande user).

### Valide / non valide
- ✅ **VALIDE DSS par l'user (« parfait all good »)** apres la 2e vague (le 1er test DSS
  avait revele le popover decale + le besoin d'animation).
- 163 node + 582 backend verts, build Vite OK, 0 tiret, PROD intacte.
- Zips DEV livres : `index-B59yJsHB.js` (arc 1) puis **`index-CY8CEJu8.js`** (arc 2, final,
  80 entrees). Arc 2 = frontend-only (python-lib identique a l'arc 1).

### Prochaines etapes
- Promotion PROD de l'arc Source Data complet quand l'user la demande.
- Optionnel differe : leviers de charge routes source (timeout court, semaphore, throttle
  dedie), fenetre recall > 3 tours, artefacts inline.

## Run 5 - Analytics d'usage de la webapp (webapp_events_v1 + POST /track + track.js) - VALIDE DSS

### Objectif
Cadrer PUIS implementer un systeme d'event tracking de l'usage de la webapp (facon GA4 :
qui, quand, quelles features, quels parcours), distinct des logs agentiques existants, pour
construire ensuite un dashboard d'adoption dans Dataiku.

### Fait
- **Cadrage (3 scouts Sonnet //)** : ancienne webapp Dash (`owismind-dash`) = 1 fichier JSON
  par event dans un folder Dataiku S3 (`usage_logger.py`, 6 events), faiblesses prouvees
  (milliers de petits fichiers, recette folder->dataset non versionnee, pertes silencieuses) ;
  nouveau plugin = zero telemetrie front, mais tous les patterns prets (tables _v1 lazy,
  best-effort, throttle, identite serveur, chokepoint request(), stores Pinia identifies).
- **Decision (validee user)** : table SQL unique `webapp_events_v1` (PostgreSQL SQL_owi),
  PAS folder/S3, PAS hybride. 1 table brute + rollups Dataiku en aval ; sessions reconstruites
  par gap 30 min (pas de session_end).
- **Backend** (Workflow 2 Opus //) : DDL dans `storage/migrations.py` (13 colonnes : event_id PK,
  ts serveur, client_ts, seq, user_id, app_session_id, event_name, event_category, view_name
  (renomme, VIEW = mot reserve PG), conversation_id, agent_key, mode, props TEXT ; 4 index dont
  (app_session_id, seq)) ; NOUVEAU `storage/events.py` (EVENT_CATEGORIES = source de verite,
  validate_events pur : caps batch 40 / props 2000c / troncatures / dedup in-batch / client_ts
  parse py3.9 Z-suffix ; record_events = 1 INSERT multi-lignes ON CONFLICT DO NOTHING + COMMIT,
  never-raises) ; bucket throttle dedie (12 cap / 0.5 par s, `evidence/throttle.py`) ; route
  `POST /track` (get_json force+silent pour sendBeacon text/plain, identite serveur,
  **impersonation = drop avant tout write**, jamais de 500).
- **Frontend** : NOUVEAU `services/trackModel.js` (pur, node-testable : whitelist, caps miroir,
  queue 200 drop-oldest, drain 40, limiteur 10 events erreur/session) + `services/track.js`
  (app_session_id crypto.randomUUID, seq monotone, flush 5s ou 20 events, fetch keepalive,
  sendBeacon pagehide/hidden, no-op en impersonation, webapp_opened a l'init) ; hooks minces
  dans 13 fichiers (router afterEach, stores chat/ui/session/evidence/sources/promptContext/
  benchmark, MessageAgent feedback, SourceTable+EvidenceTable cell popover, main.js erreurs
  window+Vue, backend.js request() error_backend hors /track et /chat/poll).
- **Revue adversariale (Workflow 12 Opus : 4 lentilles + refutateur par finding)** : 1 confirme
  HIGH corrige (error_backend stockait le path AVEC query string -> `?q=<recherche user>` des
  routes distinct = PII ; fix = split('?')[0]), 7 refutes.
- **Run 2 (retour user)** : renommage complet de la taxonomie -> **38 events auto-descriptifs**
  (question_sent, answer_received {duration_ms}, webapp_opened, page_viewed...) ; l'event
  evidence_tab_changed SUPPRIME au profit d'events de 1er rang mappes sur les vraies cles
  d'onglet : chart_viewed / table_viewed / kpi_viewed / source_data_viewed /
  evidence_proof_viewed + fallback evidence_tab_viewed {tab}. Whitelists back/front verifiees
  byte-identiques par script (38/38), zero ancien nom restant (grep exhaustif).

### Decisions
- SQL plutot que folder/S3 : coherence archi totale, dataset Dataiku immediat sans parsing,
  volume beta trivial en batch, elimine les faiblesses prouvees de l'ancien systeme.
- Une seule table brute (modele GA4) : axes d'agregation en colonnes promues, variance par
  event dans props JSON ; chat_v5/users_v1/usage_monthly restent la verite de leur domaine.
- Nommage : verbe au passe auto-descriptif ; les onglets Evidence deviennent des events de
  1er rang (top features = GROUP BY event_name sans fouiller props).
- Privacy : jamais le TEXTE des recherches (longueur seule) ; paths d'erreur sans query string.
- Process : scouts Sonnet (lecture seule) + implementation/revue Opus (consigne user re-affirmee).

### Valide / non valide
- VALIDE DSS (user : "ca marche super bien" puis "parfait tout fonctionne all good") : v1 puis
  taxonomie renommee, zip DEV re-uploade.
- 614 tests back (+32) + 184 node (+21) verts, build Vite OK, 0 tiret, parite whitelist 38/38.
- Zip DEV final `owismind_dev-upload.zip` entry `index-D-NkcYpc.js`, PROD intacte (le build
  prod lance par reflexe a ete STOPPE avant execution, regle dev-first respectee).
- Table DEV a l'ancienne taxonomie : DROP TABLE conseille a l'user (recreation lazy).

### Prochaines etapes
- Dashboard d'adoption Dataiku : dataset SQL sur la table + rollups (DAU/WAU, top features,
  sessions par gap 30 min, parcours via app_session_id+seq).
- Promotion PROD de l'arc analytics quand demandee (avec le reste des arcs en attente).
- Events v2 selon ce que le dashboard revele (latences fines, clics additionnels).

## Run 6 - Mode ephemere (Smart par defaut, reset a l'envoi) + mode persiste et affiche par reponse - VALIDE DSS

### Objectif
Retour user sur les modes Smart/Pro/Claude : (1) Smart = defaut PERMANENT, Pro/Claude
selectionnables de maniere EPHEMERE (des que la question part, le picker revient a Smart ;
plus aucune memorisation du dernier mode) ; (2) le mode de CHAQUE reponse affiche dans la
ligne tokens/cout sous la reponse, donc persiste en base par echange.

### Fait
- **Backend (Opus //)** : colonne `mode VARCHAR(16)` nullable sur `webapp_chat_v5` SANS bump
  `_v6` (un bump aurait masque toutes les conversations existantes) : DDL CREATE + **ALTER
  `ADD COLUMN IF NOT EXISTS` idempotent** dans le meme ensure (2e usage du precedent sanctionne
  `users_v1`, commente pareil) ; NOUVEAU helper pur `agents/context.resolve_effective_mode`
  (requested + supports_modes -> 'smart'|'pro'|'claude'|None) ; `/chat/start` resout le mode
  AVANT le write phase-1 et le passe a `save_user_message(mode=)` (edit/regenerate = meme
  chemin, stamp automatique des versions soeurs) ; `mode` ajoute a `_COLUMNS` ->
  `/conversation` l'expose NULL-safe. `read_exchange` (benchmark) et stream_manager inchanges.
- **Frontend (Opus //)** : `ui.js` boot 'smart' inconditionnel + **persistance localStorage du
  mode SUPPRIMEE** (purge de la cle legacy `owismind.modelMode` a l'init) + `resetModelMode()` ;
  `chat.js` : `sentMode()` (null si l'agent ne gere pas les modes), capture `rawMode`/`runMode`
  AVANT reset, stamp `newVersion({mode: runMode})`, reset SYNCHRONE avant l'await (picker revient
  instantanement), stream avec `rawMode`, analytics `question_sent`/`answer_received`/`question_
  edited`/`answer_regenerated` lisent le STAMP (jamais le store possiblement resete) ;
  `rowToExchange` stamp `modeFromRow(r)` (reload) ; `timelineModel.js` : `mode: null` au shape
  `createAnswerState` + helper pur `modeFromRow` + `ANSWER_MODES` (miroir de usageFromRow ;
  reducer/applyEvent/timelineSignature INTOUCHES) ; `MessageAgent.vue` : segment mode dans la
  ligne usage existante (classes `.u-seg`/`.u-sep`, libelles `mode.smart|pro|claude` reutilises,
  tooltip `msg.usage_mode` fr+en dans extra.js). Vieilles reponses (mode NULL) = rendu identique.
- **Revue adversariale (Workflow 3 lentilles + refutateur, focus timelineModel gele + chemin
  critique chat)** : **0 confirme** (2 lentilles vides ; 1 nit de commentaire refute, corrige
  quand meme : le stamp se lit quand le run se termine, pas "right away").

### Decisions
- ADD COLUMN nullable plutot que `_v6` : la continuite des conversations existantes prime ;
  relaxation du no-ALTER deja sanctionnee une fois (users_v1), motif identique, documente.
- Source de verite du mode affiche = le STAMP sur la version (live) puis la colonne (reload),
  jamais le store ui (resete). Agents sans modes -> NULL partout (plus honnete que 'smart').
- Reset synchrone au dispatch (avant l'await) : le picker revient a Smart a l'instant de l'envoi.

### Valide / non valide
- VALIDE DSS (user : « super ça marche très bien ») : reset ephemere + affichage du mode +
  persistance. Zip DEV `index-CbbPsbRU.js` uploade, backend redemarre par l'user (l'ALTER
  s'applique tout seul au premier ensure).
- 629 back (+15) + 186 node (+2) verts, build Vite OK, 0 tiret ajoute, PROD intacte.

### Prochaines etapes
- Dashboard d'adoption (Run 5) : le mode par reponse est maintenant croisable chat_v5 x events.
- Promotion PROD des arcs en attente quand demandee.
