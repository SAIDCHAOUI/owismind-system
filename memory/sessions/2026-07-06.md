# Session 2026-07-06 (démarrée le 2026-07-05 au soir) - Manipulation de données sans IA : agrégats DB, zone Calculer, plages de dates, unification Evidence, plugin dev_v2

## Objectif
Donner aux utilisateurs métier (profil Excel) le pouvoir de manipuler/vérifier la donnée SANS SQL et
SANS appel IA : d'abord une session de brainstorming/cadrage (webapp uniquement), puis, sur feu vert
user, l'implémentation en 3 vagues avec agents Sonnet/Opus uniquement, livrée dans un NOUVEAU plugin
coexistant `owismind_dev_v2` (prod ET dev stable intacts : démo dans quelques jours).

## Fait

### Vague 0 - Brainstorming/cadrage (workflow 4 scouts Sonnet + 3 experts Opus)
- Constat clé : filtres déjà structurés `{column, op, values}`, types de colonnes connus serveur,
  résultats capturés bornés (200 lignes, flag truncated) -> tout total doit être calculé EN BASE sur
  le jeu filtré complet (la fenêtre affichée n'est qu'une pagination). Plan en phases validé user,
  avec 2 corrections user : UI ultra-simple (réflexe Excel), et calculs TOUJOURS en base (jamais sur
  les lignes affichées).

### Vague 1 - Spec d'agrégation serveur + premières surfaces (2 agents Opus //)
- **Backend `POST /source/aggregate`** : spec structuré `{agent, source, q, filters, group{column,bucket},
  measures[{fn,column}], limit}` ; fns whitelistées (count, count_distinct, sum, avg, min, max),
  gate numérique sur le schéma live, `DATE_TRUNC` à unité figée, LIMIT interne obligatoire (50+1),
  2e requête totals non groupée pour les % exacts, read-only + statement_timeout + throttle hérités.
  Builder pur `build_aggregate_query` (+ `NULLS LAST`), classifieurs `is_numeric_type`/`is_temporal_type`
  dans `sql_config`, validateur borné.
- **Frontend** : barre de totaux (compte DB exact), stats de colonne via sigma, vue Analyser
  (group-by 1 colonne + mesure, % du total, data-bars neutres), lien discret « Vérifier ce chiffre »
  (ouvre Evidence sur l'onglet Sources, gaté sur `meta.available`).
- **`tools/build_dev_plugin.py --v2`** : 3e plugin coexistant `owismind_dev_v2` (id, labels DEV v2,
  base Vite, staging + zip dédiés) ; `--check` OK dans les 2 modes.
- Revue adversariale (4 lentilles + réfutateurs) : 8 confirmés / 0 réfuté, tous corrigés (stale count
  sur échec de refresh, NULLS LAST, petites valeurs != « 0 », gros entiers exacts sans dérive 2^53,
  fetch lignes évité en vue Analyser, clic mort verify sur échange dégradé, aria sigma, ARIA segmenté).
- QA Playwright réelle (harnais stub `getWebAppBackendUrl` + mini-serveur) : 9/9 puis re-checks.
- **✅ VALIDÉ DSS par l'user** (screenshot dev2 : « super c'est vraiment bien ça marche »).

### Vague 2 - Retour user : zone Calculer + plages de dates + médiane (2 agents Opus //)
- Retour user : group-by trop technique pour un sales ; les métriques doivent vivre dans la zone vide
  en haut à droite SANS quitter le dataset ; plage de dates en 1-2 clics ; garder l'onglet Analyser.
- **Backend** : fn `median` (PERCENTILE_CONT(0.5), gate numérique) ; op `BETWEEN` accepté sur les
  filtres `/source/*` uniquement (exactement 2 valeurs), préservé dans `_source_conditions`.
- **Frontend** : zone **CALCULER** (select colonne + cartes KPI par TYPE : num = Somme/Moyenne/
  Médiane/Min/Max ; date = Min/Max ; texte = Valeurs distinctes), persistante (suit les filtres),
  sigma -> zone, pied réduit au compte ; **filtre de plage** sur colonnes temporelles (2 inputs month
  De/À + « Année complète » one-shot) -> chip `BETWEEN` éditable pré-rempli, bornes
  `[YYYY-MM-01, dernier-jour T23:59:59.999999]` ; « Médiane de X » dans Analyser.
- Revue : 6 confirmés / 1 réfuté, tous corrigés (classifieur temporel front = miroir EXACT du back,
  borne microseconde, count auto-réparé, récap de scope en libellé mois, aria selects, placeholder
  AAAA-MM pour le fallback texte Firefox/Safari).
- QA 10/10 (+ re-check ciblé après détection d'un run QA sur bundle pré-fix).

### Vague 3 - Unification Evidence Studio (retour user : « même artefact, juste pré-filtré ; réunir les deux codes »)
- **Backend** : module partagé **`evidence/aggregate_core.py`** (templates, buckets, plan) consommé
  par les DEUX services ; `_evidence_conditions` extrait byte-identique de `evidence_rows` (chips
  verrouillées kept_ids + filtres + advanced + search + drill), `BETWEEN` accepté aussi sur
  `/evidence/rows` ; **`POST /evidence/aggregate`** = agrégats DB sur EXACTEMENT le périmètre
  pré-filtré par le SQL de la réponse.
- **Frontend** : factory unique **`composables/aggregateSurface.js`** (machine count/Calculer/Analyser,
  gardes seq+epoch, `invalidate()`) instanciée par `sources.js` (byte-identique) ET `evidence.js` ;
  `SourceCalc`/`SourceAnalyze` bi-hôtes via prop `surface`+`columns` ; `RangePopoverFields.vue`
  partagé (SourceChips + EvidenceChips, chips USER seulement, chips agent verrouillées intactes) ;
  onglet Source data d'Evidence = même layout (zone Calculer, segmenté, sigma, compte).
- Revue : 6 confirmés / 0 réfuté, tous traités : **surface paresseuse** (plus de COUNT à chaque
  réponse d'agent : calcul seulement quand l'onglet Source data est visible + rattrapage),
  classifieur numérique = miroir exact (money/serial offerts, interval/geopoint exclus), cap des
  buckets garde les plus RÉCENTS (`key DESC` SQL + re-tri chronologique client `sortBucketRowsAsc`),
  limitation multi-table documentée (colonnes du meta = table par défaut, convention pré-existante).
- QA : parcours COMPLET stubbé (conversation -> réponse agent -> Evidence -> Source data) : 8/9 PASS
  + 1 SKIP justifié ; preuve payloads : mêmes `kept_ids` + même `BETWEEN` microseconde sur
  `/evidence/rows` ET `/evidence/aggregate` ; re-check ciblé post-fixes 3/3 (lazy, buckets ASC
  affichés, interval/money).

### Livrable
- **`Plugin/ready-for-dataiku/owismind_dev_v2-upload.zip`** (82 entrées, bundle `index-CRcrpMCq.js`),
  contient les 3 vagues. `owismind-upload.zip` (prod, 24/06) et `owismind_dev-upload.zip` (03/07)
  **intacts au timestamp près**.
- Suites finales : **694 backend + 268 node verts**, Vite OK, 0 tiret, accolades équilibrées.

## Décisions
- **Tout chiffre montré = calcul DB sur le jeu filtré complet** ; un calcul client sur fenêtre
  partielle est interdit (source de mauvaises réponses). Affichage inversé : « affichage des N
  premières lignes, les calculs portent sur tout ».
- **Un seul primitif serveur** (spec d'agrégation) ; totaux, stats, top-N, buckets = presets.
- **Un seul code pour accueil et Evidence** (aggregate_core + aggregateSurface + composants bi-hôtes).
- **dev_v2 = 3e plugin coexistant** (`--v2`), jamais toucher prod/dev pendant la période de démo.
- Zone Calculer par TYPE de colonne, générique (aucun nom de colonne en dur) ; group-by relégué à
  l'onglet Analyser (conservé, à améliorer plus tard).
- Surface Evidence paresseuse (coût instance) ; chips agent verrouillées jamais transformées en plage.

## Validé / non validé
- **✅ Validé DSS (user)** : vague 1 sur dev_v2 (barre de totaux, Analyser, filtres) - screenshot user.
- **✅ Validé local (QA runtime + revues)** : vagues 2 et 3 ; parcours Evidence complet en stub.
- **⏳ NON validé DSS** : zip dev_v2 FINAL (vagues 2+3) pas encore uploadé ; smoke à faire (voir
  prochaines étapes). Drill + multi-table switch côté Evidence non exercés par le stub (1 seule table).

## Prochaines étapes
1. **Uploader `owismind_dev_v2-upload.zip`** (remplace le plugin dev_v2) + redémarrer le backend de
   sa webapp. Smoke : zone CALCULER sur `amount_eur` (5 cartes dont Médiane) ; plage `year_month`
   De/À + « Année complète » ; Analyser (Médiane de X, buckets chronologiques) ; Evidence -> Source
   data : chip agent verrouillé + zone Calculer + plage + compte ; ouverture simple du panneau de
   preuves = AUCUN appel aggregate (lazy) ; dark.
2. Si OK : décider du port vers le plugin DEV principal puis prod (à la demande user).
3. Différés proposés : refetch meta par table (lève la limitation multi-table), export CSV du résultat
   d'agrégat, vues sauvegardées, nudge pré-envoi « répondre sans IA », comparaison de périodes,
   profil de colonne.

## Run 2 (nuit du 06, session autonome sur retours user) - Source Data v3 : popover, mesures choisies, plages string, cascade, persistance, contexte ecran -> agent

### Objectif
Traiter les 5 retours user du soir (message vocal, user absent toute la nuit, decisions deleguees) :
(1) le popover « Ajouter un filtre » ne se ferme pas au clic exterieur en DSS ; (2) zone Calculer =
trop de KPI d'un coup + gros chiffres tronques par ellipsis ; (3) plages de dates impossibles sur sa
colonne `year_month` (clic mois par mois) ; (4) filtres en cascade (le picker du filtre N doit refleter
les filtres actifs) + interconnexion generale des filtres entre surfaces ; (5) GROS morceau : donner a
l'agent la conscience de ce que l'user a fait a l'ecran (filtres/calculs), sur CONSENTEMENT, sans jamais
envoyer les lignes brutes. Process impose par l'user : scouts Sonnet, implementation Opus, 1-2 agents
Fable pour la planification pointue.

### Fait
- **Exploration** : 6 scouts Sonnet // (popovers, calc, backend distinct/aggregate, interconnexion
  stores, pipeline promptContext, cote agent). Decouvertes clefs : le canal `screen_context` ->
  bloc `[ON SCREEN NOW]` existait deja (Run 2 du 02/07) et l'orchestrateur le strippe deja via
  `_CTX_BLOCK_RE` ; la colonne user est tres probablement typee string (d'ou pas de mode plage) ;
  le distinct n'envoyait aucun filtre (cascade a cabler back+front).
- **(1) Popover** : fermeture STRUCTURELLE : backdrop invisible plein viewport (`@click` ferme,
  `@mousedown.prevent`) + option `closeOnWindowBlur` de `useClickOutside` (blur de l'iframe DSS =
  clic sur le chrome DSS ; garde activeElement pour le calendrier natif du month-input). SourceChips
  + EvidenceChips (miroirs conserves).
- **(2) Zone Calculer** : `calcFns` (sous-ensemble choisi, defauts par type : num=Somme,
  temporel=Min+Max, texte=Distincts) dans `aggregateSurface` + dropdown Mesures a checkboxes
  (SourceCalc, bi-hote) ; cartes GRANDES (20px heavy, wrap, `title`, zero ellipsis) ; l'aggregate ne
  demande QUE les fns choisies.
- **(3) Plages sur colonnes string** : sniffing `looksLikeIsoDateValues` (fenetre distinct 100% ISO)
  -> mode plage auto avec bascule « Choisir des valeurs precises » (et l'inverse sur colonnes
  temporelles) ; bornes lexicales SURES pour texte `['YYYY-MM','YYYY-MM-99']` (raisonnement collation
  byte-wise + punctuation-insensitive documente) ; edition d'une chip BETWEEN sur string re-entre en
  mode plage.
- **(4) Cascade** : `/source/distinct` et `/evidence/distinct` passent **GET -> POST** avec
  `filters` (+ `scope_q`, et cote evidence `kept_ids`/`drill`/`include_advanced`/`exclude_id`) ;
  conditions via les MEMES blocs que rows (`_source_conditions`, `render_predicate`) ; retro-compat
  byte-identique sans les nouveaux champs ; le front exclut la chip en cours d'edition
  (`excludeChipKey` store-side). Nouveau `test_cascading_distinct.py`.
- **(4bis) Interconnexion / persistance** : `sourceViewMemory.js` (Map par (agent, dataset), cap 24,
  sanitizers purs) branche dans `stores/sources.js` : chips/q/sort/calc survivent fermeture/reouverture
  du panneau, switchs d'agent et mode agent embarque d'Evidence (meme store Pinia). Le scope de preuve
  Evidence (table detectee de la reponse) reste volontairement isole (regle produit).
- **(5) Contexte ecran -> agent** : conception finalisee par un agent Fable (go-with-changes) puis
  implementation en 2 lots. Backend : `sanitize_source_state` (whitelist, caps, strip U+27E6/27E7,
  never-raises) + section SOURCE-DATA VIEW dans le bloc `[ON SCREEN NOW]` (budget 1200 chars, echelle
  de degradation, phrase de permission verbatim TOUJOURS presente) ; `_sanitize_screen_context` accepte
  `source_state` meme panneau ferme ; `_SCREEN_TABS` +kpi+sources ; events `screen_context_offered/
  included/dismissed` (41 whitelistes, parite front) ; paragraphe ajoute au prompt orchestrateur DEV
  (repo ; a recoller DSS). Frontend : `screenContextModel.js` pur (snapshots explorer/evidence,
  signature de scope, caps miroirs) + store `screenContext` (consentement STICKY PAR SIGNATURE :
  accepte/refuse tant que la vue ne change pas, chip retirable, resets conversation/agent) + bandeau
  charte dans PromptBar (filet orange 4px, role=status) + envoi `screen_context.source_state` LIVE a
  chaque send consenti dans `chat.js`. JAMAIS de lignes brutes ; chiffres = valeurs DB deja calculees.
- **Verification** : revue adversariale 4 lentilles Opus + refutateurs (10 findings -> **7 confirmes /
  3 refutes**, tous traites : kept_ids forwarde au distinct evidence, echelle de troncature reserve la
  phrase de permission + rung drill, desync du champ recherche apres restore (watcher q->term),
  role=status, copy du bandeau honnete, aria du dropdown Mesures, suppression du validateur GET mort
  + migration de ses tests) ; **QA Playwright reelle** (harnais L129/L131, bundle prouve frais L132) :
  8/8 PASS puis re-checks post-fix R1-R4 PASS, 0 erreur console, captures clair/sombre LUES.
- **Docs** : backend-api.md a jour (2 routes POST, source_state, nouveaux validateurs).
- **Livrable** : zip `owismind_dev_v2-upload.zip` reconstruit (82 entrees, bundle `index-BvJL4hQU.js`),
  prod + dev stable INTACTS au timestamp pres.

### Décisions
- Contexte ecran via le canal EXISTANT `screen_context`/`[ON SCREEN NOW]` (pas de bloc colle au
  message visible : le precedent cell-to-agent pollue le message stocke et fuit dans le task des
  sous-agents) ; le bloc porte sa PROPRE phrase de permission -> degrade proprement tant que le
  prompt orchestrateur n'est pas recolle.
- Consentement sticky par signature de scope (pas de re-prompt a chaque envoi, pas de nag apres
  refus) ; snapshot LIVE a chaque envoi ; jamais persiste en DB.
- Bornes de plage TEXTE = `['YYYY-MM','YYYY-MM-99']` (surs sous les 2 familles de collation) vs
  bornes timestamp microseconde pour les colonnes temporelles.
- Backdrop = le 1er clic exterieur ferme SANS activer l'element dessous (UX dropdown standard).
- Distinct evidence : kept_ids desormais honore (miroir exact de evidence_rows) ; scope verrouille
  re-derive serveur sinon.
- Process : 3 agents Opus ont fini leur travail mais echoue 5x sur l'emission de la sortie structuree
  -> verifier git status + transcript AVANT tout re-run (travail complet a chaque fois) ; gros agents
  d'implementation en sortie TEXTE.

### Validé / non validé
- **Valide en local** : 772 tests backend + 334 node + 316 dataiku-agents verts ; Vite OK ; 0 tiret
  (diff + fichiers nouveaux) ; QA runtime complete + re-checks sur bundles prouves frais
  (`index-B5noBeo4.js` en harnais ; zip = `index-BvJL4hQU.js`).
- **NON valide DSS** : tout ce run (zip dev_v2 a uploader + restart backend webapp dev_v2 ; paragraphe
  orchestrateur a recoller dans le Code Agent DEV env 3.11).
- Non exerce en QA : plage string cote EvidenceChips (miroir code de SourceChips, couvert par tests),
  blur reel d'iframe DSS (couvert par le backdrop en fallback).

### Prochaines étapes
1. **Uploader `owismind_dev_v2-upload.zip`** + redemarrer le backend de la webapp dev_v2. Smoke :
   popover se ferme au clic exterieur (y compris clic chrome DSS) ; zone Calculer = 1 carte Somme par
   defaut + dropdown Mesures + gros chiffre complet ; filtre `year_month` = mode plage auto (colonne
   string) avec bascule ; cascade (filtre Actuals puis picker client = seulement les clients Actuals ;
   edition d'une chip = ses propres valeurs restent visibles) ; fermer/rouvrir le panneau = filtres +
   recherche + calc restaures ; taper un prompt avec filtres actifs = bandeau -> Inclure -> la reponse
   de l'agent exploite les chiffres de l'ecran ; dark.
2. **Recoller le prompt orchestrateur DEV** (`OWISMIND_DEV_OWIsMind_orchestrator.py`, env 3.11,
   agent 038G7mlF) : paragraphe SOURCE-DATA VIEW (fin de section # WHAT'S ON THE USER'S SCREEN).
   Sans lui la feature marche (permission embarquee dans le bloc), avec lui l'orchestrateur cite
   mieux les chiffres et restate les filtres dans le task du specialiste.
3. Puis a la demande : port DEV principal / prod ; differes precedents (refetch meta par table,
   export CSV, comparaison de periodes) + nouveaux (chips contexte editables, contexte evidence
   legacy enrichi).

## Run 3 (matin du 06, retours user au réveil) - fix plages sur type SQL réel + filtre depuis cellule + transparence totale du contexte écran

### Objectif
3 retours user sur le Run 2 : (1) BUG DSS bloquant : la plage sur `year_month` jette
`invalid input syntax for type date: "2026-01"` (colonne string côté schéma DSS mais DATE côté
PostgreSQL -> les bornes lexicales `'2026-01'..'2026-06-99'` ne parsent pas) ; demande d'une
« fonction intelligente qui détecte le format » ; (2) dans le popup de cellule, en plus de
« utiliser pour l'agent », pouvoir FILTRER directement sur la valeur ; (3) transparence totale du
contexte écran donné à l'agent (« je ne veux rien cacher du tout »), visible élégamment.

### Fait
- **(1) Bornes intelligentes** : `sampleIsoShape(values)` (forme du 1er échantillon : jour, séparateur
  T/espace, secondes, fraction, suffixe Z/offset) + `monthRangeToBetweenSmart(from, to, samples)` :
  les bornes MIMENT le format observé (ex. `'2026-01-01T00:00:00.000Z'..'2026-06-30T23:59:59.999Z'`) =
  tri lexicographique correct sur une vraie colonne texte (format uniforme) ET littéral ISO 8601
  parseable par les parseurs date/timestamp de PG (le schéma peut mentir, les deux réalités marchent).
  `monthRangeToBetweenLexical` (sentinelle `-99` non parseable) SUPPRIMÉ. Fallback sans échantillon =
  bornes calendaires. Mimétisme des secondes (`hh:mm` vs `hh:mm:ss`, durci par l'orchestrateur).
- **(2) Filtre depuis cellule** : action « Filtrer sur cette valeur » dans CellActionPopover
  (SourceTable bi-hôte) : merge dans la chip `=`/`IN` existante de la colonne (dédup String, cap 50),
  sinon nouvelle chip `=` ; jamais de merge dans un BETWEEN ; réutilise le tracking existant.
- **(3) Transparence** : colonne **`screen_ctx` TEXT** sur `webapp_chat_v5` (3e usage de l'exception
  ADD COLUMN IF NOT EXISTS, pattern L126) : le `source_state` consenti est sanitisé AVANT la phase 1
  et stampé en JSON compact (>4000c -> NULL) ; `/conversation` l'expose verbatim. Front :
  `screenCtxFromRow` pur (timelineModel, reducer/signature INTACTS F8) + stamp live dans `chat.js` ;
  `ScreenContextDetail.vue` partagé (liste dl plate, charte) ; chip pré-envoi CLIQUABLE (popover du
  snapshot LIVE exact) ; sous le message user : ligne dépliable « Contexte écran joint » qui SURVIT
  au rechargement (lue de la DB). `describeSnapshot` pur, 100% localisable (connecteurs de/par/lignes
  et « +N autres » traduits par le composant, jamais d'anglais en dur).
- **Vérification** : revue 2 lentilles + réfutateurs = 2 confirmés / 3 réfutés, corrigés (garde
  Apply pendant le chargement des échantillons en mode lexical ; connecteurs anglais du détail) ;
  QA runtime S1-S4 : bornes EXACTES `.000Z/.999Z` observées sur le payload, merge IN depuis cellule,
  détail avant/après envoi, 0 erreur console (captures indisponibles dans l'env : charte vérifiée
  par computed styles clair+sombre). 5 tirets HISTORIQUES purgés de timeline.test.js.
- **Docs** : backend-api.md (`screen_ctx` + transparence) + data-model.md (colonne, 3e exception).
- **Livrable** : zip dev_v2 RECONSTRUIT (`index-gYAOS4OQ.js`, 82 entrées), prod + dev stable intacts.

### Décisions
- Le schéma DSS peut MENTIR sur le type SQL réel : les bornes de plage sur colonne « string »
  MIMENT le format observé des valeurs (jamais de sentinelle non parseable type `-99`).
- Transparence par PERSISTANCE (colonne dédiée relue par /conversation), pas par pollution du texte
  du message ; le détail pré-envoi montre exactement ce qui partira au prochain send.
- Filtre depuis cellule : merge additif dans la chip existante (comportement Excel), pas de chip
  dupliquée par colonne.

### Validé / non validé
- **Validé local** : 788 backend + 351 node verts, Vite OK, QA runtime S1-S3 PASS + charte par
  computed styles, 0 tiret ajouté. **NON validé DSS** (zip à uploader + restart ; la colonne
  `screen_ctx` se crée au 1er ensure).

### Prochaines étapes
1. Uploader le zip dev_v2 + restart backend. Smoke Run 3 : plage sur `year_month` (bornes
   `.000Z/.999Z`, plus d'erreur 409) ; clic cellule -> Filtrer sur cette valeur (2e valeur = IN) ;
   bandeau -> Inclure -> clic sur la chip = détail ; envoyer -> ligne « Contexte écran joint »
   sous le message, dépliable, encore là après F5.
2. Toujours à recoller : paragraphe orchestrateur DEV (Run 2).

## Run 4 (journée du 06, retours user post-déploiement) - filtre depuis cellule PARTOUT + menu de colonne Sort/Filter + colonne orange

### Objectif
Retours user après upload Runs 2+3 (webapp + orchestrateur recollés, « ça marche à merveille ») :
(1) « Filter on this value » : parfois inopérant quand un filtre existe déjà, et TOTALEMENT mort
dans Evidence Studio ; doit marcher partout, avec ou sans filtre existant ; (2) kiff perso : un
petit contrôle à droite de chaque en-tête de colonne ouvrant Sort + Filter : tri activable ET
retirable (aujourd'hui impossible à enlever), Filter ouvrant le même dropdown de valeurs pré-réglé
sur la colonne, et colonne filtrée affichée en ORANGE ; partout (Source data + Evidence Studio).

### Fait
- **Fix Evidence (cause réelle)** : `EvidenceTable.vue` montait `CellActionPopover` (avec le nouveau
  bouton) mais SANS handler `@filter` -> clic mort. Handler ajouté : merge dans la chip user/éditable
  non-BETWEEN de la colonne (user préférée), sinon `evidence.addFilter` (chip user) ; chips agent
  VERROUILLÉES jamais touchées (kept_ids préservés, prouvé par QA).
- **Fix « rien ne se passe »** : tous les cas silencieux ont maintenant un toast (Filtre appliqué /
  Valeur déjà filtrée / Filtre plein 50) dans les DEUX tables : plus aucun no-op muet.
- **Tri 3 états** : helper pur `nextSortDir` (asc -> desc -> aucun) ; `setSort(column, dir)` des 2
  stores : dir explicite ('asc'|'desc'|null) OU cycle si omis ; clic sur le nom d'en-tête = cycle
  (le tri se retire enfin).
- **Menu de colonne** : NOUVEAU `ColumnMenu.vue` (Teleport, x/y clampés, click-outside + Échap,
  charte carrée) : Trier croissant / décroissant (coche active) / Ne plus trier (désactivé si non
  trié) / Filtrer les valeurs... ; déclencheur chevron discret à droite de chaque th (hover/focus,
  épinglé si trié/filtré) dans les 2 tables.
- **Filter pré-réglé** : `requestColumnFilter(column)` sur les 2 stores ; les 2 composants chips
  watchent et rejouent openAdd+pickColumn = TOUT le picker existant réutilisé (distinct, cascade,
  sniffing, plages). Au cap des 20 filtres : item désactivé avec tooltip + garde watcher (finding
  revue : backdrop sans picker = piège à clic).
- **Colonne orange** : Set des colonnes filtrées (sources : toutes les chips ; evidence : toutes,
  verrouillées incluses) -> texte d'en-tête `--orange-text` + chevron épinglé ; disparaît au retrait.
- **Vérification** : revue ciblée + réfutateur = 1 confirmé (cap 20, corrigé) / 0 autre ; QA runtime
  M1-M6 TOUS PASS sur bundle frais, dont le parcours Evidence COMPLET stubbé (conversation +
  meta/rows/aggregate/distinct, chip verrouillée id=1 préservée dans kept_ids, en-tête orange
  hérité de la chip agent, menu + tri dans la table Evidence) ; 0 erreur console.
- **Livrable** : zip dev_v2 RECONSTRUIT (82 entrées), 788 back + 352 node verts, Vite OK, 0 tiret.

### Décisions
- Le picker pré-réglé s'ouvre à la barre de chips (au-dessus de la table), là où le filtre atterrit :
  réutilisation à 100% de la machinerie existante plutôt qu'une extraction risquée du picker.
- Indicateur orange = texte d'en-tête seul (charte : orange rare, pas d'aplat).
- Evidence : merge uniquement dans les chips user/éditables ; les verrouillées restent intouchables.

### Validé / non validé
- **Validé local** (QA M1-M6 PASS + revue soldée). **NON validé DSS** : zip à uploader + restart.

### Prochaines étapes
1. Uploader le zip dev_v2 + restart. Smoke Run 4 : cellule -> Filtrer (avec un filtre existant sur
   une AUTRE colonne = nouvelle chip ; même colonne = fusion IN ; toast à chaque fois) ; Evidence
   Studio table détectée : cellule -> Filtrer = chip user + colonne orange ; chevron d'en-tête ->
   menu (tri croissant/décroissant/retrait, coche) ; clic nom de colonne x3 = retour non trié ;
   menu -> Filtrer les valeurs = picker pré-réglé sur la colonne ; colonne filtrée orange, redevient
   normale au retrait de la chip ; dark.
