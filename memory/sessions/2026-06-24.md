# Session 2026-06-24 - Fix UI : pouces feedback + modes de réponse par agent (Smart/Pro/Claude)

## Objectif
Deux corrections webapp demandées par l'user :
1. Le pouce 👍/👎 sous chaque réponse d'agent est illisible (« coupé », « apparaît comme un simple
   carré »). Et : ne plus afficher le sélecteur de mode pour un agent qui ne le supporte pas (idéalement
   détecté tout seul).
2. Les modes eco/medium/high : (a) ne doivent apparaître que pour OWIsMind, configurés **par agent** dans
   l'admin ; (b) renommés **eco->Smart, medium->Pro, high->Claude** ; (c) code couleur poussant **Smart**
   à fond (recommandé pour presque tout) ; (d) descriptions : Pro plus puissant, **Claude** réservé aux
   questions très complexes, bien construire son prompt, **beaucoup plus cher, épuise vite le quota 50 $/mois**.

## Fait (repo only - NON validé DSS)
- **Pouces (cause prouvée au navigateur, capture 13px + zoom)** : glyphes outline filiformes -> carré
  illisible à petite taille ; en plus `Icon.vue` impose `width:1em` inline qui écrase la règle CSS 13px du
  footer. **Fix** : `components/ui/icons.js` -> `thumbsUp`/`thumbsDown` remplacés par des glyphes **pleins**
  (Material, `fill="currentColor"`, lisibles à toute taille) ; `MessageAgent.vue` -> boutons pouce passés à
  `:size="15"` + `aria-label`/`aria-pressed` + classe `fb-btn`. Re-testé au navigateur : pouces nets à
  13/15/16px (orange en like, rouge en dislike).
- **Modes par agent** : nouveau flag booléen `modes` dans le profil d'agent.
  - Backend : `security/validation.py` `validate_agent_meta` ajoute `"modes": bool(raw.get("modes"))`
    (défaut off, ne lève jamais) ; `api/routes.py` `/agents` projette `modes` ; **`/chat/start` gate** :
    ne relaie le token `⟦owi:mode=…⟧` que si `agent.profile.modes` (sinon `mode=None` -> aucun token,
    donc plus de fuite du token dans le prompt d'un agent visuel). C'est le « détecter tout seul » fiable
    (enforcement serveur) + le toggle admin explicite.
  - Frontend : `stores/session.js` -> computeds `selectedAgent` + `selectedAgentSupportsModes` ;
    `PromptBar.vue` -> `<ModelModePicker v-if="session.selectedAgentSupportsModes" />` (picker masqué sinon) ;
    `views/AdminView.vue` -> case « Cet agent gère les modes de réponse (Smart/Pro/Claude) » dans la fiche
    (emptyProfile/editForm/openEditor/applyEditor + style `.ed-check` carré charté).
- **Renommage + couleurs** (clés internes eco/medium/high INCHANGÉES, seuls libellés/descriptions changent) :
  `i18n/extra.js` FR+EN -> `mode.eco=Smart`, `mode.medium=Pro`, `mode.high=Claude` ; intro/reco_line/hints/
  descs/cost/envelope réécrits (Smart recommandé pour presque tout ; Pro plus puissant et plus cher ; Claude
  le plus puissant, complexe seulement, soigner le prompt, **beaucoup plus cher, épuise vite l'enveloppe
  50 $/mois**) ; nouvelle clé `mode.high_warning` + clés admin `f_modes`/`f_modes_opt`/`f_modes_hint`.
  `ModelModePicker.vue` : badge « RECOMMANDÉ » **vert** (chip status), callout **vert** (Smart) / callout
  **rouge** d'avertissement (Claude), jauge de coût teintée par sévérité (vert/orange/rouge). Rendu vérifié
  au navigateur en clair ET sombre (contrastes AA).
- **Tests** : +3 tests `test_validation.py` (flag `modes` défaut off + coercition bool). **456 backend OK**,
  **124 frontend OK**, **build Vite OK**, **0 tiret** (scan Python), parité i18n FR/EN OK.

## Incident + correction (important)
- J'ai d'abord packagé la **PROD par erreur** (`/build-plugin` + `/package-plugin`), ce qui a régénéré les
  fichiers prod tracked (`resource/owismind-app/` + `body.html`) + le zip prod. **Recadrage user** : on
  développe en DEV, la prod ne se touche **qu'après validation du DEV**.
- **Annulé proprement** : `git checkout` de `resource/owismind-app` + `body.html` + `git clean` des assets
  neufs -> prod 100% restaurée à l'état déployé (`index-CApWkAm7.js`). Puis **zip DEV** produit via
  `tools/build_dev_plugin.py` (skill `/package-plugin-dev`) -> `owismind_dev-upload.zip` (id `owismind_dev`,
  68 entrées, base `/plugins/owismind_dev/…`, entry `index-BKICdg4x.js`). Vérifié : il embarque pouces
  pleins + Smart/Pro/Claude + flag `modes`.
- L'user a ensuite voulu **récupérer le zip prod** (peur de le perdre) : régénéré **à l'identique de l'état
  déployé** via `git stash` -> package -> `git stash pop` -> `owismind-upload.zip` (entry `index-CApWkAm7.js`,
  backend SANS mes changements, 80 entrées). Les deux zips coexistent ; mes 10 fichiers source intacts.
- **Mémoire gravée** (`~/.claude/.../memory/dev-first-never-touch-prod-artifacts.md`) : en dev, packager
  UNIQUEMENT le DEV ; ne jamais build/package/supprimer la prod tant que l'user n'a pas validé et demandé la
  promotion ; la prod vit dans git (régénérable), jamais « perdue ».

## Décisions
- Pas de « détection 100% auto » du support des modes : techniquement non fiable (seul le code orchestrateur
  OWIsMind interprète le token ; un agent visuel ne le déclare nulle part). -> **toggle admin explicite +
  enforcement backend** (token jamais envoyé si non supporté) = le comportement « intelligent » le plus sûr.
- Flag `modes` **défaut OFF** : après déploiement, le picker est masqué pour TOUS jusqu'à ce que l'admin
  coche la case sur OWIsMind. Voulu (gate « uniquement OWIsMind »).
- Libellés UI changés mais clés internes eco/medium/high gardées -> orchestrateur non impacté.

## Validé / non validé
- ✅ Build, tests (456 back + 124 front), rendus pouces + picker vérifiés au navigateur (clair/sombre),
  zip DEV vérifié (contenu + base).
- ⏳ **NON validé DSS** au moment du log. L'user a re-uploadé le DEV et dit ensuite « ok all good » (cf.
  troubleshooting cache/Development-vs-Uploaded/bonne webapp donné en séance). Considéré OK par l'user en
  fin de session.

## Prochaines étapes
- Quand le DEV est jugé bon : **promouvoir en prod** = `/build-plugin` + `/package-plugin` puis upload +
  **redémarrer le backend** (python-lib changé : `validation.py` + `routes.py`). Pas de recoll d'agent.
- Côté DSS : après upload DEV, **Administration > Agents > Modifier la fiche** sur l'orchestrateur OWIsMind
  -> cocher « gère les modes de réponse » + Enregistrer (sinon picker masqué partout).
- Rappel déploiement DEV : plugin **Uploaded** (un Development même-id ne se met pas à jour par ré-upload :
  supprimer d'abord) ; hard-refresh navigateur ; bien ouvrir la webapp « OWIsMind - AI Agents (DEV) ».
