# Session 2026-07-22 - Premier build AUTO (scenario step-based) + brief design console + diagnostic hub/process

## Objectif
Suite du parcours guide valide la veille : (1) automatiser le run du scenario de build depuis la
webapp (l'user devait le lancer a la main dans DSS) et passer le scenario en step-based, en suivant
STRICTEMENT la doc developpeur Dataiku ; (2) preparer la refonte UX de la console (brief pour Claude
Design) ; (3) comprendre pourquoi le nouvel expert repond dans le Playground mais pas dans la webapp.

## Fait
1. **Scenario step-based** (`project-library/python/owismind_factory/flow_builder.py`) :
   - `ensure_refresh_scenario` cree desormais `create_scenario(name, "step_based",
     {"params": {"steps": [...]}})` : un step visuel `build_flowitem` par dataset de connaissance
     (builds `{type DATASET, itemId, partitionsSpec}`, `jobType RECURSIVE_BUILD`), INACTIF +
     trigger quotidien 03:00 inchange. Read-back `raw_steps` apres creation : echec BRUYANT si les
     steps n'atterrissent pas. `build_scenario_code` (custom python) supprime.
   - Nouvelle `run_scenario_and_wait(ctx, spec)` : run + attente bornee (30 min, poll 10 s,
     progression au journal toutes les 5 min), rattachement a un run DEJA en cours via
     `get_current_run()` (jamais de double run), outcome + erreur lisible via
     `get_details().first_error_details` (repli erreur par step). Outcomes synthetiques
     NOT_FOUND / NOT_STARTED / TIMEOUT / UNKNOWN. `run_scenario_now` supprimee.
2. **Etape guidee `first_build` MANUELLE -> AUTO** (`owismind_factory/guided.py`) :
   - `_run_first_build` : lance le scenario, attend, puis PROUVE les lignes (verdict inconnu tolere
     quand le run est vert ; dataset absent/VIDE = echec avec la recette nommee). Echec scenario =
     problemes FR actionnables (message DSS + onglet Last runs + chemin fix partition) ; TIMEOUT
     explique le rattachement au re-run. Etape relancable (`can_run` accepte failed).
   - `heal_interrupted` etendu : MIGRE un run persiste bloque sur l'ancienne etape manuelle
     (waiting_user) vers l'etape auto relancable (sinon run coince : ni runnable ni verifiable).
   - `_instr_first_build` / `_check_first_build` supprimees ; precheck positif conserve (lignes
     prouvees = etape sautee, pas de re-build inutile).
   - Zero changement backend.py / script.js (front generique par statut, contrat gele).
3. **Decision doc-first** : trigger "dataset modified" NON automatise : sa forme brute n'est PAS
   dans la doc officielle (seuls les triggers temporels ont des helpers documentes) ; a ajouter en
   2 clics dans l'UI du scenario a l'activation. Note dans README console + AGENT_FACTORY.md.
4. **Tests 676 -> 692** (+16) : creation step-based + read-back, `TestRunScenarioAndWait` (succes,
   WARNING, erreurs details/step, timeout borne, rattachement sans double run, NOT_FOUND, fire
   annule), `TestFirstBuildAuto` (avance sur succes, erreur DSS remontee + relance, VIDE echoue,
   timeout, precheck skip, zero tiret banni), migration heal, 2 tests console adaptes
   (profile_review remplace first_build comme etape manuelle type).
5. **Revue adversariale Opus du diff : SAIN** (7 exigences verifiees, pieges chasses : boucles,
   slot de job mutant, caps SQL etat, contrat front). Son seul point (CAPABILITY_MATRIX.md perime)
   corrige. Docs a jour : README console, AGENT_FACTORY.md, CAPABILITY_MATRIX.md.
6. **Commit `1c88695` + PUSH** (demande user explicite, il pull pour DSS).
7. **Brief design remis dans le chat** (pas de fichier repo) : prompt complet pour Claude Design =
   refonte UX de la console (5 ecrans, 13 etapes avec libelles exacts, tous composants/etats,
   contraintes charte Orange + webapp Standard ES5 sans CDN, 6 objectifs UX, livrable maquette
   HTML/CSS autoportee light+dark). Portage ES5 + revue charte a faire quand la maquette arrive.
8. **Terrain user** : 2e domaine cree de bout en bout via l'assistant : `opportunities_won`
   (agent `agent:4Ghpi5hm`, tool `iUR8wLX`). Puis ecart constate : Playground de l'orchestrateur
   repond "j'ai acces aux opportunites gagnees", la webapp repond "pas d'agent pour ca" en
   enumerant revenus/tickets/livraisons (= le registre d'HIER).
9. **Diagnostic (code a l'appui, orchestrateur L1348-1470)** : `capabilities.json` est charge UNE
   FOIS a l'import du module (demarrage du process). Playground Design = process frais (voit la
   nouvelle capability) ; webapp via Mesh = process CHAUD demarre avant l'activation (ne la voit
   pas). "Nouvelle conversation" ne suffit PAS. Deblocage transmis : re-save de l'orchestrateur
   dans DSS. Correctif durable PROPOSE, pas implemente (decision user en attente) : reload du hub
   a chaque debut de conversation avec cache TTL ~60 s + textes de l'etape enable/succes corriges.

## Decisions
- Step-based ancre sur le documente : type `build_flowitem` (docstring get_raw_steps) + modes de
  build du helper in-scenario `BuildFlowItemsStepDefHelper` ; jamais de forme devinee ; read-back
  + preuve e2e par le run auto comme filet.
- `RECURSIVE_BUILD` conserve (semantique de l'ancien `build_dataset` par defaut, valide terrain).
- Trigger dataset-modified : manuel UI (non documente API), pas devine.
- Push actifs cette session (demande user explicite renouvelee : "commit et push").

## Valide / non valide
- VALIDE (tests + revue) : 692 tests OK, revue adversariale Opus sans finding.
- VALIDE (terrain user) : parcours guide complet sur `opportunities_won` (2e domaine).
  Non confirme explicitement : si ce run a utilise le premier build AUTO (depend du re-collage
  library post-pull) ; a confirmer a la prochaine session.
- VALIDE (terrain + code) : la cause Playground vs webapp = chargement du hub au demarrage du
  process. NON FAIT : le correctif durable (reload TTL) et les textes enable/succes.

## Prochaines etapes
- User : re-save `OWIsMind_orchestrator` dans DSS -> nouvelle conversation -> verifier que
  l'expert opportunites repond dans la webapp.
- Decision attendue : implementer le reload du hub par conversation (TTL ~60 s) dans
  l'orchestrateur + corriger les textes enable/succes de l'assistant (option recommandee).
- Refonte UX console : recevoir la maquette Claude Design, porter en ES5/DSS (contrat gele),
  passer charte-orange-reviewer.
- Confirmer en DSS le premier build AUTO (prochain domaine guide) + smoke metier delivery_snapshot
  et opportunities_won (questions reelles, Evidence, non-regression revenus/tickets).
