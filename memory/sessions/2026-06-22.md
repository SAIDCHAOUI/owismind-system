# Session 2026-06-22 - `Solution` retiré du modèle + agents dupliqués par projet DSS

> Session **repo only** (aucune action DSS de ma part). 283 tests verts tout du long, 0 tiret cadratin.
> Détail durable -> `PROJECT_STATE.md` (bloc IDs par projet) + **L099** ; charte/règles inchangées.

## Objectif
1. Comprendre l'impact d'un changement de dataset (lignes +/-, colonne supprimée) sur le modèle sémantique.
2. Mettre à jour le repo après suppression de la colonne `Solution` (le user a déjà mis à jour les modèles
   live DEV + PROD).
3. Clarifier la séparation DEV/PROD : dupliquer la partie agentic par projet, avec les bons IDs, fichiers
   préfixés, et graver le workflow "dev en DEV puis promotion en PROD".
4. Ranger par type (agents/tools/recipes/semantic_model) + versionner la config des modèles en JSON.

## Fait
- **Dérive de dataset (réponse)** : 3 couches. Chiffres toujours live (SQL) ; recipes (profil/value_index/
  value_catalog) s'auto-soignent au re-run ; **le modèle sémantique est la seule pièce manuelle** (ré-index
  pour valeurs neuves ; édition pour colonnes ajoutées/supprimées/renommées).
- **`Solution` retiré du repo** (la colonne n'existe plus ; la suppression n'est PAS un simple `.replace`) :
  - `OWISMIND/.../semantic_model/build_aligned_semantic_model.py` (hiérarchie `SolutionLine > Product` +
    **retrait structurel** de l'attribut + des 2 metrics `Solution`) et `update_aligned_semantic_model.py`
    (`NEW_INSTRUCTIONS` byte-identique) + golden query #4 ; `MODEL.md` + `tools/semantic_model/README.md`.
  - **Prompts des agents** : `SalesDrive_revenue_expert.py` (5 occurrences dont le prompt actif
    `AMBIGUOUS OFFER TERM`) + copie `TroubleTickets_expert.py` (2 commentaires hérités), à l'identique.
  - **Docs** : `dataiku-agents/{CLAUDE,README,DATASETS,recipes/README,tools/README}.md` (hiérarchie +
    compteurs **20 -> 19 cols**). Orchestrateur = déjà propre (pas de hiérarchie d'offre).
  - **Nouveau script** `semantic_model/drop_column_and_reindex.py` (DRY_RUN, retrait structurel + ré-index,
    asserts) pour appliquer ce genre de changement au modèle live.
- **Agents DUPLIQUÉS PAR PROJET (L099)** : `dataiku-agents/` -> `OWISMIND/{OWISMIND_DEV, OWISMIND_PROD_V1}/`,
  une copie complète + auto-suffisante par projet, **fichiers préfixés**, **en-tête deploy-target**, IDs du
  projet câblés. Script de migration jetable avec **un `assert` par remplacement d'ID** (0 contamination :
  PROD n'a ni `bHrWLyOL`, ni `v4oqA6R`, ni `tickets_expert`, ni `OWISMIND_DEV`). PROD **sans tickets**
  (pas fini en DEV). Capability tickets retirée de l'orchestrateur PROD par slice entre 2 ancres.
- **Rangement par type** : sous-dossiers `agents/ · tools/ · recipes/ · semantic_model/` + `registry.json`
  par projet. Tests repointés (`agents/`+`tools/`, DEV). Liens relatifs des READMEs portés corrigés.
- **Config des modèles en JSON** : `semantic_model/<ModelName>.v1.json` (sortie `dump_*`). Le user a collé
  les dumps DEV (revenus + tickets) ; j'ai retiré le préambule d'impression `Versions:/Version active:`
  pour que le JSON soit valide (revenus 3 entités/9 golden ; tickets 1 entité/0 golden). PROD = placeholder.
  Champ `config_json` ajouté aux registres.
- **Registres + carte des IDs** : `OWISMIND/README.md` (workflow DEV->PROD + table d'IDs) + 2 `registry.json`.
- **Mémoire + instructions** : `CLAUDE.md` racine (ligne agents) ; `dataiku-agents/CLAUDE.md` réécrit (+ règle
  #9 "DEV before PROD") ; `PROJECT_STATE.md` (bloc IDs par projet) ; `CONTEXT.md` (bandeau focus) ; **L099**.
- **Nettoyage** : ancienne structure plate supprimée (`agents/`, `recipes/`, `tools/`, `registry.json`),
  script de migration supprimé, pas de `__pycache__`.

## Décisions
- **Duplication complète par projet** (vs moteur partagé) : choix user ("les deux auront la même chose"),
  colle au workflow de promotion (éditer DEV -> porter dans le jumeau PROD avec les IDs prod).
- **PROD sans tickets** : tickets pas fini en DEV ; `tickets` reste un domaine non-staffé -> gap honnête.
- **JSON de config non pré-rempli depuis le paste antérieur** : le paste datait d'avant le retrait de
  `Solution` ; un `dump` frais = source juste (et 1500 lignes non reproductibles à la main sans risque).
- Modèle sémantique dédié par domaine déjà acté (L098) ; rien changé là-dessus.

## Validé / non validé
- **Validé (local)** : 283 tests verts, compile des 31 fichiers, 5 JSON valides, 0 contamination croisée
  (asserts + greps), 0 tiret. Le user a confirmé "les modèles sémantiques sont à jour en DEV et en PROD".
- **Non fait / DSS** : re-coller le sous-agent revenus pour que le prompt sans `Solution` prenne effet ;
  re-runner les 3 recipes ; peupler le JSON de config PROD via `dump`.

## Prochaines étapes
- **DSS** : re-coller `OWISMIND_DEV_SalesDrive_revenue_expert.py` (et le jumeau PROD à la promotion) ;
  re-runner recipes ; coller le dump PROD dans `Drive_Revenues_Model.v1.json`.
- **Sweep doc restant** (anciens chemins `dataiku-agents/agents|tools|recipes/`) : `project-documentation/
  05-agents/*`, `docs/scaling/PLAN_AGENTS.md`, skill `agentique-python-dataiku`. (Signalé, non fait.)
- **Tickets** : finir en DEV (PLAYBOOK), puis promouvoir en PROD avec les IDs prod.


# Session 2026-06-22 (Run 2) - Modèle sémantique tickets ultra-explicite + DEBUG "agent invente le nom / 0 ligne"

## Objectif
Rendre le modèle sémantique tickets et le sous-agent vraiment experts (comme les revenus) : 21 colonnes
ultra-décrites, calcul sur `id` unique (snapshots dupliqués), LD (`Service_id_1`) au premier plan, dates,
identité client, valeurs exactes (pas d'ILIKE), couverture (année courante + 3 ans, 1er du mois). Puis
DEBUG d'un échec DSS réel : « combien de tickets algerie telecom cette année » -> le modèle a **inventé**
`'Algérie Télécom Mobile'` (vrai = `ALGERIE TELECOM SPA`) et filtré `Latest_Closed_Date` -> **0 ligne** ;
+ remplir la "Description for LLM" du tool oubliée. Méthode demandée : **sous-agents d'audit/critique**.

## Fait
- **Cerveau tickets** (`semantic_model/update_tickets_semantic_model.py`) réécrit : instructions ~9.8k
  (1 table, **snapshots dupliqués -> COUNT(DISTINCT id) direct + dernier snapshot via `DISTINCT ON (id) ...
  ORDER BY lastUpdate DESC` pour l'état courant**, LD = `Service_id_1` dominant, exact-value/anti-ILIKE,
  **anti-fabrication**, dates **DEFAULT creationDate** / `Latest_Closed_Date` clôture only, identité GROUP
  BY Customer_id + display Account_name, divulgation comptes liés sans fusion, durée en **minutes**) +
  injecte aussi **entité + 21 descriptions de colonnes + métriques `COUNT(DISTINCT id)`** + **11 golden
  queries** (patterns génériques, placeholders `EXAMPLE CUSTOMER` / `LD000123`).
- **DEBUG racine (2 sous-agents Opus audit + 1 critique adverse)** : le profileur écrivait `indexed=False`
  pour TOUTE colonne -> prompt UNDERSTAND « labels of: (none) » -> terme client jamais extrait -> grounding
  sauté -> le modèle sémantique reçoit la question brute et **devine**. **Fix RC1** : `profile_dataset_recipe.py`
  **dérive `indexed`** via `should_index_value_column` (parité exacte avec `build_value_index_recipe`).
  **Fix RC2** (trouvé par le critique) : le profileur élisait `Latest_Closed_Date` (tri alpha) -> ajout
  `time_name_rank` (création/ouverture > clôture/maj) -> `creationDate` gagne. Miroir PROD (recettes
  identiques). **+3 tests** (parité index, élection temps) -> **286 verts**.
- **Anti-fabrication** ajoutée aussi au cerveau **revenus** (en plus du no-ILIKE de la 1re passe) +
  question ILIKE de l'user traitée (revenus faisait `ILIKE '%...%'`).
- **"Description for LLM"** (oubliée) : nouveau `semantic_model/TOOL_DESCRIPTIONS.md` (blocs tickets +
  revenus prêts à coller) + référencé registry/README/PLAYBOOK.
- **Cleanup** : `dump_tickets_semantic_model.py` supprimé (fusionné dans `dump_semantic_model.py`
  générique). Doc mises à jour (PLAYBOOK §2 overrides : `COUNT_DISTINCT` sur id, time=creationDate auto,
  display Customer_id->Account_name, synonymes LD ; README ; registry `metric`/`tool_description`).
- **Dé-fixette** (retour user) : retrait des exemples qui recopiaient le cas de test (golden de la
  question exacte supprimée, nom de société -> placeholder, exemples en prose généralisés) ; les fixes
  restent **structurels/généraux**.
- **`.v1.json` restaurés à TON dump** (retour user : le json est un **dump à sens unique**, entrée pour
  moi, pas consommé à l'update ; source de vérité du cerveau = le **script**). Restaurés à `6d813c6`
  (0 golden, `COUNT(id)`). Je ne touche plus aux `.v1.json`.

## Décisions
- Le moteur du sous-agent reste **inchangé** (corps gelé byte-identique) : l'expertise tickets vit dans
  le **modèle sémantique** + les **overrides de profil**, pas dans le code agent.
- Fix RC1/RC2 **dans le profileur** (générique, vaut pour tout dataset) plutôt qu'en overrides manuels
  fragiles ; overrides humains gardent la priorité.
- Ne PAS indexer Account_name côté modèle sémantique (cap 1000, haute cardinalité) : le grounding passe
  par le value_index (sous-agent) + `attribute_lookup` (orchestrateur).
- `.v1.json` = dump (je lis, je n'écris jamais).

## Validé / non validé
- **Validé local** : 286 tests, parité `should_index_value_column` vs value_index (6+ cas), élection temps
  `creationDate`, compile, 3 JSON valides, 0 tiret cadratin.
- **NON validé DSS** : tout le comportement réel (extraction du terme en mode eco, grounding, SQL généré)
  est intestable hors instance. L'échec d'origine venait du DSS.

## Prochaines étapes (DSS, ordre important)
1. **Re-runner la recette de profilage tickets** (INDISPENSABLE : active `indexed` ; sans ça le bug
   "devine le nom" revient).
2. Overrides profil (PLAYBOOK §2) : `COUNT_DISTINCT` sur `id`, display `Customer_id->Account_name`,
   synonymes LD (time=creationDate désormais auto).
3. Exécuter `update_tickets_semantic_model.py` (modèle `dM4jA4G`), tester Playground, **re-dumper** le
   `.v1.json`.
4. Coller la **"Description for LLM"** (`TOOL_DESCRIPTIONS.md`) dans le tool `tickets_semantic_query`.
5. Re-coller le sous-agent tickets (env 3.11) ; **tester en mode eco** (là où ça a cassé).
6. (revenus) re-runner `update_aligned_semantic_model.py` (no-ILIKE + anti-fabrication), re-dumper ;
   corriger en UI DSS les 3 tirets cadratins des descriptions d'entités revenus.
- **Limite "ATM MOBILIS"** : pas de colonne groupe dans les tickets -> l'agent ne peut pas deviner
  l'appartenance au groupe Algérie Télécom. Option future : fournir un mapping de groupe (glossaire/catalogue).
