# Session 2026-06-16 — Optimisation modèle-agnostique : nativité artefacts, streaming, escalade 3 modes, KPI

## Run 4 (soir) — AUDIT agents + langue / messages live / conscience écran / escalade pilotée + timeline UX + fix mini (⏳ CODÉ + 674 tests + revues, à valider DSS — L066-L070)

**Objectif** : audit complet des agents (orchestrateur + sous-agent : routage, tools/artefacts, prompts)
puis implémenter les 6 priorités user — (1) langue de réponse fiable, (2) réponses live ChatGPT-style,
(3) en vrais messages, (4) plus de narrate-and-stop, (5) conscience de l'écran, (6) escalade éco-first
pilotée par le modèle. Puis 2ᵉ retour user : (7) le **mini ne marche pas du tout** (narrate-and-stop) et
(8) la **timeline est surchargée/moche** (fenêtre max-5 cassée + narration redondante).

**Méthode** : 2 workflows multi-agents — audit 7 dimensions (8 agents) puis revue adversariale 4 dimensions
+ revue sécurité Dataiku 4 dimensions. Skills : `agentique-python-dataiku`, `frontend-design`.

**Fait** (tout codé + testé local) :
- **Langue (L066)** : `context.detect_prompt_language` (word-boundary), `build_user_suffix` appendé en FIN,
  tokens `⟦owi:mode⟧⟦owi:lang⟧` lus en DERNIER (anti-spoof user), `_strip_context_block`, frontend
  `webapp_lang`, sous-agent `forced_language`. Fichiers : `context.py`, `routes.py`, `stream_manager.py`,
  `backend.js`, `useChatStream.js`, `chat.js`, orchestrateur, sous-agent.
- **Messages live (L067)** : préambule modèle via `_txt`/answer_delta (vrai message persisté) ;
  `timelineBodyItems` whitelist.
- **Narrate-and-stop + mini (L067)** : prompt **ACT-FIRST** (revert de l'emphase narration = régression
  L063 réintroduite) ; garde `_looks_like_premature_stop` + **nudge** + **AUTO-escalade Sonnet** si échec.
- **Escalade pilotée (L068)** : `LoopChat` (transcript-replay, pairing préservé) + outil `escalate_to_expert`
  (mini éco/medium) + `switch_model` + **rôles alternés** (bloqueur 400 Vertex corrigé) + verrou one-way ;
  `pick_loop_llm` simplifié, `complexity_is_high` supprimé.
- **Conscience écran (L069)** : `build_screen_state` + `_build_screen_block` (owner-scopé, gaté `open`) +
  `_sanitize_screen_context` + frontend `screen_context`.
- **Timeline UX (L070)** : `timelineSegments` ignore la narration → events fusionnés → **max-5 globale OK** ;
  narration grise supprimée ; hiérarchie (done en recul, running dominant). `MessageAgent.vue`,
  `timelineModel.js`, `timelineSteps.js` (event `ESCALATING`).

**Décisions user (AskUserQuestion)** : escalade = hand-over mid-tour **avec synthèse** ; Éco **peut**
s'auto-escalader ; conscience écran = **hybride** (backend + pointeur frontend).

**Revues** : audit (7 dim) → plan ; revue adversariale → **1 bloqueur corrigé** (escalade sans préambule =
2 tours `user` → 400 Vertex) + 4 should-fix appliqués ; revue sécurité Dataiku → **« safe-to-ship »**
(SQLi/ownership/instance/prompt-injection OK) + 1 LOW appliqué (lecture gatée sur panneau ouvert) +
**1 anti-spoof proactif** (token autoritaire lu en dernier).

**Validé / non validé** : 674 tests verts (179 agents + 381 backend + 114 frontend) + build Vite + zip.
Revues sécu/adversariale OK. **NON validé DSS** (rendu visuel timeline + comportement mini intestables
hors instance).

**À déployer** : recoller les **2 Code Agents** (env 3.11) + uploader le zip (**77 entrées,
`index-CT0YQv_u.js`**) + **redémarrer le backend**. Smoke-tests : EN puis FR (2ᵉ répond FR) ; mini sur
question revenus (doit appeler l'outil ou auto-escalader, pas s'arrêter) ; « explique ce graphique » ;
timeline = max-5 qui défile, gris/orange ; escalade = vrai message + event `ESCALATING`.

**Différé** : robustesse multi-spécialistes (chart au bon dataset) — sans valeur tant qu'il n'y a qu'un agent.

---

## Clôture (fin de journée) — ✅ VALIDÉ DSS par l'user : « super all good ça marche pas mal »
Le système Run 2 + Run 3 (narration live **écrite par le modèle**, orchestrateur boucle simple, graphique
qui s'affiche, Evidence lazy loading, modes Éco/Medium/High) est **validé en DSS** — l'arc complet
2026-06-16 passe de ⏳ à ✅. L063-L065 confirmées DSS. Reste optionnel (non urgent) : sélecteur SQL
multi-résultats, durcir la narration sur mini si souhaité. Session fermée via `/log-session`.

---

## Run 3 (jour) — RETOUR DSS « ça marche beaucoup mieux » → narration live + Evidence lazy + nettoyage (L065)
**Retour DSS** : Run 2 validé (« beaucoup mieux »). Nouvelles demandes : (1) **messages intermédiaires
au fil de l'eau** sur n'importe quel modèle (« je suis là comme un con à attendre, je vois que des event
kinds toujours les mêmes ») ; (2) **Evidence « Explore source data »** : ≥20 lignes, toutes colonnes,
scroll, **lazy loading**, sélecteur si plusieurs tables (« parfois 2 lignes seulement ») ; (3) nouvel id
sous-agent `agent:bHrWLyOL` ; (4) **renommer** les fichiers (orchestrateur `OWIsMind_orchestrator`,
sous-agent `SalesDrive_revenue_expert`) ; (5) **nettoyage repo** surtout la partie agents ; (6) sous-agents
de MON workflow = **Opus uniquement**.

**Fait :**
- **Narration live (L065)** — mécanisme : events `NARRATION` → backend event `narration` **transient**
  (live, jamais persisté), rendus en flux par `MessageAgent` (ligne discrète, point pulsé), **nettoyés en
  vue terminale**. **Run 3 bis (retour user : "c'est le MODÈLE qui doit narrer comme ChatGPT, pas du
  hard-code")** : on streame désormais le **préambule que le modèle écrit lui-même** (`resp.text` du tour
  avec tool-calls → `state.preamble` → streamé) ; le prompt l'**invite doucement** (étape 0, OBLIGATION
  tool-call sur le même tour, « jamais finir avec juste la phrase » → anti narrate-and-stop) ; le texte
  déterministe `_NARR` n'est plus qu'un **FILET** (si le modèle n'a rien écrit). Phases internes du
  sous-agent restent déterministes (l'orchestrateur ne les voit pas). 0 appel LLM en plus. Réponse à
  l'hypothèse user : **oui, possible en LangGraph** (et c'est bien le modèle qui narre quand il coopère).
- **Evidence « Explore source data »** (sous-agent **Opus** en arrière-plan) — bug « 2 lignes » = collapse
  flex → conteneur scroll borné (`max-height min(60vh,480px)`, `min-height 220px`) ; **lazy/infinite
  loading** (IntersectionObserver, append, cap 500, stop sur `has_more=false`) ; toutes colonnes + en-tête
  collant + scroll H/V ; **sélecteur multi-tables** (`/evidence/meta.sources`, param `table` côté rows,
  matché au set du SQL). +8 tests.
- **Renommage** : `OWIsMind_orchestrator.py` + `SalesDrive_revenue_expert.py` (= noms des Code Agents) ;
  loaders de tests re-pointés ; `agent_id` → `agent:bHrWLyOL`.
- **Nettoyage repo** : supprimés `orchestrator/` + `salesdrive/` (v2 legacy + CSV de traces) et les
  versions linéaires v3 `*_agent.py` + `test_orchestrator_v3.py` (dead code, **préservés en git**) ;
  entrée registre dormante `salesdrive_v2` retirée ; `test_dataset_expert` re-pointé sur le fichier actif
  (stub langgraph ajouté ; 2 assertions périmées du vieux format `build_semantic_question` mises à jour).
  Docs (CLAUDE.md racine + dataiku-agents/CLAUDE.md + README) alignées sur les nouveaux noms.
- **633 tests** (154 agents + 366 backend + 113 frontend) + vite OK + build + zip (**77 entrées,
  `index-BM3sFZCq.js`**). **À recoller LES 2 Code Agents** (`OWIsMind_orchestrator`,
  `SalesDrive_revenue_expert`) + upload zip + redémarrer backend. Détail → **L065** (et L063-L064 pour Run 2).

---


## Run 2 (jour) — RETOUR DSS USER : « rien ne marche » → refonte de la refonte (L063-L064)
**Retour DSS** : en Éco/Medium le modèle **narrait puis s'arrêtait** (« Je consulte l'expert revenus… »
= fin), seul High enchaînait ; **bridé** par le hard-code du graphique ; **lent** (2 min) ; **graphique
vide** (« Result not kept ») ; demande d'un **sélecteur SQL** quand plusieurs SQL ; « si tu utilises des
sous-agents, **Opus** uniquement ; **résultats + perf d'abord** ».

**Corrigé (Run 1 = sur-ingénierie qui cassait) :**
- **Narration-first SUPPRIMÉE** (cause du narrate-and-stop sur petit modèle, L063) + prompt « HOW TO WORK »
  revenu à : ROUTE → ASK-FOR-EVERYTHING-AT-ONCE / **parallèle jamais série** → PRESENT (render libre +
  rédige l'analyse).
- **Synthèse séparée SUPPRIMÉE** → le modèle rédige la réponse dans la boucle (1 passe en moins ≈ 15 s).
- **Indice de rendu forcé SUPPRIMÉ** → invite légère non prescriptive (le modèle choisit chart/colonnes).
- **Modes = choix du modèle de boucle** (`pick_loop_llm`) : Éco/Medium=mini, High ou Medium+complexe=Sonnet
  (heuristique sur la question) ; plus de `synth_llm_id`/`_stream_synthesis`/`_synthesis_briefing`.
- **Graphique vide CORRIGÉ** (L064) : sous-agent `n_query` — résultat attaché au **dernier** span SQL
  (`i==last_i`, pas `i==0`) ; `evidence/service._load_sql_item` préfère le dernier item réussi **avec
  résultat**. **Perf** : appel LLM « headline » du sous-agent coupé (`SUBAGENT_LLM_HEADLINE=False`, ~29 s).
- **Revue Opus** (1 agent, modèle opus) : aucun bloqueur ; durci le cas-limite plafond-de-boucles (réponse
  vide + données présentes → pointe vers le panneau au lieu du message d'échec).
- **À RECOLLER LES 2 Code Agents** : `orchestrator_langgraph.py` ET `dataset_expert_langgraph.py` (env 3.11)
  + upload zip (Evidence) + redémarrer backend. **Sélecteur SQL multi-SQL = différé** (la capture corrigée
  règle déjà le « not kept » ; le sélecteur = confort, l'user l'a classé non-urgent).
- 637 tests verts (168+360+109). Streaming token-par-token de la réponse finale = **abandonné** (cassait ;
  les labels d'étapes live suffisent comme feedback). Détail → **L063-L064**.

---

## Run 1 (nuit) — construction initiale (L060-L062)
> ⚠️ Run 1 a sur-optimisé et CASSÉ le comportement DSS (voir Run 2). Conservé ci-dessous pour l'historique.

> Session autonome (nuit, user endormi). Mandat : rendre le système **performant même sur petit
> modèle** (gpt-5.4-mini), indépendant d'un gros modèle ; nativité des tools de visualisation ;
> streaming + narrations ; escalade coût/qualité ; KPI/Evidence. Orienté résultat (build + zip faits).

## Objectif
Génie logiciel d'abord, modèle ensuite : que l'orchestrateur **utilise nativement** les artefacts
webapp (charts/tables/KPI) au lieu de recopier des tables markdown, qu'il **streame** (narration +
réponse au fil de l'eau), qu'il **escalade** vers Sonnet seulement quand utile (3 modes Éco/Medium/High
choisis par l'user), et améliorer Evidence Studio. Le tout testé local + buildé + zippé pour validation
DSS au matin.

## Décisions clés
- **Streaming = pas de pari sur l'API.** Le SDK Mesh streamé n'expose PAS de preuve qu'il rend les
  `tool_calls` ; je **ne touche pas** au mécanisme `execute()` de décision d'outils (cœur validé).
  Streaming livré par : (1) narration de **préambule** (le texte que le modèle écrit avant un tool-call,
  jusqu'ici jeté, est streamé live) ; (2) **synthèse finale dans un nœud dédié** `execute_streamed()`
  **sans tools** → vrai streaming token par token, fiable (pattern du linéaire d'origine, validé).
- **Nativité par ARCHITECTURE, pas par espoir de prompt** (L060) : le sous-agent renvoyait
  `headline + table markdown` → le petit modèle recopiait la table. Désormais l'orchestrateur hand au
  modèle `headline (table strippée) + DATA compacte JSON + RENDERING HINT` (tool + chart_type + colonnes,
  dérivé de l'intent). Le modèle n'a plus de table à copier → il appelle `show_chart/show_table/show_kpi`.
  **+ filet de sécurité déterministe** : données ≥2 lignes non rendues → **auto-tableau**.
- **Escalade conservatrice 3 modes** (réponse user) : `Éco`=mini seul ; `Medium`=mini + escalade auto de
  la **synthèse** (étape qualité) vers Sonnet sur complexité (heuristique ≥2 signaux) ou échec ;
  `High`=Sonnet partout. Le routing/tool-decisions restent sur mini même en Medium (cheap). Sélecteur
  manuel dans la barre de saisie + plomberie token `⟦owi:mode=…⟧` (prefix → orchestrateur parse+strip).
- **`strict:True` retiré des tool specs** (blocker revue, L061) : OpenAI strict exige toutes les
  propriétés en `required` (optionnelles nullable). Mes specs avaient `title/style/delta` optionnels hors
  `required` → 400 à chaque tour si Mesh forwarde `strict`. Retour exact au pattern validé ; la nativité
  ne dépend pas de `strict` (architecture + descriptions + few-shot suffisent).

## Fait (tout codé + testé local + build + zip)
**Orchestrateur** (`dataiku-agents/agents/orchestrator_langgraph.py`, +~500 l) :
- prompt « OUTPUT CONTRACT » (jamais de table markdown ; toujours show_* ; commenter pas recopier) +
  narration de préambule + split gather/synthesis ; few-shot dans les descriptions de tools.
- `_subagent_tool_output` (strip table + DATA + hint), `_rendering_hint` (intent→tool/chart/colonnes),
  `_strip_markdown_tables`, `_compact_data_block`, `_synthesis_briefing`.
- nœud `node_finish` → **synthèse streamée** (`_stream_synthesis`/`_run_synthesis_call`) avec
  escalade-on-failure ; modes (`parse_mode`, `loop_llm_id`, `synth_llm_id`, `complexity_is_high`).
- nouvel outil **`show_kpi`** + artefact `kind:"kpi"` ; `ESCALATION_LLM_ID = …vertex_ai/claude-sonnet-4-6`.
- **Sous-agent (`dataset_expert_langgraph.py`) NON touché** (rollback intact) — seul l'`intent` de son
  `AGENT_RESULT` est désormais capté par l'orchestrateur.

**Backend** (`python-lib`) : token mode dans `context.build_user_prefix(mode=…)` + lecture
`body.mode` (whitelist serveur, défaut medium, jamais stocké) ; KPI dans `streaming._normalized_artifact_event`,
`storage/artifacts._sanitize`, `stream_manager` (persistance), `evidence/chart_payload.build_kpi_payload`,
route `/evidence/meta`.

**Frontend** : `ModelModePicker.vue` (segmenté Éco/Medium/High, roving tabindex + flèches) dans la
PromptBar ; `ArtifactKpi.vue` (carte grand chiffre + delta) + onglet KPI dans `EvidencePanel` ;
`ArtifactChart` style `stacked` ajouté (donut/aire/groupé/horizontal déjà gérés) ; **`sqlPretty.js`**
(format + coloration SQL sûre, tokens échappés) dans `EvidenceSql` ; store `ui.modelMode` + plomberie
`startChat(mode)` → `/chat/start`. i18n fr+en (KPI, modes).

**Tests** : +13 agents (170), +12 backend (360), +7 node (109) = **639 verts**. py_compile + vite OK.
**Build + zip refaits** : zip **77 entrées, `index-B015Ius_.js`**.

## Revue adversariale (3 sous-agents : Opus orchestrateur, Sonnet backend+sécu, Sonnet frontend)
- **1 BLOCKER** (strict-mode) → corrigé. **MEDIUM** : `value_raw` KPI non borné → retiré (inutilisé front).
  **MEDIUM** : a11y picker → roving tabindex + flèches. **LOW** : `latest_intent` mort → retiré ; token
  mode cross-turn → strip défensif sur **chaque** message rejoué. **NIT** : `LEFT` dupliqué + guillemet
  isolé dans sqlPretty → corrigés. Verdicts backend/frontend « safe to ship » ; orchestrateur OK après fix.

## Validé / non validé
- ✅ Local : 639 tests, py_compile, vite build, zip clean, rollback intact, revue passée.
- ⏳ **NON validé DSS** (user endormi, pas d'accès instance) : le flux streamé réel + escalade + nativité
  doivent être confirmés sur l'instance. Recoller les **2 Code Agents** ? → **orchestrateur seulement**
  (`agent:MODpGFcC`? non — l'orchestrateur LangGraph est le Code Agent actif ; recoller
  `orchestrator_langgraph.py`). Le sous-agent `dataset_expert_langgraph.py` est **inchangé** (rien à
  recoller). Upload zip + **REDÉMARRER backend** ; `reasoning=high` déjà réglé.

## Prochaines étapes (validation DSS au matin)
1. Coller `orchestrator_langgraph.py` dans son Code Agent (env 3.11) ; uploader le zip ; redémarrer backend.
2. Smoke-tests : (a) « revenus YTD EVPL actuals vs budget » → narration live + KPI/chart dans le panneau,
   pas de table markdown dans le chat, réponse streamée ; (b) top clients → tableau natif ;
   (c) question complexe en Medium → vérifier l'escalade Sonnet ; (d) toggle High/Éco.
3. Confirmer l'id Mesh Sonnet `…vertex_ai/claude-sonnet-4-6` (sinon éditer `ESCALATION_LLM_ID`).
4. Si l'orchestrateur recopie encore une table : durcir le prompt / le filet (déjà déterministe).
5. (Diff.) narration texte du sous-agent lui-même (aujourd'hui = labels de blocs live, suffisant).

---

## Run 5 (session « salle de sport », autonome) — MODEL-AGNOSTIC : fin de l'escalade + Dataset Lookup + stop optimiste + hiérarchie timeline

### Objectif
Le test DSS du Run 4 a échoué : **gpt-5.4-mini escalade quasi systématiquement** (même « combien avec
le client X » → bascule + message hardcodé) et **quand il n'escalade pas, il narre puis s'arrête** (pas
de réponse finale). Demande user : abandonner gpt-5.4-mini → **Gemini 2.5 Flash** (orchestrateur) +
**Sonnet** (master), **arrêter tous les hacks mono-modèle**, viser une architecture optimale qui tourne
**même sur les petits modèles**. + fixer le **bouton stop** (latence 5-6 s ressentie), réserver le SQL aux
calculs et utiliser le tool **Dataset Lookup** pour les recherches simples, renforcer le **streaming de
vrais messages** et la **hiérarchie d'events** (descente vers le sous-agent).

### Fait
- **L071 — escalade SUPPRIMÉE en entier** (`OWIsMind_orchestrator.py`) : retirés tool `escalate_to_expert`
  (+ spec), `can_escalate`, `ESCALATION_LLM_ID`, `_ESCALATE_MSG/_HANDOVER`, `LoopChat.switch_model`,
  `_tool_specs_esc`, l'auto-escalade de `node_agent`, le hand-over de `node_tools`, l'event `ESCALATING`,
  le label `_L["escalating"]`. Nouveau `LOOP_LLM_BY_MODE = {eco:Flash, medium:Flash, high:Sonnet}` +
  `GEMINI_FLASH_ID`/`SONNET_ID`/`GPT_MINI_ID` ; `pick_loop_llm(mode)` rend 1 modèle/tour. Prompt :
  ACT-FIRST conservé **+ « NARRATE AS YOU GO »** (narration naturelle = vrai message persisté). Nudge
  narrate-and-stop conservé (model-agnostic). Sous-agent : `UNDERSTAND_LLM_ID`/`SQLGEN_LLM_ID = GEMINI_FLASH_ID`.
- **L072 — Dataset Lookup** (`SalesDrive_revenue_expert.py`) : config `DATASET_LOOKUP_TOOL_ID="9FEzVZk"`,
  intent **`lookup`** (UNDERSTAND : description + champ `attributes` + bloc « ALL COLUMNS ») ; helpers
  `build_lookup_filter` (EQUALS / OR même colonne / AND colonnes ≠ / None si vide / dédup),
  `extract_lookup_rows` (tolérant `output.rows` dicts OU `{columns,rows}` listes), `lookup_note`,
  `MyLLM._lookup_rows` (projection cols filtre+attributs, dédup, cap, None si 0). **Schéma LIVE** :
  `_read_live_columns` (`read_schema`, O(1)) → `Profile.live_columns` ; `attribute_columns`/`match_attribute`
  résolvent une colonne hors profil (ex. account_manager). `n_query` : branche lookup (return si succès/vide,
  démotion `custom` + fallback SQL si erreur/aucun filtre), span gelé `semantic-model-query` (sql = note).
  `validate_understanding` : résolution `attributes` + démotion lookup→custom (pas de terms / tool désactivé).
  `HEADLINE_LOOKUP` ; `KNOWN_BLOCK_IDS += "lookup"`, `KNOWN_TOOL_NAMES += "dataset_lookup"` + registre orchestrateur synchronisé (anti-drift).
- **L073 — stop optimiste** (`stores/chat.js`) : `stopGeneration()` applique `applyEvent(activeVersion,
  {type:'stopped'})` immédiatement (partiel figé + spinner off + token annulé), POST `/chat/stop` best-effort
  en fond. Suivi `activeVersion` ajouté (module-scope, nettoyé `finally`/`cancelActive`).
- **L074 — hiérarchie timeline** (`MessageAgent.vue`) : helper `isSubStep` (eventKind `SUB_AGENT_*`) →
  classe `.sub-step` (indent + bordure `var(--border)`) sur ticker live + activité terminale. Additif (reducer/ids/signature intacts).
- **Build + package** : `npm run build` → `index-CZu1PIfv.js` ; `body.html` recâblé (Write, `cp` refusé F10) ;
  zip **77 entrées** propres (frontend/node_modules/CLAUDE.md/__pycache__ exclus, fichiers requis présents).
- **Tests mis à jour** : `TestModelIds` (modèle par mode), `TestNoEscalation` (tool/section/symboles absents),
  `TestModelMode`, `TestLoopChat` (appariement sans switch_model) ; nouveaux `TestLiveSchemaAttributes`,
  `TestLookupFilter`, `TestExtractLookupRows`, `TestLookupRows`, `TestLookupUnderstanding`.

### Décisions
- **Abandon total de l'escalade en cours de tour** (recommandation assumée) : c'était la cause directe des
  2 bugs + un hack mono-modèle contraire à l'objectif. Mode = choix d'**un** modèle pour tout le tour.
- **gpt-5.4-mini retiré des défauts** (gardé en option nommée `GPT_MINI_ID`). Défaut = Gemini Flash + Sonnet.
- **Dataset Lookup** réservé aux lectures d'attribut ; SQL pour agrégations/calculs. Schéma auto-découvert (id seul).
- **Stop = optimiste** (le stream LLM Mesh n'a aucune API d'annulation ; le backend ne coupe qu'entre 2 chunks).
- **Persistance de la chaîne d'events au reload = DIFFÉRÉE** (touche persistance+reload, risque, non validable hors DSS).

### Validé / non validé
- ✅ **Local** : 193 tests agents + 114 tests frontend verts ; build Vite + compile-check ; zip propre 77 entrées.
  Revue adversariale manuelle des chemins risqués (flux lookup, clés d'état, ordre des blocs, aucune réf morte).
  Revue par subagent tentée 2× mais **API 529 (surcharge serveur)** → revue manuelle à la place.
- ❌ **NON validé DSS** (intestable hors instance) : comportement réel des modèles, Dataset Lookup réel, stop ressenti, rendu timeline.

### Prochaines étapes (validation DSS)
0. **VÉRIFIER `GEMINI_FLASH_ID`** dans les 2 fichiers (best-guess `…:vertex_ai/gemini-2.5-flash`) = id exact de la connexion Mesh.
1. **Recoller LES 2 Code Agents** (env 3.11) ; **upload zip** (`index-CZu1PIfv.js`). Backend python-lib **inchangé → pas de redémarrage**.
2. Smoke-tests : (a) « combien avec le client X » en Éco/Medium (Flash) → appelle l'outil, étapes intermédiaires,
   jamais d'escalade ni de message hardcodé, ne s'arrête pas ; (b) « account manager des 3 premiers clients » →
   Dataset Lookup (pas de SQL monstrueux) ; (c) EN→FR ; (d) bouton stop instantané ; (e) steps sous-agent indentés.
3. Si l'id Gemini est faux → l'agent ne répond pas (corriger 1 ligne). Si Flash narre trop/pas assez → ajuster le prompt.
4. (Diff.) persistance de la chaîne d'events au reload.

### Run 5b — ajustements après retour user (L075)
- **Modèles par mode revus** : **Éco = gpt-5.4-mini GARDÉ** (marche bien, ~gratuit), Medium = Gemini Flash,
  High = Sonnet. `LOOP_LLM_BY_MODE` (orchestrateur) + `LLM_BY_MODE` (sous-agent) ; mode propagé via
  `context_msg` (`MODE:`) → `forced_mode` → `pick_subagent_llm` threadé dans l'état (concurrency-safe). En
  **High = Sonnet partout** (orchestrateur + sous-agent). Hook `SEMANTIC_TOOL_ID_BY_MODE` pour le modèle sémantique.
- **Narration séparée** : section « NARRATE AS YOU GO » détachée du prompt, ajoutée seulement si
  `narration_enabled(mode)` (≠ eco). Le mini ne narre plus pendant le tool-call (anti narrate-and-stop) → ticker
  déterministe. ACT-FIRST partout.
- **Stop = attente + feedback** (revert de l'optimiste) : flag `stopping` sur la version (nettoyé sur tout
  terminal) ; `stopGeneration` pose `stopping=true`, POST `/chat/stop`, **garde le polling** ; `MessageAgent`
  affiche un spinner + « Stopping… » clignotant (reduced-motion respecté).
- **Popup de mode** : `ModelModePicker.vue` réécrit (pilule → `Modal` : 3 cartes libellé/description/jauge coût
  €/€€/€€€/badge Recommandé + encart enveloppe 50 €/mois). i18n `mode.*` enrichi (fr+en), hints corrigés. Medium défaut.
- **Vérifs** : 199 tests agents + 116 frontend verts + build Vite + zip **77 entrées `index-3FmqVbc1.js`**. NON validé DSS.

### Run 5c — correctifs de l'audit à l'aveugle + parallélisme (L076)
- **Audit** : sous-agent « architecte agentique » lancé **à l'aveugle** (skill `agentique-python-dataiku`,
  copies isolées dans `/tmp/owi_agent_audit/`, interdiction d'explorer le repo / la mémoire). Audit dense et
  bien calibré (2 Critical, plusieurs High). Décision user : tout corriger + relancer à l'aveugle après.
- **Corrigés** : #1 caches keyés stable (`_tables[ds]`/`_semantic_keys[tool_id]`, plus de reset) ; #2 helper
  `_pair` + filet d'appariement tool_call↔output ; #3 repli honnête sur cap de boucle (drop du lead-in) ;
  #4 `loop_llm` nommé dans log+ERROR ; #7 garde SQL valide chaque table (FROM/JOIN + virgule) ; #10 « last
  chance » 5000 lignes fetchée 1×/req (cache) au lieu de N× ; #14 `original_intent` + note comparaison
  dégradée ; #8 `_cap_cell` aligné (NaN/inf) + test cross-fichiers ; #9 warning profil sans index ; #13 doc
  non-durable ; #17/#19 mineurs.
- **NON faits (assumés)** : #15 rename `MyLLM` (nom de classe peut-être lié au contrat Code Agent DSS) ; #12
  timeout par-tool (backend a déjà `MAX_RUN_SECONDS=300`) ; #5 découpe complète de `n_query` (refactor pur, risqué).
- **Parallélisme** : orchestrateur OK (fan-out sous-agents borné). Sous-agent : `run_parallel(tasks, max_workers=4)`
  (ordre préservé, erreur→None, instance-safe) appliqué aux fetchs fuzzy par-terme + mécanisme drop-in futurs tools.
- **Vérifs** : 209 tests agents verts. Zip **inchangé** (seuls les Code Agents ont bougé). NON validé DSS.
- **À faire** : recoller les 2 Code Agents ; relance de l'audit à l'aveugle (2ᵉ passe) pour voir s'il remonte du neuf.

### Run 5c (suite) — 2ᵉ passe d'audit à l'aveugle + correctifs (L077)
- Audit relancé à l'aveugle sur les fichiers corrigés (copies fraîches `/tmp/owi_agent_audit2/`). Findings
  NEUFS corrigés : **bypass garde SQL** `FROM"table"` sans espace (Critical, fermé via `\s*` + littéraux
  blanchis + rejet tables système) ; faux positifs blacklist `set`/`comment`/`lock` retirés ; lookup OR sur
  `alt_columns` + fallback SQL sur lookup vide ; cap de boucle garde une vraie réponse ; nudge 1×/run (flag) ;
  observabilité json-mode ; `total_customers`≠EUR. **Revert** du parallélisme de résolution fuzzy (séquentiel,
  instance safety ; cache last-chance + helper `run_parallel` gardés pour futurs tools).
- **Déclinés** (assumés) : #7 re-execute completion (validé DSS), #12 timeout par-tool (backend cap 300s),
  #15 rename MyLLM (contrat DSS), découpe n_query (différée).
- **Vérifs** : 215 tests agents verts. Zip inchangé. NON validé DSS. (Une 3ᵉ passe possible si l'user le veut.)
