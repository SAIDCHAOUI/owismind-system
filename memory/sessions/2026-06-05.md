# Session 2026-06-05 - Conversion frontend Vue 3 : Phases 0, 1, 2

> Objectif : construire le vrai frontend OWIsMind (Vue 3 + Vite) en retranscrivant fidèlement la maquette, sur une
> archi modulaire à registres, en **réutilisant** le backend/transport validés. Travail dans `Plugin/owismind/frontend/`.
> Résultat : **Phases 0-2 faites et validées en LOCAL** (dev server + screenshots Chrome DevTools + build prod temporaire).
> **Pas encore buildé dans `resource/` ni testé EN DSS** (= Phase 5).

## Étape 0 - Compréhension (avant de coder)
- Lu en première main : mémoire canonique (CONTEXT/PROJECT_STATE/LESSONS), 3 `CLAUDE.md`, frontend existant
  (`services/backend.js` 12 fns, ancien `App.vue` chat, `AdminPanel.vue`), et les 12 docs `maquette/docs/`.
- Workflow de digestion (fan-out) des gros fichiers source maquette (`app.js` 147 Ko, `workspace.js`, `admin.js`, tokens, i18n…).
- **Maquette bien présente sur le disque** (corrige l'ancienne note « à redéposer »).

## Décisions actées par l'user (AskUserQuestion)
1. **Evidence Studio DIFFÉRÉ** - V1 = chat + conversations + admin + pages ; Evidence plus tard.
2. **Thème** = light défaut + dark via tokens (fidèle maquette).
3. **Conversations** = dérivées de `/history` par `session_id`.
4. **Versioning messages** = UI-only signalé.
- Installs (par l'user) : `pinia vue-router vue-i18n markdown-it dompurify`.

## Phase 0 - Socle & primitives (✅ validé local)
- `styles/tokens.css` : theme.css **verbatim** (orange, spacing, type, radius, motion, `body[data-theme]` light/dark) + tokens
  manquants en **no-op** (`--s-1`, `--r-pill/-lg`, `--z-*`, `--success/danger/warn/info`, `--orange-soft-dark`, `--orange-on-dark`).
- `styles/base.css` : reset + scrollbar + keyframes + utils (`.mono/.muted/.u-no-shrink`).
- `components/ui/` mutualisés (la maquette les réimplémente 3-5×) : `Icon`(+`icons.js` 57 icônes), `Button`(primary/ghost/danger/link/icon),
  `Badge`(chip), `Tabs`(soulignement orange), `Menu`(ctx-menu, Escape+click-outside, placement top/bottom), `Modal`(teleport, scrim, focus),
  `ToastHost`(+`composables/useToasts.js`). + `useClickOutside`, `useReducedMotion`. Barrel `ui/index.js`.
- Validé : showcase, light+dark, modal téléporté. **Build prod temp OK** (33 modules).
- **Bug → L022** : `:global(body[data-theme="dark"]) .x` casse (descendant perdu, peignait le body) → fix `:global(…​ .x)` (sélecteur entier).

## Phase 1 - Shell + stores + routing + i18n (✅ validé local)
- **i18n** : `i18n/messages.json` (327 clés FR/EN **extraites** de `OWI_I18N` via eval Node, cf. L023) + `langs.json` ; `i18n/index.js`
  (`createI18n` legacy:false, locale persistée `owismind.lang`, `<html lang>`) ; `composables/useTr.js` (données `{fr,en}`).
- **Stores Pinia** : `ui` (thème + sidebar collapse/largeurs persistés, applique `body[data-theme]`) ; `session` (`/me`+`/agents`+
  conversations dérivées `/history`, dégradation propre hors DSS).
- **Router** : `router/index.js` `createWebHashHistory` (HASH = DSS sans réécriture SPA) ; routes chat/agents/settings/feedback/faq/
  project/admin(**gardée** `requiresAdmin`)/help ; `/`→`/chat`. Vues lazy.
- **Shell** : `AppLayout` (grille `.app` 2 col + **resize-handle** orange drag) · `Sidebar` (brand+nav+conversations+menus foot) ·
  `MainTop` (titre contextuel + toggles thème/langue). `App.vue`+`main.js` recâblés (pinia+i18n+router, thème pré-mount). `views/PagePlaceholder`.
- Validé : shell light/EN + dark/FR, bascule i18n+thème persistées, routing, menus, collapse. **Build prod temp OK** (82 modules).

## Phase 2 - Chat (cœur, ✅ validé local)
- `composables/useChatStream.js` : boucle **polling 500 ms** + `handleEvent` (8 events) **portés verbatim** de l'ancien `App.vue` ;
  front n'envoie que `{sessionId, message, agentKey}`.
- `stores/chat.js` : thread, `draft`, `sending`, `canSend` ; **modèle de versions** (regenerate → version + nav N/M, UI-only) ;
  `openSession`/`newConversation` ; refresh sidebar après envoi.
- `composables/useMarkdown.js` : markdown-it (HTML off) + **DOMPurify** (seul chemin `v-html`, réponse agent, D7).
- `registries/timelineSteps.js` : `eventKind→{label i18n,icône}` + humanizer ; labels **mergés** dans vue-i18n.
- `components/chat/` : `AgentPicker` (**repeuplé `/agents`**), `PromptBar` (textarea auto-grow, Enter=envoi, mic placeholder),
  `MessageUser`, `MessageAgent` (timeline live + corps markdown + panneau SQL + Copier/like/dislike/Régénérer + nav versions),
  `ChatThread` (auto-scroll streaming), `ChatEmpty`. `views/ChatView` câblé (vide/thread, notice `needsConfig`, sync route↔conversation).
- Validé : thread démo injecté (`window.__pinia` en DEV) - timeline (libellés FR + sous-libellés mono + durées), markdown (titre/gras/liste/
  code fence), panneau SQL (méta succès/lignes), actions + nav versions 2/2, prompt bar - **fidèle light & dark**. **Build prod temp OK** (175 modules,
  ChatView lazy avec md+DOMPurify isolés). Retiré orphelins `App.css`/`style.css`.

## Différé / signalé (cohérent docs/07)
- Édition message → **branche** (non porté ; régénération→versions fait). Voix (placeholder). Cartes de suggestion (mock éditorial).
  Like/dislike (toggle UI-only). Envoi **end-to-end EN DSS** (Phase 5).

## État & next
- **Mémoire mise à jour** : CONTEXT (focus + gotchas F1-F7), PROJECT_STATE §13 (archi/fichiers/phases/décisions), L022+L023.
- **Reste** : Phase 3 (pages secondaires, états vides), Phase 4 (Admin = réutiliser `AdminPanel.vue`), Phase 5 (build/package/**DSS**), Evidence (différé).
- Prompt de reprise pour session vide : `maquette/docs/FRONTEND_NEXT_SESSION_PROMPT.md`.

---

# Run 2 - Phases 3, 4, 5 + audit sécurité pré-DSS (✅ VALIDÉ EN DSS)

> Objectif : finir la conversion front (pages secondaires + Admin), builder/packager, **auditer** la sûreté avant tout
> upload, puis tester EN DSS. Résultat : **front Vue 3 complet validé EN DSS** (confirmation user « ça fonctionne à merveille »).

## Phase 3 - Pages secondaires (états vides honnêtes) - ✅ validé local puis DSS
- Fondations partagées `components/pages/` (+ barrel) : `PageShell` (scroll+colonne+header optionnel, prop `wide`), `EmptyState`
  (état vide honnête : icône+titre+tag « bientôt »+desc, prop `bordered`), `SettingCard`. `PagePlaceholder` **refactoré** sur `PageShell`.
- Vues : `SettingsView` (profil **réel `/me`** + thème segmenté + langue ; budget/usage = `EmptyState`), `FeedbackView` (form fidèle,
  **submit désactivé** + note ; liste vide), `FaqView` (groupes `<details>` + **recherche client ajoutée**), `AgentsView` (liste `/agents`
  enrichie par registre `agentMeta`, fiche détail, **fallback générique honnête**, CTA `selectAgent`+`/chat`), `ProjectView` (page minimale).
- Registres/i18n : `registries/agentMeta.js` (6 fiches + `resolveAgentMeta` par label normalisé), `registries/faqContent.js` (4 groupes Q/A),
  `i18n/extra.js` (chaînes Phase 3/4 mergées, `messages.json` reste pristine). Routes promues placeholder → vues dédiées. **L024**.

## Phase 4 - Admin (réutilise la logique validée) - ✅ validé local puis DSS
- `views/AdminView.vue` : console à **onglets** (`Tabs`) réutilisant **verbatim** la logique de `AdminPanel.vue` (storage + agents
  whitelist + users/flag admin, 7 endpoints). Overview = **KPIs réels** (users/agents/connexion) + carte storage ; Quotas/Activity =
  `EmptyState` « bientôt ». ~45 clés `admin.*` ajoutées (la maquette n'en avait aucune). `AdminPanel.vue`+`.css` **supprimés** (orphelins). **L025**.

## Phase 5 - Build / package / DSS - ✅ validé DSS
- `/build-plugin` → `resource/owismind-app/` (197 modules) ; `body.html` recâblé via **Write** (le `cp` refusé ; structure du build a
  changé = chunk `Icon` préchargé + CSS dédiée, pas un simple remplacement de 2 hash). `/package-plugin` → zip propre.
- **Testé EN DSS par l'user → « ça fonctionne à merveille »** = 1ʳᵉ vraie validation DSS du nouveau front (chat live, identité **avec prénom**,
  agents, history, toutes les pages + Admin).

## Audit sécurité pré-DSS (workflow 6 dimensions, adversarial) - verdict GO → 4 correctifs
- Workflow : 6 auditeurs // (sql/perf-ram-threads/auth/wiring/xss/build-leak) → vérif adversariale par finding → synthèse + complétude.
  **37 agents, GO** (0 critical/high/medium, 1 LOW). Confirmé sain : zéro DDL destructive, tout paramétré (zéro f-string SQL), whitelist
  agents serveur, scoping owner, threads daemon+cap+TTL, hooks DEV tree-shakés. Détail + correctifs → **L026**.
- **Corrigés (backend-only, repackage seul)** : (1) LOW `/me` renvoyait pas `display_name` → ajouté ; (2) bornes RAM par run
  (`MAX_ANSWER_CHARS`/`MAX_LIVE_EVENTS`, no-op normal) ; (3) garde profondeur trace (anti-RecursionError) ; (4) logs connexions INFO→DEBUG.
  Re-vérif adversariale des 4 fichiers = **CORRECT** (non-régressif). `py_compile` OK, zip repackagé propre.

## Décisions
- États vides **honnêtes** partout (zéro faux chiffre) : budget/usage, feedback, project, Quotas/Activity admin = « bientôt » étiqueté.
- Profil Settings = **réel uniquement** (`/me` : display_name/user_id/groups), pas d'email/rôle/poste inventés ; édition profil désactivée.
- Token `--orange-soft-dark` pour les teintes orange → **évite tout `:global` thème** (raffinement L022).
- Durcissements RAM/récursion ajoutés **par prudence** (l'audit prouvait déjà les bornes) car l'user a insisté sur la sûreté RAM/DSS.

## Validé / non validé
- ✅ **Validé EN DSS** (confirmation user) : front Vue 3 complet (Phases 0-5), chat live, identité (prénom), agents, history, pages, Admin.
- ✅ Validé local : screenshots light/dark + FR/EN des 5 pages + Admin (`.phase3-screens/`), console sans erreur, build prod 197 modules.
- ⏸️ **Evidence Studio** = toujours différé (décision user). Hooks/registres réservés.

## Prochaines étapes
1. **Evidence Studio** (si relancé) : `registries/artifacts.js`+`proofModes.js`, `stores/evidence.js`, `components/evidence/`, aside `AppLayout`.
   ⚠️ blocant : backend ne renvoie pas les **lignes** de résultat (`generated_sql` = SQL+row_count) → états vides/certifiés, à arbitrer.
- Notes opérationnelles DSS (cf. L026) : 1ᵉʳ admin = 1ᵉʳ à ouvrir ; modèle polling suppose backend **mono-process** ; agent sous identité webapp.
2. (Backend, option) route « set my name » (`display_name` éditable) ; multi-webapps `webapp2`.
3. Durcissements « info » restants (optionnels) : cache auth `/chat/poll`, pagination `/history`.
