# Session 2026-07-10 : restructuration du repo en miroir exact du projet DSS prod OWIsMind_PRD_V1_2

## Objectif
La prod (`OWISMIND_PRD_V1_2`, clone de DEV) est lancee et utilisee. L'user veut que le repo reflete
EXACTEMENT l'arborescence DSS (noms de fichiers/dossiers = noms des objets DSS : flow, recettes,
agents, tools, modeles semantiques), que le plugin porte la version prod (1.2.0, zip versionne),
que toute version de dev perimee disparaisse, et que la montee en version se fasse par branches git
nommees comme le projet prod. L'user a fourni les captures DSS (page projet + flow) et les listings
(agents, modeles, tools avec ids). 4 decisions validees par l'user (AskUserQuestion) : WIP v1.3 sur
branche separee ; id technique `owismind` conserve + version 1.2.0 ; miroir a la racine
`OWIsMind_PRD_V1_2/` ; mecanisme dev coexistant conserve.

## Fait
- **Branches** : WIP feedback hub v1.3 (non commite, spec `docs/superpowers/specs/2026-07-08-user-feedback-hub-v1_3-design.md`)
  sauve sur **`OWIsMind_PRD_V1_3-dev`** (`6b403ca`) ; **`OWIsMind_PRD_V1_2`** creee depuis `a07ee1e`
  (tip de `refactor/deep-clean-v1.2`). `main` DEPRECIEE (une branche par version desormais).
- **Restructure** (`5565a0f`, git mv purs) : `dataiku-agents/` -> **`OWIsMind_PRD_V1_2/`** :
  `agents/{OWIsMind_orchestrator, SalesDrive_revenue_expert, CSSO_Trouble_Tickets_Expert}.py`
  (prefixe `OWISMIND_DEV_` retire), `tools/attribute_lookup_tool.py`,
  `flow/{SalesDrive_Revenue_Expert, CSC_ticket_AI_Agent}/compute_*.py` (6 recettes aux noms DSS
  exacts = les 3 generiques dupliquees, meme code colle tel quel dans DSS, IO par cablage du Flow),
  `flow/Webapp_Zone/`, `semantic-models/{Drive_Revenues_Semantic_Model, TroubleTickets_Semantic_Model,
  scripts/}` (9 scripts), `tests/`, `registry.json`, README/CLAUDE/PLAYBOOK/DATASETS.
  SUPPRIMES (legacy) : `OWISMIND_PROD_V1/` (jumeau prod a ids distincts), `tools/promote_agents_to_prod.py`.
- **Contenu** (`b5a4ef6`, 7 agents Opus en parallele via Workflow + fixes) : README miroir (carte
  repo<->DSS + tous les ids), CLAUDE.md du miroir, registry.json (cle `OWISMIND_PRD_V1_2`, ids DEV
  conserves), PLAYBOOK, flow/README + DATASETS (colonne `Solution` restauree, 20 cols),
  Webapp_Zone/README, tools/README (4 tools : `UUoynaL` lookup, `aNxeOc4` legacy, `v4oqA6R`/`nEirlso`
  semantic query), semantic-models/README (statut des 9 scripts, re-dump revenus EN ATTENTE),
  tests repointes, CLAUDE.md racine + CONTEXT + PROJECT_STATE, regle `.claude/rules/agents.md`
  (glob `paths` -> `OWIsMind_PRD_V1_2/**`), skills package-plugin(-dev) versionnes,
  `tools/build_dev_plugin.py` (noms d'artefacts derives de plugin.json), dss-deployer, LAB
  (script.js -> `OWISMIND_PRD_V1_2`, guides), docs (runbook `DEPLOY_PROD_V1_2.md`, V1_1 SUPERSEDED,
  build-test-deploy, architecture).
- **Plugin** : `plugin.json` 1.1.0 -> **1.2.0** (id `owismind` inchange) ; zip regenere
  **`Plugin/ready-for-dataiku/owismind-v1_2-upload.zip`** (95 entrees, propre, valide par les checks
  du skill) ; zips legacy supprimes (`owismind-upload.zip`, `owismind-v1_3-upload.zip`).
- **Merge** : `OWIsMind_PRD_V1_2` -> `OWIsMind_PRD_V1_3-dev` (`5469a65`) ; conflits `tools/` resolus
  par UNION (helper de version V1_2 + identite `--version` du WIP ; zip du slot `--version` renomme
  `owismind_vX_Y-upload.zip`, forme underscore = id du plugin, pour eviter la collision avec le zip
  PROD `owismind-vX_Y-upload.zip`). Puis bump `plugin.json` **1.3.0** sur la branche dev (`680b7f8`).
- `project-documentation/` NON touche (convention : passe dediee sur demande explicite uniquement).

## Decisions
- Miroir a la racine `OWIsMind_PRD_V1_2/` (symetrie avec `OWIsMind_LAB/`), pas de dossier chapeau.
- Id plugin technique `owismind` conserve (l'id definit `/plugins/owismind/...` et l'identite de
  l'install DSS) ; c'est la VERSION + le nom du zip qui portent le versionnage.
- Nommage artefacts : prod `owismind-v<MAJ_MIN>-upload.zip` (derive de plugin.json, `.` -> `_`) ;
  dev `owismind-v<VER>-dev-upload.zip` (+ `-dev-v2`) ; slot `--version` : `owismind_vX_Y-upload.zip`.
- Recettes : mirroir = MEME code dans les 6 fichiers (constantes fallback revenue-nommees, inoffensives),
  seule la ou sortie du Flow differe. Fichiers .py du miroir modifies UNIQUEMENT en commentaires
  (chemins) ; DSS se resynchronise au prochain recollage naturel.
- `SalesDrive_AI_Agent` (`rNTZ781a`) documente LEGACY (pas de code miroir) ;
  `Drive_Revenues_resolve_filter_value` (`aNxeOc4`) documente LEGACY, suppression DSS en attente.

## Valide / non valide
- VALIDE : miroir 316 tests OK (2 skips pandas), LAB 343 OK, plugin 790 OK (les 3 suites relues) ;
  `py_compile` + `--check` + `--check --version 1.3` OK ; zip 95 entrees propre ; revue adversariale
  du diff complet (a07ee1e -> tree) : **PASS**, 0 bloquant, 3 mineurs corriges (prose staging
  PROJECT_STATE, statut tickets_semantic_query, docstrings recettes) ; neutralite comportementale
  des .py miroir PROUVEE (AST-identiques, seuls commentaires changes) ; 46/46 fichiers de
  `dataiku-agents/` comptabilises (deplaces ou supprimes volontairement).
- NON VALIDE (a faire en DSS) : upload du zip v1.2.0 (l'instance affiche encore 1.1.0) ; lancement
  `repoint_tickets_prod_clone.py` sur le clone ; suppression du tool `aNxeOc4` ; re-dump du JSON
  revenus (le `.v1.json` date d'avant le re-add `Solution`).

## Prochaines etapes
- Upload `owismind-v1_2-upload.zip` en DSS pour aligner la version affichee (runbook
  `docs/DEPLOY_PROD_V1_2.md`).
- CLONE prod : lancer `OWIsMind_PRD_V1_2/semantic-models/scripts/repoint_tickets_prod_clone.py`
  + smoke tickets + grounding `Solution` (voir sessions/2026-07-08.md).
- Re-dump `Drive_Revenues_Semantic_Model.v1.json` apres validation (script dump_semantic_model.py).
- Reprendre le feedback hub v1.3 sur `OWIsMind_PRD_V1_3-dev` (spec du 2026-07-08 ; branche deja
  fusionnee avec la nouvelle structure, plugin.json 1.3.0).
- MODEL.md / TOOL_DESCRIPTIONS.md : rafraichir le corps (etat pre-re-add `Solution`) apres re-dump ;
  verifier en DSS si la "Description for LLM" des tools semantic query a ete mise a jour le 2026-07-08.
