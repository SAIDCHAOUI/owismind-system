# Session 2026-07-10

> Run 1 (restructuration du repo en miroir exact du projet DSS prod) est documente sur la
> branche `OWIsMind_PRD_V1_2` (son propre `sessions/2026-07-10.md`, lecons L147-L148).
> Ce fichier documente le Run 2, effectue sur la branche `OWIsMind_PRD_V1_3-dev`.

## Run 2 (session de nuit) : Agent Factory v1.3

### Objectif
Consacrer la v1.3 a l'industrialisation de la creation de sous-agents specialises :
(1) recherche documentaire officielle Dataiku (jusqu'ou l'API Python permet d'automatiser
datasets / recettes / modeles semantiques / tools / Code Agents), (2) plan multi-phases
approuve par l'user, (3) implementation complete pendant la nuit (mandat autonome : tout
ce qui est sur est implemente a fond, tout ce qui est incertain est generique + gate par
les sondes Phase 0, rien ne peut nuire a l'instance ; commit + push explicitement autorises).

### Fait
- **Recherche** : workflow 19 agents (7 chercheurs Sonnet + verification adversariale +
  critique Opus + 4 complements) sur doc.dataiku.com / developer.dataiku.com /
  knowledge.dataiku.com exclusivement ; contradictions tranchees par grep sur la SOURCE du
  client officiel `dataiku-api-client-python` (verdict : `create_agent(name, "PYTHON_AGENT")`,
  `create_semantic_model`, `new_agent_tool(type)`, `create_llm_interaction_logging_dataset`
  existent tous ; les settings d'agent portent un bloc `pythonAgentSettings` par version).
  Digest complet : scratchpad de session (755 lignes, toutes URLs citees).
- **Spec** : `docs/superpowers/specs/2026-07-10-agent-factory-v1_3-design.md` (decisions
  prises en autonomie : hub = project library ; console = webapp Standard SEPAREE du plugin ;
  recettes clonees depuis les recettes validees ; seeding semantique duplicate-and-modify ;
  gates par sonde ; zero suppression).
- **Package `OWIsMind_PRD_V1_2/project-library/python/owismind_factory/`** (13 modules,
  stdlib + dataiku, compatible 3.9, dry-run par defaut, idempotent) : fctx, spec, hub,
  registry, flow_builder, semantic_builder, wizard (LLM Mesh `with_json_output` + questions
  de clarification en francais), tool_builder + agent_builder (GATES), probes (lecture seule
  + write-probe flag-gate), pipeline (13 etapes), align (repoint generique de TOUS les
  modeles d'un clone), doctor (prompt doctor, jamais d'auto-apply).
- **Notebooks 00-06** : sonde, push hub, align clone, create domain, wizard, logging, doctor.
- **Config & Prompt Hub** : `/owismind_hub/` dans la project library ; seeds repo
  (`OWIsMind_PRD_V1_2/hub/`) GENERES depuis la source de l'orchestrateur (equivalence
  byte-a-byte garantie par test) ; loaders dans les 3 agents (orchestrateur : PERSONA +
  CAPABILITIES avec validation stricte et fallback silencieux ; specialistes : regles
  additives `understand_extra.md` par domaine). Iteration de prompt = editer le hub +
  redemarrer l'agent, plus de re-paste.
- **Console webapp** (`webapps/agent-factory-console/`, webapp Standard, 4 panes) : ecrans
  Vue d'ensemble / Sonde / Nouveau domaine (plan dry-run -> execution journalisee -> wizard
  Q&A) / Prompts ; confirm:true sur toute mutation ; jobs en arriere-plan ; gates resolus
  automatiquement depuis `probe_results.json` du hub. Charte Orange : PASS (reviewer dedie).
- **Docs** : `factory-docs/README.md` (architecture), `factory-docs/DEPLOY_V1_3_DEV.md`
  (guide pas-a-pas phases A-G sur le projet clone), `factory-docs/CAPABILITY_MATRIX.md`
  (template a remplir avec la sonde), `hub/README.md`, pointeurs dans README/CLAUDE.md du miroir.
- **Qualite** : 379 tests verts (DSS-free ; anti-derive hub<->orchestrateur, generation sur
  le VRAI moteur, pipeline dry-run sur FakeProject lecture seule, equivalence des DEUX
  validateurs executes cote a cote) ; revue adversariale Opus (2 findings, corriges) ;
  revue charte Orange (PASS) ; accents francais restaures dans la console.
- **Git** : 8 commits sur `OWIsMind_PRD_V1_3-dev`, branche POUSSEE sur origin
  (autorisation explicite user cette nuit).

### Decisions
- Hub dans la project library (editable UI DSS + API publique + git) plutot que variables
  projet (ecriture tout-ou-rien) ou wiki.
- Console = webapp Standard separee du plugin Vue (dernier mot de l'user : gestion des
  agents a part pour l'instant, fusion dans une version future).
- Nouvelles capacites livrees `enabled=false` ; activation humaine apres smoke.
- Scenario de refresh cree INACTIF (trigger nocturne defini) ; premier build toujours humain.
- Ids de lecons L149-L150 (L147-L148 pris par la branche prod).

### Valide / non valide
- VALIDE localement : compilation, 379 tests, node --check, scans tirets, revues.
- PROUVE par l'historique : l'API modeles semantiques (create/new_version/save/index)
  marche sur l'instance (build_aligned 2026-06).
- NON VALIDE (attend les sondes sur l'instance) : `create_agent`/`new_agent_tool` cote
  serveur, cles exactes de `pythonAgentSettings` (injection de code), type string + params
  du tool Semantic Model Query. La factory degrade en checklists manuelles tant que la
  sonde n'a pas confirme. RIEN n'a ete execute contre DSS cette nuit.

### Prochaines etapes
1. User : cloner le projet DSS v1.2 -> v1.3-dev, puis suivre `factory-docs/DEPLOY_V1_3_DEV.md`
   phases A-D (installer la lib, sonde 00 en lecture seule, push hub 01, re-paste des 3 agents,
   verifier la neutralite comportementale, puis write-probes optionnelles).
2. Rapporter le rapport de sonde a Claude -> deverrouillage (ou non) des gates, mise a jour
   de CAPABILITY_MATRIX.md, ajustements eventuels de tool_builder/agent_builder.
3. Premier domaine end-to-end (phase F) + wizard ; puis logging + doctor (phase G).
4. Residuels v1.2 inchanges (tickets repoint sur le clone v1.2, upload zip prod, LAB).

## Run 3 : orchestration Claude + Codex/GPT-5.6 (setup outillage, aucun code produit)

### Objectif
Reproduire sur OWIsMind le setup valide sur saiplan : Claude orchestre, Codex CLI (GPT-5.6,
plugin officiel `codex@openai-codex`) execute des taches bornees, pour repartir la
consommation entre les deux abonnements.

### Fait
- `.codex/config.toml` (racine) : modele par defaut des delegations `gpt-5.6-terra`,
  `model_reasoning_effort = "high"` (surcharges Sol/Luna en commentaire).
- `AGENTS.md` (racine) : instructions natives Codex = role d'executant borne, contexte
  projet, sources de verite dans l'ordre (CLAUDE.md -> memory -> rules), les 4 commandes
  de verification reelles du repo, 8 interdits (NO INSTALL, zero action DSS, artefacts
  generes, securite SQL, memory/, pas de commit/push, charte Orange, zero tiret cadratin),
  definition of done (diff minimal, preuve de tests, resume en francais).
- `.claude/rules/model-routing.md` : doctrine de routage complete adaptee aux agents de CE
  projet (adversarial-reviewer / charte-orange-reviewer / dss-deployer / memory-curator en
  Opus, jamais Fable en fan-out ; Explore en Sonnet ou graphify d'abord ; Codex Terra par
  defaut, Sol justifie, Luna mecanique) + 4 regles non negociables + 2 pieges verifies.
- `CLAUDE.md` : regle courte de routage ajoutee dans "Style de travail".
- Hors repo : `~/.codex/config.toml` : ajout de
  `[projects."/Users/saidchaoui/projects/owismind"] trust_level = "trusted"`.
- Memoire auto (cache personnel) : fiche codex-routing-and-trust-gotcha.

### Decisions
- Base de review croisee = branche de version courante (`/codex:review --base
  OWIsMind_PRD_V1_2`), `main` etant depreciee dans ce repo.
- Review-gate Codex NON active (brulerait les limites ChatGPT a chaque tour) ; review
  croisee a la demande avant commit.

### Valide / non valide
- VALIDE (preuves executees) : `codex exec --sandbox read-only ... </dev/null` -> "OK" ;
  session Codex `"model":"gpt-5.6-terra"`, `"cwd":".../owismind"` APRES ajout du trust.
- Piege decouvert et corrige : sans trust du projet dans `~/.codex/config.toml`, la config
  projet `.codex/config.toml` est IGNOREE (le test partait en gpt-5.6-sol, modele global).
- Rien d'autre teste : aucune delegation reelle de tache de code encore faite sur ce repo.

### Prochaines etapes
- Utiliser `/codex:rescue` sur un vrai 2e chantier et `/codex:review` avant un commit pour
  valider le flux end-to-end sur ce repo.
- Sur le VPS (saiget/saive) : penser au trust du projet dans `~/.codex/config.toml`.
