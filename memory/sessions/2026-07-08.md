# Session 2026-07-08

## Run 1 - Re-ajout colonne Solution + repoint des modeles semantiques vers le clone prod

### Objectif
Sur le CLONE prod `OWISMIND_PRD_V1_2` (duplication du projet DSS DEV), depuis un notebook du clone :
1. **Revenus** : re-ajouter la colonne d'offre `Solution` (retiree du modele le 2026-06-22, les users en
   ont de nouveau besoin) + restaurer la regle de priorite d'offre, ET repointer la table vers le dataset
   du clone.
2. **Tickets** : aucun changement de modele, juste repointer la table vers le dataset du clone.
Contrainte user : l'UI DSS ne permet pas de changer la reference dataset ; economie de tokens
(sonnet/haiku pour les recherches).

### Fait
- **Archeologie git (sous-agent Sonnet)** : commit `e6ba899` (2026-06-22) = retrait de `Solution` via
  `drop_column_and_reindex.py`. Etat d'origine confirme verbatim : hierarchie `SolutionLine > Solution >
  Product` (sirano_product = code technique secondaire) ; ordre de preference **1. Product, 2. Solution,
  3. SolutionLine, 4. sirano_product**. `Solution` etait un ATTRIBUT de l'entite `commercial_offer` avec 2
  metriques (`Distinct Solutions`, `Average Products per Solution`). Golden query #4 = "IP Transit is both
  a Product and a Solution". AUCUNE golden query ne filtrait litteralement sur `Solution` (elle filtre sur
  `Product`, `Solution` seulement dans la question NL).
- **Cree `dataiku-agents/OWISMIND/add_solution_and_repoint_prod_clone.py`** : inverse exact de
  `drop_column_and_reindex.py`. Restaure `Solution` partout ou le retrait l'avait strippe (instructions =
  section "Commercial offer hierarchy" a 4 niveaux ; description entite `commercial_offer` ; attribut
  `Solution` indexe ; metrique `Distinct Solutions` ; terme de glossaire ; description attr `Product` ;
  libelle golden query #4) PUIS remap de cle projet (datasetRef + table physique) vers le clone. DRY_RUN
  (preview), re-index a l'apply, check non-fatal du schema (`Solution` present dans le dataset du clone ?).
- **Cree `dataiku-agents/OWISMIND/repoint_tickets_prod_clone.py`** : remap PUR de cle projet (aucun
  changement d'instructions / golden / attribut), DRY_RUN, re-index.
- **Gate de verification** (sans DSS) : simulation end-to-end de chaque script via un FAUX module
  `dataiku` contre les VRAIS JSON des modeles (`scratchpad/sim_revenue.py`, `sim_tickets.py`). Revenus =
  **17/17 PASS** (Solution restaure partout + 12 refs `OWISMIND_DEV` -> 0 + table repointee) ; tickets =
  **4/4 PASS**. Les 2 scripts compilent (`py_compile`), zero tiret cadratin (dash-guard).

### Decisions
- **Modele du repoint** : reecriture des 2 formes de la cle projet dans le raw config -
  `OWISMIND_DEV.` -> `<clone>.` (datasetRef logique) ET `OWISMIND_DEV_` -> `<clone>_` (table physique des
  golden queries). Cle cible DERIVEE du projet courant (`project.project_key`), modele resolu par NOM
  (`MODEL_ID=""`, robuste a une reattribution d'id).
- Ne restaurer QUE la metrique `Distinct Solutions` (calquee sur `Distinct Solution Lines` existante, donc
  surement valide) et PAS `Average Products per Solution` (expression d'origine jamais committee -> risque
  de metrique invalide, non necessaire a la colonne ni a la regle).
- Auto-detection de la cle source (au lieu du hardcode `OWISMIND_DEV`) PROPOSEE puis REVERTEE : l'user
  posait juste une question, ne demandait pas un refactor. `SOURCE_PROJECT_KEY="OWISMIND_DEV"` conserve =
  simple terme de recherche a remplacer.

### Valide / non valide
- **VALIDE DSS (script revenus)** : l'user l'a execute sur le clone. Preuve : `datasetRef =
  OWISMIND_PRD_V1_2.DRIVE_Revenues`, SQL genere `FROM "OWISMIND_PRD_V1_2_drive_revenues"`. => Solution
  restauree + table repointee, cote clone. **Cle du clone CONFIRMEE = `OWISMIND_PRD_V1_2`** ; le clone
  garde les ids DEV (modele revenus = `AHUh9hb`).
- Clarification donnee a l'user : `datasetRef` (forme LOGIQUE `CLE.Dataset`, avec un point, casse
  preservee) vs table PHYSIQUE (`CLE_dataset`, underscore, minuscules) = 2 formes correctes distinctes
  (convention Dataiku standard), les deux bien repointees. Rien a corriger.
- **NON confirme DSS** : execution du script tickets (ecrit + simule seulement).

### Prochaines etapes
- Lancer `repoint_tickets_prod_clone.py` sur le clone + smoke tickets end-to-end.
- Tester dans le Playground du modele revenus un terme d'offre au niveau `Solution` (grounding).
- Mise en coherence repo : `OWISMIND_PROD_V1/` + `promote_agents_to_prod.py` legacy vs le clone reel.
