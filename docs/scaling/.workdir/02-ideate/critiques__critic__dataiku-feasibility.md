All load-bearing claims verified. The artifacts table has exactly three payload columns (`exchange_id`, `user_id`, `artifacts`) with no `data` column; the sub-agent imports only stdlib + `dataiku` + `langgraph` (zero plugin imports, confirming the standalone constraint). I have enough to write the adversarial critique.

---

# ADVERSARIAL CRITIQUE - Lens: Dataiku Fidelity & Technical Feasibility

The five proposals are unusually well-grounded (file/line citations check out), but they collectively launder several **UNVERIFIED platform assumptions into "verified-feasible" claims**, and at least two would break a **frozen contract** if implemented as written. Severity-ranked.

## BLOCKER

**B1. `tool-exposure` Class 2 (`render_chart`/`render_table`) and `deliverables` Phase 4 both rest on an `exchange_id` handoff that does not exist in the foreign-agent path - and the proposed schema fix breaks the artifacts contract.** The artifacts table has exactly `(exchange_id, user_id, artifacts, created_at)`; there is **no `data` column** (verified). `chart_payload.build_chart_payload` requires `result.get("captured")` from our `generated_sql` - a foreign agent never populates that. The `tool-exposure` brief is honest that this is NEEDS-DSS-VALIDATION, but `deliverables` Phase 4 (`build_report`/`draft_email` agent tools "flow the proven seam `_normalized_artifact_event` -> `save_artifacts`") quietly assumes the **same `exchange_id` + in-process LangGraph stream** that ONLY exists for OUR orchestrator. A plugin agent tool's `invoke()` runs in the DSS tool runtime with **no `get_stream_writer`, no `state["latest"]`, no exchange_id** - `_normalized_artifact_event` is never invoked for it. So Phase 4 as written cannot work for any agent except our own orchestrator, where it's pointless (the orchestrator already has built-ins). The `tool-exposure` "new `_v2` table with inline `data`" is the honest fix, but `deliverables`' claim that deliverable artifacts "flow the already-proven seam" is **over-claimed and must be downgraded to NEEDS-DSS-VALIDATION**. MUST spike the exchange_id handoff before any of this is sequenced as "feasible."

**B2. Every proposal that adds a new event kind or extends `_ARTIFACT_KINDS` is touching a FROZEN contract and will be silently dropped without a coordinated change.** `webapp-capabilities` #3 (new `SUGGESTIONS` event kind), #8 (`confidence` field), and `deliverables`' `"deliverable"` kind all assume you can "append to the existing whitelist." But the artifact normalizer is gated on `eventKind == "ARTIFACT"` AND `ctype in _ARTIFACT_CHART_TYPES = ("line","bar","pie")` (verified streaming.py:63,163), and `_sanitize` rejects `kind not in _ARTIFACT_KINDS` (verified artifacts.py:57). Adding a kind requires editing **three coupled places in lockstep** (`_ARTIFACT_KIND`/`_ARTIFACT_CHART_TYPES` in streaming, `_ARTIFACT_KINDS`/`_sanitize` in artifacts, the frozen `KNOWN_*` anti-drift test on the agent side) - and the sub-agent's `KNOWN_BLOCK_IDS`/`KNOWN_TOOL_NAMES` are guarded by a test that **fails the build** if labels drift. None of the proposals acknowledge that a new event kind on the *agent* side must be mirrored in the *Code Agent* files (recoll, env 3.11) AND the anti-drift test updated. This is a multi-file frozen-contract change masquerading as "append one item." MUST be called out as a coordinated contract bump, not a one-liner.

## HIGH

**H1. `webapp-capabilities` #4 (AI title) and `agent-factory` Path A both casually require things that are install-gated or frozen, without flagging loudly enough.** The AI-title proposal says "fire a single eco-model summarization in the `start_run` thread" - but `start_run` runs in the **Flask 3.9 backend**, which CAN call Mesh (it already does indirectly), so this is feasible; however the proposal also floats "a small `webapp_conv_meta_v1` table or a derived title field via no-ALTER `_vN`" - the `_vN` pattern means a **whole new table copy**, not a column add, so "derived field" is misleading. Minor but the plan must pick the table, not hand-wave. **Path A (`owismind_specialist` packaged lib) is correctly flagged as install-gated**, but its deeper problem is fidelity: it "partially breaks the standalone-paste property" - and the standalone constraint is not a preference, it's how DSS Code Agents are deployed here (verified: zero plugin imports). `agent-factory` correctly recommends Path B (codegen) instead. Good - but the codegen's "engine-body identical CI test" assumes the canonical engine never legitimately diverges per domain; the moment one domain needs a different node (e.g. a duration metric for tickets, which `domains-and-360` itself flags as a `metric_unit`/`format_cell` schema risk), the "byte-identical engine" invariant breaks and codegen must template *inside* the engine, not just the header. This tension is unacknowledged.

**H2. `domains-and-360`'s "clone `SalesDrive_revenue_expert.py` -> `Tickets_expert.py`, change ~7 constants" understates the calibration AND ignores that non-currency/duration metrics may hit untested code paths.** The proposal itself notes "verify `metric_unit`/`format_cell` handle non-currency + a duration metric" - but `metric_unit` derives currency from the **column name** (`amount_eur→€`); a ticket count or MTTR-in-hours has no currency column, so the formatter falls through to defaults that have **never been exercised in DSS**. This is a real NEEDS-DSS-VALIDATION, not a footnote. Also: tickets datasets are frequently **multi-table by nature** (ticket + status-history), colliding with the absolute ONE-TABLE/no-JOIN rule - the proposal flags this for *delivery* but waves it through for *tickets*. The one-table assumption must be validated **per dataset before** committing a session budget, exactly as the proposal's own closing flag says - so the "FIRST, ~2 sessions, lowest risk" ranking is optimistic until the dataset shape is confirmed.

**H3. `tool-exposure` Track C (MCP) and any "DSS 14.0+" claim is UNVERIFIED on this instance and correctly parked - but `deliverables`/`webapp-capabilities` PDF claims about the env are NOT validated.** All three PDF proposals assert "ReportLab is pure-Python, runs on 3.9, just `pip install`." True in general, but **which** code env? The Flask backend is 3.9.23; the Code Agents run in a **separate 3.11 env**; a *plugin agent tool* runs in yet **another** code env whose Python version is UNVERIFIED (`tool-exposure` honestly flags this as spike #2). `deliverables` puts PDF generation in the **backend** (3.9) - fine - but `tool-exposure`'s `build_pdf` is a **plugin tool** (unknown env). These are *different runtimes with different installed packages*; "install ReportLab" must specify **which env**, and the matplotlib-Agg "almost certainly already present" claim is an assumption, not a fact. MUST verify per-env before sequencing.

## MEDIUM

**M1. `webapp-capabilities` #14 / `domains-and-360` B1 semantic cache "checked in `/chat/start` before `start_run`" has a correctness landmine.** The cache key is "(question + resolved_filters)" but resolution happens **inside** the sub-agent run (RESOLVE node, after `attribute_lookup`), not at `/chat/start`. You cannot key on *resolved* filters before the run that resolves them. `domains-and-360` partially fixes this ("key on resolved filters post-`attribute_lookup`"), placing the check in `_run_subagents` instead - correct, but then it's a **per-sub-agent** cache, not a per-conversation one, and the two proposals disagree on placement. Also "invalidate on data refresh" has no hook today (datasets refresh via Flow scenarios the webapp doesn't observe) - staleness is real. Defer until metrics justify (both proposals say so; good).

**M2. `tool-exposure` Track A `query_<domain>` "runs as the calling user (access governance)" is asserted, not verified.** When the Flask backend calls `get_agent_tool(id).run()`, it runs **as the API key / project identity**, not the end user. Whether per-user access governance is even possible through this path is UNVERIFIED. The blocking/sync `invoke()` returning a JSON string is correctly noted, but the identity claim must be downgraded.

**M3. `deliverables`/`webapp-capabilities` email: even "draft-only + mailto" is fine, but the `requires_confirmation` server gate must not regress non-negotiable #4.** Both correctly defer SMTP. Good. The only risk is the deliverable spec storing `to_hint`/`body_html` in the artifacts JSON, which is fine (bounded), as long as no recipient/send decision ever comes unsanitized from the front.

## LOW

**L1. `webapp-capabilities` #1/#2 client-side PNG/CSV are genuinely zero-risk and correctly the first ship.** No notes except: the CSV "rows already in the `evidence` store" path only works if `/evidence/meta` delivered the full result; for capped/truncated results the client CSV silently under-exports. Prefer the server route reading captured `generated_sql[].result` for fidelity (the proposal offers both; pick server for correctness).

**L2.** `domains-and-360`'s "single up-front `attribute_lookup` resolve so all sub-agents filter the same canonical entity" is the single best correctness idea in the set, but the orchestrator currently resolves lookups **per sub-call**, not once up-front - this is a real (small) orchestrator change, not free.

---

## TOP 5 THINGS THE FINAL PLANS MUST CHANGE OR CAVEAT

1. **SPIKE the `exchange_id` foreign-agent handoff FIRST** (one afternoon on the instance). Until it passes, `render_chart`/`render_table` for foreign agents and ALL "agent emits a deliverable artifact" claims are NEEDS-DSS-VALIDATION, not feasible. The fix requires a NEW `_v2`/parallel table with inline `data` (no `data` column exists today) - never an ALTER (#6).
2. **Treat every new event kind / artifact kind as a coordinated FROZEN-CONTRACT bump**, not an append: `streaming.py` (`_ARTIFACT_KIND`/`_ARTIFACT_CHART_TYPES`), `artifacts.py` (`_ARTIFACT_KINDS`/`_sanitize`), AND the agent-side `KNOWN_*` anti-drift test must change in lockstep, with a Code Agent recoll (env 3.11).
3. **Specify WHICH code env each install targets.** Backend = 3.9.23 (PDF/CSV/XLSX server code lives here). Code Agents = separate 3.11. Plugin agent tools = a third, UNVERIFIED env. ReportLab/openpyxl/matplotlib presence must be verified **per env**; "almost certainly present" is not a fact. Never WeasyPrint (system libs, violates #1).
4. **Validate the ONE-TABLE assumption and the non-currency `metric_unit`/`format_cell` path per dataset before committing a domain to a session budget.** Tickets/opportunities may be multi-table (no JOIN allowed) and duration/count metrics hit untested formatter code. The "tickets = lowest risk, 2 sessions" ranking is conditional on this.
5. **Fix the semantic-cache key placement**: it must live in `_run_subagents` keyed on POST-resolution filters (per sub-agent), not in `/chat/start` on pre-resolution filters. And it has no data-refresh invalidation hook today - keep it deferred behind real cost metrics.

## GENUINELY GOOD IDEAS WORTH AMPLIFYING

- **Backend-as-tool-caller (Track B)** is the highest-leverage, lowest-risk insight: `get_agent_tool(id).run()` from Flask 3.9 is **verified working in-process** (orchestrator line 1293), so PDF/CSV/PNG export needs **no agent, no LLM, no quota burn, no exchange_id** - ship it first.
- **Client-side `toBase64Image()` PNG** - verified the instance is held and never called; truly zero-install, zero instance-load.
- **Claim-vs-result reconciliation (`evidence/reconcile.py`, pure 3.9 regex)** appears in four proposals as the P0 anti-hallucination guard - correctly placed where `generated_sql[].result` is already joined; this should be the governance floor before any second agent.
- **Codegen Path B over packaged-lib Path A** for the agent factory - honors the standalone-paste DSS deployment reality absolutely, no install, instant rollback.
- **Resolve-the-account-ONCE up-front for the 360** - the right defense against each sub-agent resolving "Airbus" differently; just budget it as a real orchestrator change.